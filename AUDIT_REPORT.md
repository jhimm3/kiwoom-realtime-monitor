# 기능개발서와 실제 구현 감사

감사 기준: 2026-09-09 작업 트리. `README.md`, 기술명세서 v2.0, AI 인수인계서 v3.3, NAS 설계서, 릴리스 노트와 `src/`, `tests/`, `deploy/`를 대조했다.

환경 확인: 2026-09-09 승인된 제한 밖 실행에서 Python 3.13.15, 원본 `.venv`의 PySide6 6.11.1과 FastAPI 0.141.1이 정상임을 재확인했다. 앞선 본체 삭제 진단을 정정한다. 제한 환경에서의 접근 거부가 실행 실패 원인이었으며 재설치는 필요하지 않다.

최신 추가 검증: 실시간 0B·분봉 보완·기간/역사적 신고가·기본정보·NXT 확인·신고가 목록·KRX 카탈로그·TOP20 시장 보완·이미지 OCR·Google Drive·앱 업데이트 worker controller와 메인 표 배치·열 설정·표시 계산, 매매일지 기간 요약·비용/순손익/유형 표시 화면 모델·묶음/개별 체결 차트 선택 범위·수동 묶음 편집 명령·분봉 보완 대상/선택 차트 갱신·일봉 캐시/표시 대상/최신 대기 요청 판정·시장지수 분봉/일봉 원자적 저장 및 실패 재시도·최신 대기 요청·뉴스창 공통 JSON 명령 채널·중앙 동기화 실행 수명·체결/비용 및 분봉/상태 원자적 저장, 중앙 DB 문서/스냅샷 공통 codec·coverage 판정·범위 조회·인증 API 및 로컬 메인·매매일지 관측 메타데이터 연결을 포함한 핵심 회귀 457개가 48.955초에 종료 코드 0으로 통과했다. `test_main_window` 전체 15개도 반복 실행에서 드러난 생성 직후 종료와 예약 API 설정창의 수명 경계를 최소 보강한 뒤 9.724초, 종료 코드 0으로 통과했다.

검증 결과: 원본 프로젝트의 가상환경을 실행기로 사용하되 작업 트리 `src`를 우선하도록 고정했다. 이전의 본문 `OK` 뒤 Windows 네이티브 종료 오류는 닫힌 Qt 객체가 지연 삭제되지 않은 테스트 수명 문제로 재현됐고, `test_detached_chart_window`와 `test_main_window`가 `DeferredDelete`까지 처리하도록 보강했다. 메인 창은 종료 뒤 남은 0ms API 설정 예약도 무시한다. 현재 메인 화면 15개와 핵심 회귀 457개가 모두 종료 코드 0으로 통과한다.

## 문서와 구현 불일치

| 분류 | 문서 주장/누락 | 실제 구현 | 조치 |
| --- | --- | --- | --- |
| 문서가 오래됨 | 기술명세서가 저장소를 단일 `data/monitor.db`로 설명 | `monitor.sqlite3`, `news.sqlite3`, `journal.sqlite3`로 분리 | `ARCHITECTURE_CURRENT.md`, `DB_SCHEMA.md`에 현재 구조 기록 |
| 문서가 오래됨 | 기술명세서가 “상장 후 전체 기간 최고가 제외” 및 5/20/250일만 설명 | 연·월·일봉 근거를 이용한 역사적 신고가와 `historical_high_evidence` 구현 | 현재 지도에 반영; 기술명세서는 과거 제품 요구 문서로 취급 |
| 문서가 오래됨 | 기술명세서 API 표가 순위·봉·기본정보·NXT 중심 | 수급, 프로그램매매, 시장지수, 계좌 체결/비용, NAS REST/WebSocket 추가 | API 계약과 모듈 지도에 반영 |
| 목표와 현재 차이 | NAS 설계 상단은 PostgreSQL을 유일 원본으로 설명 | 앱은 여전히 로컬 SQLite에 먼저 저장하고 중앙과 병합하며 오프라인 캐시로 사용 | 목표 문서와 현재 문서를 분리해 명시 |
| 릴리스 기준 갱신 | 1.1.20 이후 중앙 서버, 장애전환, 중앙 뉴스/AI/테마/일지 동기화, 자원 진단이 작업 트리에 구현 | 2.0.0 버전·README·릴리스 노트에 반영 | 완료 |
| README 갱신 | README 소개가 직접 Kiwoom·Google Drive 중심이었음 | NAS 데이터 모드와 중앙 서버 실행/배포가 존재 | 2.0.0 릴리스에서 현재 사용자 기능 설명과 문서 링크를 갱신 |
| 이름 혼재 | 코드/문서에서 `중앙 서버`, `개인 시놀로지`, `개인 서버`, UI의 `NAS`가 같은 축을 가리킴 | 내부 enum은 `personal_server`, 사용자 표시는 NAS | 코드 식별자는 호환을 위해 유지하고 문서에서는 “NAS/중앙”으로 정의 |
| 이름 혼재 | `신고가`가 기간 최고가/역사적 최고가를 함께 가리킴 | 선택 헤더 모드에 따라 계산 근거가 다름 | 모듈 지도에서 일봉 신고가와 역사적 신고가 서비스를 분리 |
| 실제 검증 미완료 | NAS 설계가 중앙 실시간 연결 완료라고 기술 | 코드/자동 테스트는 있으나 문서 자체가 장중 병행 실측을 후속으로 표시 | “구현 완료”와 “실데이터 검증 완료”를 분리 유지 |

## 장기 로드맵 대비 구조 감사 후 조치

- TOP20 거래대금의 분 마감 저장 조건이 현재 차트의 날짜·1분/5분/60분 모드에 영향받지 않도록 수집 표본 갱신을 화면 분기보다 먼저 수행한다. 5분봉 화면에서도 다음 분 경계에 직전 1분 값이 저장되는 회귀 테스트를 추가했다.
- 사용자 전략팩 평가 결과를 만들 때 `warnings`와 `unverifiable` 위치 인수가 뒤바뀌던 오류를 이름 인수로 수정하고 두 필드가 분리되는 테스트를 추가했다.
- TOP20 코호트·구간 기준값·분 마감·종료 시 부분 기록 상태를 `application/top20_trade_value_collector.py`로 옮겼다. `MainWindow`는 실시간 값 공급, 저장과 화면 표시만 연결하므로 차트 모드 변경이 수집 상태를 소유하지 않는다.
- 실시간 순위 뒤의 저우선순위 분봉→일봉→기본정보→신고가/NXT worker 완료 순서를 `SecondaryDataFollowupCoordinator`로 모았다. 순위 준비 중 양보 조건과 실제 TR worker는 그대로 두고 순서 계약만 단위 테스트로 고정했다.
- 순위 회차 사이에 유지되는 밀린 요청·부분 응답 재시도·모달 중 최신 응답·마지막 순위 상태를 `RankingExecutionCoordinator`로 옮겼다. Qt 창 없이 가짜 시계와 가짜 순위 응답으로 예약·중복 요청 병합·재시도·지연 반영을 검증하며, 화면 렌더링은 `MainWindow`에 남아 있다.
- 0B/0w 구독의 시작·동일 구독 유지·실행 중 종목 교체·장 종료·이전 worker 정리 판단을 `RealtimeSubscriptionCoordinator`로 옮겼다. 순위 QThread의 생성과 신호 연결은 `RankingWorkerController`가 소유하며 `MainWindow`에는 Qt 신호의 화면 반영만 남겼다.
- 분봉·일봉·시장 상태·후보군을 함께 전달할 `MarketDataObservation` 계약을 추가했다. 거래소, 단위, 실제/추정/파생, 진행/확정/부분/미확정, 시장 기준 시각과 정보 가용 시각을 분리하며, 기존 상태 문자열과 시각이 없는 행은 임의로 확정하거나 당시 이용 가능했던 값으로 승격하지 않는다. 기존 DB 저장은 아직 바꾸지 않았다.
- 체결 스냅샷은 필드별로 실시간 관측/장후 보완/누락을 판정한다. 수급 응답 객체가 있어도 `available=false`이면 확인된 자료로 보지 않으며, 나중에 연결한 뉴스와 장후 수급에는 `backfilled` 출처를 남긴다.
- 매매일지 UI 안에 중첩돼 있던 회차별 스냅샷 연결, 매수 진입 자료 선택과 판정 불가 항목 계산을 순수 application 모듈로 분리했다. 분석 결과와 표시 문구는 유지한다.
- 회차별 자동분석 본문과 진입 스냅샷·시장 상태 표시 문구를 Qt 비의존 포맷 모듈로 분리하고 기존 출력 계약을 단위 테스트로 고정했다.
- 하루 종합·대표 뉴스·연결 데이터 상태 표시를 같은 포맷 경계로 옮겼고, 기본/사용자 전략팩의 복기 참고자료 선택을 순수 application 서비스로 분리했다.
- 매매일지 UI가 직접 수행하던 스냅샷 DB 조회와 뉴스 시간 범위 검색·장후 저장을 persistence 서비스로 분리했다. 기존 조회 범위와 오류 처리 방식은 유지한다.
- 기본분석과 사용자 전략팩 후보 평가, 최종 표시 결과 선택, 수동 회차 유형·과거 유형 이전을 application 조정 서비스로 옮겼다. 매매일지 UI는 저장과 위젯 반영만 담당한다.
- `_show_trade_analysis`가 직접 조정하던 메인 DB 일봉 준비, 자동 판정 저장, 과거 단일 수동 유형의 회차별 이전, 전략 초안 수 집계를 `application/trade_analysis_preparation_service.py`로 분리했다. 화면 메서드는 분석 실행과 위젯 반영만 남겼다.
- `reload_history`와 필터 메서드가 직접 수행하던 기간 체결·비용 조회, 과거 진입 연결용 회차 구성, 표시 기간 교집합 및 종목·손익·복기 필터를 `application/trade_history_query_service.py`로 분리했다. Qt 표 생성과 선택 복원은 화면에 유지했다.
- 회차별 분석기의 입력 구성과 수동/자동 판정 분기, 스냅샷 기반 판정 가능 범위 보정을 application 서비스로 옮겼다. 분석 계산식 자체와 저장 시점은 바꾸지 않았다.
- 기간 손익·비용 반영 요약과 매매 묶음 표 15개 열, 매매유형 요약, 회차 편집 행, 분석 데이터 상태와 전체 분석 문서를 Qt 비의존 화면 모델로 만들도록 분리했다. `journal_process.py`는 저장소 조회와 위젯·선택 상태 반영 중심으로 축소됐다.
- 묶음과 개별 체결 선택 시 사용할 종목·기준일·초점·다일 범위를 기존 기간조회 모듈의 순수 정책으로 모았다. 본창은 결과를 차트와 백필 흐름에 적용하고 복기 자동저장은 현재 위치에 유지했다.
- 수동 묶음 합치기·체결 분리·자동분류 복원의 동일 종목 검증, 체결키 생성, 수동 ID 발급과 저장 명령을 전용 application 쓰기 경계로 옮겼다. 화면은 선택 행 수집·결과 메시지·성공 후 목록 재조회만 담당한다.
- 매매일지 분봉 보완의 20시 기준 대상일, 확정/실패 필터, 묶음 상태 문구와 체결시각 범위 누락 판정을 기존 기간조회 application 모듈의 순수 정책으로 옮겼다. worker 생성·신호·화면 갱신은 본창에 유지해 추가 수명 계층은 만들지 않았다.
- 분봉 보완·일반 확인 뒤 현재 선택 차트와 자동분석을 갱신하는 중복 흐름을 본창의 한 메서드로 합쳤다. 동일 차트 자료를 두 번 읽던 호출은 한 번으로 줄이고, 보완 날짜가 선택 범위에 속하는지 판정은 Qt 비의존 정책으로 고정했다.
- 일봉 캐시와 API 수신 결과의 본 차트·실시간 차트·분리 차트 표시 분기를 본창의 한 적용 메서드로 합쳤다. 표시 대상과 과거 250개 캐시 재사용 기준은 기존 차트 application 모듈의 순수 정책으로 옮기고 `test_trade_chart`를 핵심 회귀에 포함했다.
- 일봉 worker 실행 중에는 가장 최근 요청 한 건만 보관하고 종료 직후 정확히 한 번 이어서 실행하는 상태 전이를 회귀로 고정했다. 현재 구현은 두 상태와 두 전이뿐이므로 별도 Queue/Controller를 만들지 않았다.
- 5·20·250일/역사적/당일 신고가의 화면 선택 우선순위와 ka10001 기본정보 갱신 시 KRX+NXT 수정주가 보존 규칙을 `application/high_price_policy.py`로 분리했다. UI 없이 출처 우선순위를 검증할 수 있게 했다.
- 기존 메인 SQLite의 버전 1 원장을 이름·적용 시각을 갖는 v2 기준선으로 확장하고, 버전 원장이 없던 매매일지 SQLite에는 v1 기준선을 추가했다. 중앙 SQLite/PostgreSQL에도 양쪽 방언을 하나의 계획으로 적용하는 v1 기준선을 추가했다. 실행기는 연속 버전·변경 불가 이름·실패 savepoint 롤백·지원 범위보다 새로운 DB 거부를 담당한다.
- 뉴스 기사·매매일지 연결 기사·AI 결과·요청 사용량의 테이블 생성 책임을 `persistence/news_schema.py`로 모으고 뉴스 DB v1 기준선을 추가했다. 기사 저장소와 AI 저장소 어느 쪽을 먼저 열어도 같은 스키마를 확인하며 기존 뉴스·AI 행은 보존한다.
- SQLite 연결 종료와 쓰기 commit/rollback을 보장하는 작은 공통 경계를 추가하고 뉴스 기사·AI 저장소에 먼저 적용했다. Windows 파일 잠금과 예외 시 쓰기 롤백을 독립 테스트로 고정했으며, 특수 timeout·읽기 전용 URI를 쓰는 매매일지/봉 저장소는 일괄 변경하지 않았다.
- 로컬 메인 v3·매매일지 v2 마이그레이션으로 공통 시장 관측 메타데이터 표를 추가했다. 값 본체와 분리해 실제 시각·가용 시각·시장·출처·완결성을 저장하며, 기존 행은 미래 정보 유입을 막기 위해 자동 보정하지 않는다. 실제 수집기와 중앙 저장 연결은 후속 단계다.
- 중앙 SQLite/PostgreSQL도 기존 v1 기준선을 유지한 채 v2 메타데이터 표를 추가했다. 로컬·중앙 저장소는 같은 codec으로 시간대와 보수적 미확인 복원 규칙을 공유한다.
- 중앙 순위·TOP20 편입/지수·시장 상태 생산자는 값과 메타데이터를 같은 트랜잭션으로 저장한다. KST 기준시각과 실제 가용시각을 분리하고 TOP20 부분 수집 상태를 확정으로 승격하지 않는다.
- 중앙 조회/실시간 분봉과 일봉 생산자도 같은 원자적 저장 경계를 사용한다. 실시간 현재 분봉과 장중 당일 일봉은 진행 중으로 남기고, 과거/장후 조회 봉만 확정한다. 메타데이터 저장 실패 시 봉만 단독으로 남지 않는 롤백 회귀를 추가했다.
- 로컬 메인과 매매일지의 신규 분봉·일봉 저장도 공통 관측 계약에 연결했다. 메인 0B 실제 대금과 ka10080 계산 대금, 일지의 부분/조회/장후 보완 출처를 구분하고 값·메타데이터 원자성 및 확정 상태 비강등을 회귀로 고정했다. 기존 봉은 추정 보정하지 않는다.
- 체결 스냅샷의 복합 JSON은 순위·대금·테마·뉴스·수급·프로그램·호가·시장 등 필드별 `entry_context` 관측으로 분리했다. 장후 보완은 체결 기준시각과 실제 가용시각을 따로 남기며, 값과 메타데이터가 함께 롤백되고 보완 출처가 임시 재저장으로 낮아지지 않게 했다.
- 전체 회귀 종료 뒤 간헐적으로 발생한 Windows 네이티브 오류를 `test_detached_chart_window`로 좁혔다. 실제 앱 창의 재사용 동작은 유지하고 테스트에서 닫힌 창·설정 대화상자의 `deleteLater`와 지연 삭제 이벤트를 처리해 Qt 객체 파괴 순서를 고정했다.

## 기능개발서에 있으나 완료로 볼 수 없는 항목

- NAS 장중 직접/중앙 결과의 장시간 병행 실측, 단절·복구, 다른 PC 접속 검증은 코드 옵션과 자동 테스트가 있지만 실제 운영 검증 기록이 없다.
- NAS 설계의 “PostgreSQL 유일 원본”은 로컬 우선 병합 구조 때문에 아직 최종 상태가 아니다.
- 기술명세서의 실행파일 배포 후속 문구는 README의 기존 GitHub 릴리스와 충돌한다. 문구가 과거 시점에 머물러 있다.

## 구현됐으나 기준 문서에 충분히 반영되지 않은 항목

- NAS 자원 진단과 운영 설정 API.
- 로컬 중앙 서버 모드와 개인 NAS 모드의 query/realtime 장애전환·병행검증.
- 중앙 범용 콘텐츠 컬렉션과 앱/테마/뉴스/매매일지 증분 동기화.
- TOP20 통계, 다중 차트, 전략팩 등 1.1.20 뒤의 세부 수정은 릴리스 노트 한 줄보다 구현 범위가 크다.

## 레거시·중복·분산

- `presentation/main_window.py`에 있던 `choose_similar_stock` 중복 정의는 감사 직후 첫 정리 단계에서, 실제 호출되는 뒤 정의를 유지한 채 가려진 앞 정의만 제거했다.
- `themes/stock_themes`와 프로필형 `theme_profiles/profile_*`가 함께 존재한다. 최초 이전/호환 경로가 있어 사용 여부 계측 없이 삭제하면 안 된다.
- `local_server` 모드는 상위 설정 화면에서 숨겨진 곳과 상세 NAS 설정에서 노출되는 곳이 달라 사용자 명칭과 내부 경로가 어긋난다. 현재 테스트가 있으므로 삭제 대신 사용 여부를 먼저 결정해야 한다.

### 2026-09-12 보류 항목 종결 감사

- `local_server`와 `personal_server`는 화면 선택 문구가 아니라 기존 `data_source.json` 및 장애전환과의 호환 식별자다. 기본 설정 UI는 로컬/NAS 두 선택지만 표시하므로 내부 값 일괄 변경은 실익보다 이전 위험이 크다.
- 실제 사용자 DB에서 구형 `stock_themes` 773행과 현행 `profile_stock_themes` 2,040행이 확인됐다. 현행 저장소는 프로필 표만 편집하며 구형 표는 최초 이전과 구형 백업 복원에 남아 있으므로 삭제하지 않는다.
- 작은 worker controller는 수명·중복 실행·신호 형식·후속 요청을 소유하고 있어 단순 전달 계층으로 확인되지 않았다. `main_window.py`·`stock_news_window.py`도 이번 감사에서 추가 분리의 독립 책임과 결함 감소 근거가 없어 현재 유지한다.
- PostgreSQL과 SQLite의 행 codec·스키마 계획은 공통화됐고 남은 SQL 차이는 DB 방언 경계다. 실제 PostgreSQL 왕복 검사 결과 없이 더 합치지 않는다.
- 뉴스 중앙 동기화는 `bootstrap.py`, `news_process.py`, 여러 `central_*sync.py`에 나뉜다. 프로세스 경계상 일부 분산은 필요하지만 주기/충돌 규칙의 단일 조정 지점이 없다.
- 매매일지 기능은 application 모듈로 일부 분리됐지만 UI·차트·그리기·백필·중앙 동기화 조립이 `journal_process.py`에 집중돼 있다.

## AI 수정비용이 큰 영역

| 분류 | 파일/모듈 | 문제 | 왜 수정비용이 큰지 | 예상 영향범위 | 권장 대응 | 위험도 |
| --- | --- | --- | --- | --- | --- | --- |
| 나중에 분리 권장 | `presentation/main_window.py` (정리 전 7,335줄, 현재 4,753줄) | 초기 위젯 조립과 순위 결과의 실제 표 반영 순서가 여전히 큼 | UI 구성 변경 시 같은 파일의 영향 범위가 넓음 | 메인표 화면 조립 | 순위 실행·구독·후속 조회·worker 수명·표 계산은 분리 완료했다. 남은 화면 순서를 Manager로 더 감싸지 말고 전체 GUI 회귀 확보 후 UI 조립부만 검토 | 중간 |
| 현재 유지해도 됨 | `journal_process.py` (정리 전 3,771줄) | 큰 Qt 본창이지만 저장·분석·차트 계산·worker 구현은 외부 경계로 분리됨 | 남은 메서드는 주로 위젯 상태와 사용자 동작 순서를 조정 | 매매일지 본창 | 체결/비용, 시장지수, 분봉/상태 원자적 저장까지 완료했으므로 추가 계층화는 중단하고 GUI 회귀에서만 관찰 | 중간 |
| 나중에 분리 권장 | `presentation/stock_news_window.py` (현재 약 1,000줄) | 선택 상태와 worker 신호 연결이 한 창에 남음(worker 구현·설정·표시·자동분석 정책·종료 정리 분리 완료) | 목록 갱신 수정이 선택 복원과 신호 연결에 영향 | 뉴스 선택/작업 실행/창 상태 | 더 큰 coordinator 도입은 현 테스트 범위가 넓어진 뒤 검토 | 중간 |
| 현재 유지해도 됨 | `persistence/journal_database.py` (현재 약 40줄) | 스키마 초기화와 snapshot/strategy/trade/bar 저장소 조립, 종목 열람 기록만 담당 | 각 저장 영역의 수정 범위가 독립 파일로 좁혀짐 | 매매일지 저장소 조립 | 공개 `JournalRepository` 호환 facade로 유지 | 낮음 |
| 현재 유지해도 됨 | `central_server/database.py` (SQLite/PostgreSQL 병렬 구현) | 연결·트랜잭션·placeholder와 일부 CRUD SQL은 방언별로 남음 | 양쪽 수정 시 계약 확인이 필요함 | 모든 NAS 데이터와 배포 | 봉·메타데이터·문서/스냅샷 codec과 문서 조회 조건은 공통화했다. 실제 PostgreSQL 통합 테스트 전 실행 계층 통합은 보류 | 중간 |
| 현재 유지해도 됨 | 중앙 콘텐츠 동기화 묶음 | 공통 배치 업로드·문서 envelope·테이블 확인은 `central_sync_utils.py`로 통합했고 데이터별 충돌 규칙은 분리 유지 | 뉴스·테마·설정·일지는 삭제 전파와 버전 기준이 서로 다름 | 다중 PC 동기화 | 단일 sync engine으로 무리하게 합치지 말고 공통 유틸리티와 개별 계약 테스트 유지 | 낮음 |
| 현재 유지해도 됨 | `application/*_service.py` | 기능별 작은 계산 서비스 | 입력/출력 경계와 단위 테스트가 비교적 명확 | 해당 계산 기능 | 독립 모듈 원칙 유지 | 낮음 |
| 현재 유지해도 됨 | `central_server/rest_broker.py` | 중앙 큐 책임이 집중됨 | 크기가 작고 우선순위/캐시 테스트 가능 | 전체 TR | 공개 상수와 계약 테스트 유지 | 중간 |

## 과추상화 후보 추적

현재 리팩터링을 중단해 재통합하지 않고, 완료 후 별도 감사할 후보만 기록한다.

| 후보 | 현재 근거 | 지금 조치 |
| --- | --- | --- |
| `MainWindow`의 `_restore_columns`, `_save_columns`, `_resize_columns_proportionally`, `_enable_initial_column_auto_fit` 호환 메서드 | 실제 책임은 `MainTableColumnController`로 이동해 일부가 한 줄 위임이다. 다만 기존 Qt 신호와 내부 호출자의 안정된 접점 역할을 한다. | 전체 GUI 회귀 전에는 유지하고, 마지막 감사에서 직접 연결 시 파일 추적 깊이가 실제로 줄어드는지 확인한다. |
| 기능별 `*_worker_controller.py` 묶음 | worker 형식 검증·현재 객체 수명·중복 실행 방지는 실제 책임이지만 37~85줄의 유사한 패턴이 여러 파일에 반복된다. | 범용 Controller를 즉시 만들지 않는다. 리팩터링 종료 후 중복량과 기능별 완료 문맥 차이를 비교해 명시적 파일 유지와 공통 수명 도우미 중 더 단순한 쪽을 선택한다. |
| `MainWindow`의 worker 호환 속성 | 종료 목록과 기존 호출자를 위해 controller의 worker를 그대로 노출한다. 계산이나 정책은 없다. | 메인 종료 흐름의 GUI 회귀가 마련될 때 함께 제거 가능성을 검토한다. |

현재 유지 근거가 명확한 계층:

- `MainTableColumnController`: 열 표시·순서·폭의 저장 변환, 복원 중 신호 상태, 자동 맞춤 준비 여부와 수동 유지 시간을 함께 소유한다.
- `SecondaryDataFollowupCoordinator`: 분봉→일봉→기본정보→신고가/NXT의 실행 순서와 장후 분기를 소유한다.
- `main_window_layout`: Qt 객체 없이 배치 계산을 검증하는 순수 함수 묶음이며 메인 창과 열 조정 객체가 함께 사용한다.
- `JournalWindow`의 일봉 최신 대기 요청 상태: 현재 요청 1개와 종료 후 소비라는 두 전이만 있어 별도 큐 객체보다 본창의 명시적 필드가 더 짧고 추적하기 쉽다.
- `MainWindow._on_ranking_loaded`: 응답 채택 판단은 `RankingExecutionCoordinator`, 실시간 대상은 `RealtimeSubscriptionCoordinator`, 후속 보완은 `SecondaryDataFollowupCoordinator`, 셀 계산은 `main_table_formatting`에 있다. 남은 코드는 캐시를 읽어 Qt 표를 갱신하고 구독을 이어주는 화면 순서이므로 별도 Manager로 감싸지 않는다.
- `MainWindow.__init__`의 UI 조립: 길이는 크지만 객체 생성과 Qt 신호 연결이 중심이다. UI 구조를 실제로 재설계하기 전에는 Builder 계층을 추가하지 않는다.

시장지수 보완의 실패·동시 요청 흐름은 별도 재현 테스트를 만든 뒤 수정했다. 저장에 성공한 날짜만 세션 완료 목록에 넣고, 실행 중 다른 날짜 요청은 최신 한 건을 보관해 worker 종료 후 이어서 처리한다. 상태가 현재 worker·활성일·최신 대기일뿐이므로 별도 Controller는 추가하지 않았다.

매매일지의 뉴스창 요청은 이미 존재하는 `JsonCommandChannel`의 단조 증가 요청번호와 원자적 파일 교체 계약을 재사용하도록 했다. 별도 매매일지 뉴스 Service를 만들지 않아 호출 깊이를 늘리지 않았다.

매매일지 중앙 동기화의 중복 실행 상태와 daemon 스레드 수명·오류 격리는 기존 `central_journal_sync.py` 안의 실행기로 이동했다. 병합 서비스와 별도 파일을 추가하지 않았고, UI는 60초 주기 예약과 로그 문구만 소유한다.

키움 체결 조회 결과의 체결과 실제 비용은 매매일지 거래 저장소의 한 트랜잭션으로 반영한다. 비용 저장이 실패하면 같은 조회에서 받은 체결도 롤백하며, UI는 결과 검증·완료 문구·목록 재조회만 담당한다.

장후 분봉 보완은 대상 날짜의 봉만 선별해 분봉·관측 메타데이터·확정/실패 상태를 한 저장 트랜잭션으로 반영한다. 성공한 결과만 현재 선택 차트와 분석을 갱신한다. 이 경계까지를 `journal_process.py` 필수 구조 정리의 종료점으로 삼고, 복기 폼 등 단순 위젯 조정은 본창에 유지한다.

## 테스트 감사

기존 테스트가 이미 다음을 보호한다.

- TR 간격/재시도: `test_kiwoom_rest_client.py`
- 중앙 요청 병합/캐시/허용 TR: `test_central_rest_broker.py`
- 실시간 순위·구독: `test_ranking_service.py`, `test_realtime*.py`, `test_central_realtime_*.py`
- NAS API/DB: `test_central_server_app.py`, `test_central_server_database.py`
- 로컬 DB: 각 repository/database 테스트
- 테마: `test_theme_*.py`
- 거래대금: `test_minute_trade_value.py`, `test_minute_bar_repository.py`
- 매매일지: `test_journal_database.py`, `test_journal_schema.py`와 `test_trade_*.py`
- 뉴스/Gemini: `test_news_ai.py`, `test_news_ai_repository.py`, `test_news_auto_analysis.py`, `test_news_execution.py`, `test_news_view_model.py`, `test_central_ai_*.py`

이번 감사에서는 공백이었던 “대기 중인 역사 백필보다 순위가 먼저 실행”, “공개 NAS method/path 집합”, “서로 다른 관측 시각의 순위 원본 보존”을 회귀 테스트로 추가했다. TOP20 거래대금은 한 분의 합계뿐 아니라 00초·30초 코호트별 구성 종목도 함께 남는지 검증한다.

핵심 영역만 빠르게 다시 확인할 때는 `scripts/run_core_regression.ps1`을 실행한다. 전체 검증은 기술명세서의 `unittest discover` 명령을 유지한다.

## 현재 리팩터링 종료선

1. `main_window.py`: 순위 실행·실시간 구독·후속 worker 수명·표 계산 분리와 전체 메인 화면 GUI 회귀를 완료했다. 남은 위젯 조립과 화면 반영 순서는 추가 Manager로 감싸지 않는다.
2. `journal_process.py`: 저장·분석·차트 계산·worker 구현과 원자적 저장 경계를 분리했다. 남은 본창 위젯 조정은 현재 위치를 유지한다.
3. `central_server/database.py`: 문서/스냅샷 codec 중복을 공통화했고 연결·트랜잭션·SQL 방언은 실제 PostgreSQL 경계로 분리 유지한다.

따라서 기능 보존 중심 리팩터링은 여기서 종료하는 것이 적절하다. 후속 구조 변경은 Astra 과추상화 감사 결과와 실제 운영 증거가 있는 항목만 별도 작업으로 진행한다.

## 2026-09-11 안정성 후속과 종료 진행표

최종 감사에서 코드 흐름과 회귀로 확인된 저장 재시도, 실제/추정 거래대금 충돌, 빈/오래된 NAS 캐시, 수동 유형 저장 원자성, 중앙 삭제 재생성, QThread 완료 수명, PID 재사용 위험, 로컬 분봉 저장 실패와 열 설정 복원 중복 저장을 최소 수정했다. 핵심 회귀 470개와 `test_main_window` 17개가 각각 종료 코드 0으로 통과했다.

추가 구조 변경은 즉시 이어서 하지 않는다. 남은 순서와 미실행 항목은 `REFACTORING_CLOSEOUT_PLAN.md`를 단일 진행 원장으로 사용하며, NAS 소스는 마지막 단계까지 누적한 뒤 한 번만 배포한다.

## 2026-09-12 최종 종료

종료 단계에서 핵심 회귀는 477개로 확대되어 모두 통과했다. `2026.09.12-market-state-time-v1` NAS 배포 후 PostgreSQL v3 주요 저장소 왕복·롤백, WebSocket, `T88:88` 보정, 실제 NAS 중단 시 로컬 키움 전환과 NAS 자동 복귀를 확인했다. 최종 판정과 후속 운영 검증 범위는 `reports/REFACTORING_CLOSEOUT_REPORT.md`를 따른다.
