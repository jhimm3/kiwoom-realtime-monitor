# Changelog

이 파일은 앞으로의 변경을 짧게 누적한다. 배포판별 상세 사용자 변경은 `docs/RELEASE_NOTES_v*.md`에 유지한다.

형식:

```text
## [Unreleased]
### Added / Changed / Fixed / Removed / Security
- 사용자 또는 개발자에게 의미 있는 한 줄 (관련 문서/이슈)
```

## [Unreleased]

## [2.0.0] - 2026-09-12

- 상세 내역: `docs/RELEASE_NOTES_v2.0.0.md`

### Fixed

- PyInstaller 창 모드 실행 파일이 이 PC의 로컬 중앙 서버를 자식 프로세스로 시작할 때 표준 출력 스트림 부재로 Uvicorn 로깅 초기화가 실패하던 패키징 결함을 수정했다.
- ETF·ETN처럼 KRX 법인 목록으로 시장을 채울 수 없는 종목이 TOP20에 보일 때 전체 상장종목 2,700여 건을 계속 다시 저장하던 조건을 제거했다. 자동 KRX 목록 갱신은 잔여 미분류와 무관하게 성공 날짜 기준 하루 한 번만 실행되어 메인 표의 SQLite 잠금 대기를 반복시키지 않는다.
- 실패 후 남은 테마 업로드 대기 자료가 앱 시작 때 무조건 NAS를 덮어쓰지 않도록, 로컬 변경 시각과 NAS `theme_metadata` 완료 시각을 비교해 최신 스냅샷을 선택하도록 수정했다.

### Added

- NAS 경유 신고가 목록, 종목 기본정보·시가총액, NXT 가능 여부를 중앙 DB의 시점별 스냅샷으로 보존하고 기본정보·NXT는 종목별 최신 문서도 유지하도록 했다.
- 중앙 시장 관측 메타데이터를 시간 범위로 조회해 완전/부분/누락과 가용 기준시각을 판정하는 인증 coverage API를 추가했다. 무체결일 수 있는 분봉·일봉에는 임의 주기 결측 추론을 금지한다.
- 로컬 메인·매매일지 DB에 공통 `market_data_observation_meta` 명시적 마이그레이션과 저장소를 추가했다. 기존 행은 추정해 채우지 않으며, 같은 관측 키의 정정만 upsert한다.
- 중앙 SQLite/PostgreSQL 스키마 v2에 `central_market_data_observation_meta`를 추가하고 양쪽 DB가 로컬과 동일한 관측 의미 codec을 사용하도록 했다.
- 중앙 순위·TOP20 편입/거래대금 지수·KOSPI/KOSDAQ 시장 상태를 저장할 때 원본 스냅샷과 관측 시각·가용 시각·출처·완결성을 같은 트랜잭션에 기록하도록 연결했다.
- 중앙 조회/실시간 분봉과 일봉도 봉과 메타데이터를 원자적으로 저장한다. 실시간 현재 분봉과 장중 당일 일봉은 진행 중, 과거/장후 조회 봉은 확정으로 구분하며 조회 분봉의 계산 거래대금은 추정임을 출처에 남긴다.
- 뉴스 DB의 분산된 테이블 생성 코드를 단일 스키마 모듈로 모으고 v1 마이그레이션 기준선과 기존 기사·AI 결과 보존 회귀를 추가했다.
- 중앙 SQLite와 PostgreSQL이 동일한 계획으로 사용하는 v1 스키마 기준선과 `central_schema_migrations` 원장, 실패 시 DDL까지 되돌리는 회귀 테스트를 추가했다.
- 메인·매매일지 SQLite에 공통으로 사용할 명시적 스키마 마이그레이션 실행기와 현재 스키마 기준선, 기존 데이터 보존·멱등 실행·실패 롤백·신버전 거부 회귀 테스트를 추가했다.
- 봉·시장 상태·후보군의 거래소·단위·실제/추정/파생 여부·완결 상태·시장 기준 시각·정보 가용 시각을 표현하는 공통 데이터 계약과 기존 상태 문자열 어댑터를 추가했다.
- NAS가 데스크톱 앱과 무관하게 평일 08:00~20:00 TOP20 순위·0B·1분 거래대금 지수를 수집하고 당일 전체 편입 종목을 누적 보존하는 서비스를 추가했다.
- TOP20 편입 종목의 장후 당일 분봉·최근 250일 일봉 보완, coverage 기록 및 완료된 NAS 차트 우선 재사용을 추가했다.
- 시점별 시장자료와 향후 시뮬레이션 개발 순서를 `FUTURE_DEVELOPMENT_ROADMAP.md`에 기록했다.
- Yahoo 지연 시세의 `MNQU26.CME`·`CLV26.NYM` 5분봉/일봉을 NAS에 누적하는 독립 수집기와 조회 API를 추가했다.
- 외부 선물 전월물·차월물을 함께 보존하고 거래량 우위를 연속 확인해 대표 월물을 자동 교체한다. 월물 간 가격 차이는 등락률에 섞지 않고 각 월물 전일 종가 기준 방향값을 저장한다.
- NAS 이미지 태그와 `/health` 빌드명을 `2026.09.10-auto-roll-v1`로 올리고, 롤오버 소스가 없는 빌드 컨텍스트는 빌드 단계에서 거부한다.
- 현재 구현 아키텍처, 모듈 지도, NAS API 계약, DB 지도와 개발 불변 규칙을 추가했다.
- 과거 순위·봉·시장·테마를 시점 기준으로 결합하기 위한 `HISTORICAL_DATA_CONTRACT.md`와 순위 스냅샷·TOP20 코호트 보존 회귀 테스트를 추가했다.
- 중앙 TR 순위 우선순위와 공개 NAS 라우트 계약 회귀 테스트를 추가했다.
- 핵심 보호 영역만 실행하는 `scripts/run_core_regression.ps1`을 추가했다.
- 이 PC의 Python 설치 위치와 제한 환경의 실행 권한 문제를 개발 불변 규칙에 기록했다.
- Codex 작업 트리에서 Git 원본 프로젝트의 `.venv`를 자동 탐색하도록 핵심 회귀 실행 도구를 보완했다.

### Changed

- NAS 이미지 `2026.09.12-shared-backup-contract-v1`를 재빌드하고 `/health`, 인증 콘텐츠 API, WebSocket ready/pong 및 저장된 순위 스냅샷 조회를 실환경에서 확인했다.
- Google Drive의 설정/테마/둘 다 선택 모두 뉴스 AI 분석 결과를 별도 파일로 보존한다. NAS에는 메인 표의 표시·순서(폭 제외), 매매일지-뉴스 연결, 활성 프로필·별칭·종목 목록을 포함한 전체 테마 문서를 추가로 동기화한다.
- 일반 설정 백업에서 테마 DB와 PC별 창 위치·크기·열 너비를 분리하고, Google Drive 설정도 NAS와 같은 공통설정 제외 정책을 사용하도록 통일했다. 테마 파일은 활성 프로필을 포함한 전체 프로필을 계속 별도로 보존한다.
- 매매일지 본창과 따로보기의 일봉을 매매 종료일에 자르지 않고 현재 조회 가능한 최신 거래일까지 표시하되, 최초 초점은 매매 체결일에 두도록 바꿨다.
- 그리기 끄기 상태에서 차트를 마우스로 끌어 과거·최신 구간을 이동할 수 있게 했다. 이동 때 봉 수 변경 신호가 잘못 발생해 공유 상태가 즉시 위치를 되돌리던 문제와 따로보기 현재 구성 이미지 저장의 누락 import 오류도 수정했다.
- 뉴스 AI 판정 규칙 버전 변경 뒤 기존 분석 최대 100건을 앱과 NAS가 자동 재분석하던 경로를 차단했다. 자동분석은 새 identity만 처리하고 과거 결과 재분석은 사용자가 명시적으로 요청할 때만 수행한다.
- NAS가 AI 공급자의 429/5xx를 모두 502로 뭉개지 않고 실제 상태와 안전한 상세 원인을 클라이언트에 전달하도록 했다.

- 설정 파일과 Google Drive 테마 백업이 구형 `themes/stock_themes` 대신 NAS와 같은 최신 테마 프로필 구조를 사용하도록 통일했다. 활성 프로필과 테마 가져오기 규칙도 함께 복원하며 기존 version 1~3 백업은 계속 읽는다.
- 테마 변경 시 Google 동기화 대상 설정과 무관하게 설정+테마 업로드를 예약하고, NAS 공통 설정에도 활성 테마 프로필 상태가 반영되도록 했다.
- 매매일지 본창과 차트 따로보기에서 같은 종목·같은 봉 주기의 화면 봉 수와 선·텍스트를 공유한다. 따로보기의 별도 차트 설정 버튼은 본창 공통 설정을 열도록 바꾸고 예전 전용 색 설정은 렌더링에 사용하지 않는다.
- 복기 이미지의 별도 `이미지 저장 봉 수`와 `분봉·일봉 함께 저장` 설정을 제거했다. 현재 본창에 보이는 차트, 봉 수, 일봉 함께 보기 상태를 그대로 이미지에 저장한다.

- NAS 이미지 `2026.09.12-market-metadata-gaps-news-ai-v1`를 재빌드하고 `/health` 빌드, 인증 API, WebSocket ready/pong 및 새 메타데이터 조회 경로를 실환경에서 확인했다.
- NAS의 키움 실시간 원본이 끊긴 동안 TOP20 지수를 0이나 로컬 값으로 채우지 않고 분 행을 비워, 재연결 후 차트가 해당 구간을 `수집 중단`으로 표시하도록 했다.
- 뉴스 AI의 요약·전망·근거를 선택 종목의 직접 영향 관점으로 제한하고, 종목이 단순 나열된 기사에는 `판단 자료 부족`을 사용한다. 이전 분석 계약의 캐시는 보존하되 새 자동분석 완료 결과로 재사용하지 않는다.
- 리팩터링 종료까지 남은 4단계, 현재 단계, NAS 누적 배포 시점과 증거 부족으로 보류한 항목을 `REFACTORING_CLOSEOUT_PLAN.md`에 고정하고 후속 작업이 이 원장을 먼저 확인하도록 했다.
- 누적 NAS 소스를 `2026.09.11-db-codec-v1`로 동기화하고, 실행 서버 `/health`에서 같은 빌드와 정상 상태를 확인했다. 기존 `.env`, `postgres-data`, `server-data`는 보존했다.
- 중앙 SQLite/PostgreSQL 문서 저장 행, JSON 복원, 문서 조회 조건과 데이터셋 스냅샷 복원을 공통 DB codec으로 통합했다. 연결·트랜잭션·placeholder 등 방언별 SQL 실행은 실제 PostgreSQL 검증 전까지 명시적으로 분리 유지한다.
- 중앙 서버 빌드 식별자를 `2026.09.11-db-codec-v1`로 올리고 서버·Compose·Dockerfile 검증 문자열을 일치시켰다.
- 메인 창 최종 구조 감사에서 정리 전 7,335줄이 현재 4,753줄로 줄었고, 남은 대형 구간은 초기 위젯 조립과 순위 결과의 화면 반영 순서임을 확인했다. 정책·worker 수명·표 계산은 이미 분리돼 있어 추가 Manager 분리는 중단하고 현재 구조를 GUI 회귀 대상으로 확정했다.
- 매매일지 장후 분봉 보완 결과의 대상일 필터, 분봉·관측 메타데이터·확정/실패 상태를 저장소의 단일 트랜잭션으로 반영하도록 정리했다. 저장 성공 후에만 선택 차트와 분석을 갱신한다.
- 키움 체결내역 동기화 결과의 체결과 실제 비용을 매매일지 저장소의 단일 트랜잭션으로 반영하도록 정리했다. 비용 저장 실패 시 체결만 먼저 남지 않으며, 기존 개별 저장 API와 저장 후 화면 재조회 순서는 유지한다.
- 매매일지 본창이 직접 소유하던 중앙 동기화 중복 실행 상태·daemon 스레드 생성·오류 후 상태 해제를 기존 중앙 매매일지 동기화 모듈의 실행기로 옮겼다. 동기화 주기, 로컬 우선 병합과 실패 시 로컬 자료 유지 동작은 바꾸지 않았다.
- 매매일지의 관련 뉴스 열기 요청이 UI에서 요청번호와 임시 JSON 파일 교체를 다시 구현하지 않고, 기존 보조 프로세스 공통 명령 채널을 사용하도록 정리했다. 뉴스 요청 필드와 사용자 메시지는 유지했다.
- 매매일지의 코스피·코스닥 보완 결과를 화면에서 분봉·시장별 일봉으로 나눠 저장하던 흐름을 기존 시장봉 저장소의 단일 트랜잭션으로 합쳤다. 저장 중 하나라도 실패하면 분봉과 일봉을 모두 되돌리는 회귀를 추가했다.
- 일봉 worker 실행 중 여러 차트 요청이 오면 마지막 요청 하나만 보관하고, 종료 후 정확히 한 번 실행하는 기존 상태 전이를 회귀 테스트로 고정했다. 두 줄 상태를 위한 별도 Queue/Controller는 추가하지 않았다.
- 일봉 캐시와 API 수신 결과를 본 차트·실시간 차트·분리 차트에 적용하는 중복 흐름을 합쳤다. 과거 복기/분리 차트의 250개 캐시 재사용 및 대상별 표시 규칙을 테스트로 고정하고 차트 테스트를 핵심 회귀에 포함했다.
- 분봉 보완과 일반 확인 뒤 선택 차트·자동분석을 갱신하는 중복 코드를 합치고, 한 번 읽은 동일 차트 자료를 양쪽에 사용하도록 했다. 선택 종목·날짜 범위가 다른 보완 결과는 화면을 갱신하지 않는 규칙을 테스트로 고정했다.
- 매매일지 분봉 보완의 대상일·확정/실패 필터·묶음 상태·체결시각 누락 판정을 Qt 비의존 정책으로 분리했다. worker 실행 순서와 DB 저장·화면 갱신은 변경하지 않았다.
- 매매일지의 수동 묶음 합치기·체결 분리·자동분류 복원에서 검증, 체결키 생성, 수동 묶음 ID 발급과 저장을 전용 쓰기 서비스로 분리했다. 잘못된 선택은 DB를 수정하지 않는 회귀를 추가했다.
- 매매일지에서 묶음 또는 개별 체결을 선택할 때 차트에 사용할 종목·초점·날짜 범위·관련 체결 계산을 기존 기간조회 모듈로 옮겼다. 다일 보유 범위와 현재 묶음 우선 규칙을 테스트로 고정했다.
- 매매일지 본창에 섞여 있던 기간 손익·실제/예정 비용 요약, 매매 묶음 표 문자열과 회차별 수동 유형 표시 계산을 기존 Qt 비의존 화면 모델로 옮겼다. 저장소 조회·표 선택·위젯 반영 순서는 유지하고 실제/예정 비용 회귀를 추가했다.
- 새 계층은 독립 책임이 있을 때만 만들고 단순 위임·호출 깊이 증가를 피하도록 개발 규칙을 강화했다. 버그 수정은 호출 흐름과 증거로 원인을 확인한 뒤 최소 수정·관련 회귀를 수행하도록 절차를 명시했다.
- 현재 소형 worker controller와 메인 창의 호환 위임 메서드를 과추상화 후보로 기록했다. 리팩터링 도중 재통합하지 않고 완료 후 별도 감사한다.
- 실시간 0B WebSocket worker의 생성·구독 갱신·중지와 9개 Qt 신호 연결을 `RealtimeWorkerController`로 분리했다. 시간대별 구독 대상 판단과 화면 반영 순서는 유지한다.
- 분봉 보완 worker의 생성·형식 검증·신호 연결과 완료 문맥 전달을 `MinuteHistoryWorkerController`로 분리했다. 대상 선정과 순위 우선 양보, 저장·화면 반영은 기존 순서를 유지한다.
- 기간 신고가 일봉 worker의 생성·형식 검증·수신/실패/완료 신호 연결을 `DailyHighWorkerController`로 분리했다. 수정주가 갱신 대상과 장후 확정 계산은 기존 흐름을 유지한다.
- 기본정보 worker의 생성·형식 검증·수신/실패/완료 신호 연결과 다음 보완 단계 문맥을 `FundamentalsWorkerController`로 분리했다. 시가총액·유통비율 갱신과 수정주가 신고가 보존 정책은 유지한다.
- 역사적 신고가 worker의 생성·형식 검증과 수신/실패/완료 신호 연결을 `HistoricalHighWorkerController`로 분리했다. 조회 대상·수정주가 기준 버전·DB 저장·화면 표시는 기존 흐름을 유지한다.
- NXT 거래 가능 여부 worker의 생성·형식 검증과 수신/실패/완료 신호 연결을 `NxtEligibilityWorkerController`로 분리했다. 대상·캐시 저장과 KRX 동기화/실시간 구독/후속 보완 순서는 유지한다.
- 신고가 목록 갱신 worker의 생성·형식 검증과 성공/실패/종료 신호 연결을 `NewHighWorkerController`로 분리했다. 갱신 주기·가격 병합·화면 반영과 NXT 후속 단계는 유지한다.
- KRX 상장종목 카탈로그 worker의 생성·형식 검증·중복 실행 방지와 신호 연결을 `KrxStockCatalogWorkerController`로 분리했다. 자동·수동·후속 실행별 메시지, 종목명 변경 확인과 역사적 신고가 후속 흐름은 유지한다.
- TOP20 시장 구분 보완 worker의 생성·형식 검증·저우선순위 실행과 신호 연결을 `Top20MarketRepairWorkerController`로 분리했다. 30초 실행 제한, DB 재계산과 현재/과거 차트 갱신은 유지한다.
- 이미지 테마 OCR worker의 생성·형식 검증·저우선순위 실행·취소와 진행/완료/실패 신호 연결을 `ImageThemeOcrWorkerController`로 분리했다. 진행창, 결과 검토와 테마 반영 방식은 유지한다.
- Google Drive worker의 생성·형식 검증·중복 실행 방지·결과 전 수명 해제와 신호 연결을 `GoogleDriveWorkerController`로 분리했다. 메타데이터 확인 뒤 다운로드, 최초 업로드와 종료 시 업로드 흐름은 유지한다.
- 앱 업데이트 확인·다운로드 worker의 생성·형식 검증·신호 전달·취소와 현재 작업 수명을 `UpdateWorkerController`로 분리했다. 자동 확인의 조용한 실패 처리, 진행창과 설치 순서는 유지한다.
- 메인 표의 반응형 행 높이·비례 열 너비·내용 맞춤 창 크기와 저장 좌표 해석을 부작용 없는 `main_window_layout` 계산으로 분리했다. Qt 이벤트와 사용자 설정 저장 시점은 유지한다.
- 메인 표 열의 표시 여부·순서·폭 저장/복원, 비례 자동 맞춤과 수동 조절 후 30초 유지 상태를 `MainTableColumnController`로 분리했다. 기존 `column_settings` 형식과 메뉴·Qt 이벤트 동작은 유지한다.
- 별도 계층을 늘리지 않고 메인 표의 행 배경 우선순위·순위 강조 시간·소수점 제한·등락률 색상 계산을 기존 `main_table_formatting` 모듈로 옮겼다. 화면 적용과 타이머 동작은 유지한다.
- 체결 스냅샷의 순위·1/5분 거래대금·테마·신고가 거리·뉴스·수급·프로그램·호가·시장 상태를 필드별 `entry_context` 관측으로 분리해 같은 트랜잭션에 기록한다. 장후 보완 시각을 체결 시각으로 가장하지 않고, 보완값은 뒤이은 임시값으로 강등하지 않는다.
- 로컬 메인의 실시간/조회 분봉·일봉과 매매일지의 부분/확정 분봉·일봉도 값과 관측 메타데이터를 같은 SQLite 트랜잭션에 저장한다. 0B 실제 거래대금과 ka10080 추정 거래대금을 구분하고, 확정 일지 봉은 이후 부분 복사로 강등하지 않는다.
- 뉴스 기사·AI 저장소의 반복된 SQLite 연결/종료/commit 코드를 공통 읽기 연결·쓰기 트랜잭션 경계로 교체하고 예외 시 롤백과 Windows 파일 잠금 해제를 검증했다.
- 0B/0w 실시간 구독의 시작·유지·종목 교체·장 종료 판단을 `RealtimeSubscriptionCoordinator`로 분리하고, 순위 QThread 생성과 신호 수명을 `RankingWorkerController`로 옮겼다.
- 실시간 순위의 밀린 조회 병합, 부분 응답 재시도 횟수, 설정창 사용 중 최신 응답 보류와 마지막 순위 상태를 `RankingExecutionCoordinator`로 분리하고 가짜 시계·가짜 응답 회귀 테스트를 추가했다.
- 매매일지의 기간 체결·비용 조회와 과거 진입 연결용 회차 구성, 종목·손익·복기 필터를 `TradeHistoryQueryService`로 분리하고 검색 기간 밖 완결 매매 제외 규칙을 회귀 테스트로 고정했다.
- 매매일지 분석 준비와 기간 조회 조정 서비스 테스트를 핵심 회귀 실행 목록에 포함했다.
- 실시간 순위 뒤 저우선순위 보완 조회의 분봉→일봉→기본정보→신고가/NXT 완료 순서를 `SecondaryDataFollowupCoordinator`로 분리하고 장후·마감확정 우선순위를 회귀 테스트로 고정했다.
- 매매일지 자동분석 화면이 직접 수행하던 일봉 준비·자동 판정 저장·과거 단일 수동 유형 이전·전략 초안 집계를 전용 application 조정 서비스로 분리하고 기존 저장 순서를 회귀 테스트로 고정했다.
- 앱 시작 5초 뒤 `ka10001` 기본정보가 화면 메모리의 KRX+NXT 수정주가 250일 신고가를 KRX 단독값으로 덮어쓰던 문제를 수정했다. 시가총액·유통비율은 갱신하되 신고가는 현재 일봉 합산값 또는 마지막 정상 캐시를 유지한다.
- 5·20·250일·역사적·당일 신고가의 화면 선택 및 기본정보 보완 우선순위를 `application/high_price_policy.py`로 분리하고 Qt 비의존 회귀 테스트를 추가했다.
- NXT 일봉이 오류가 아닌 빈 목록으로 잠시 응답해도 마지막 정상 KRX+NXT 250일 신고가를 유지해, 앱 새로고침 직후 SK하이닉스 등이 KRX 단독 최고가로 내려가는 현상을 막았다.
- NAS 장후 보완은 NXT 일봉 0건을 완료로 기록하지 않으며, 과거에 남은 0건 완료 캐시는 키움 일봉 조회를 막지 않도록 복구 경로를 추가했다.
- NXT 빈 일봉 완료 판정 수정이 포함된 NAS 이미지 태그와 `/health` 빌드명을 `2026.09.10-nxt-daily-v2`로 올렸다.
- NAS 서버 변경 때 `SERVER_BUILD`·Compose 이미지 태그·Dockerfile 검증 문자열을 반드시 함께 올리고 `/health`로 확인하도록 개발 불변 규칙과 배포 체크리스트를 보강했다.

- NAS 서버 소스가 바뀌면 저장소 전체 동기화 후 서버 이미지를 다시 빌드하고 `/health`의 `server_build`로 반영 여부를 확인하도록 배포 절차를 명확히 했다.

- 뒤 정의에 가려져 실행될 수 없던 `main_window.py`의 중복 `choose_similar_stock` 정의를 제거했다. 실제 호출되는 구현과 동작은 유지한다.
- TOP20 차트·전용 창·시장구분 DB 보완 worker를 `presentation/top20_trade_value.py`로 분리했다. 실행 시점과 신호 계약은 유지한다.
- 메인 순위표의 열 표시·순서 편집 대화상자를 `presentation/column_manager_dialog.py`로 분리했다. 저장 방식과 적용 시점은 유지한다.
- 키움 API·NAS 연결 설정창과 NAS 자원 확인 worker를 `presentation/api_settings_dialog.py`로 분리했다. 기존 import 호환성과 연결 계약은 유지한다.
- 기본 설정창 본체를 `presentation/settings_dialog.py`로, 공통 앱 표시 정보를 `presentation/app_metadata.py`로 분리했다. 기존 설정 키·저장 시점·화면 문구는 유지한다.
- 테마 가져오기·미리보기·편집·색상·일괄 관리·프로필 대화상자를 `presentation/theme_dialogs.py`로 분리했다. 테마 저장소와 사용자 확인 흐름은 유지한다.
- Google Drive 동기화와 앱 업데이트 확인·다운로드 worker를 `presentation/background_workers.py`로 분리했다. 스레드 신호와 실행 순서는 유지한다.
- 뉴스·매매일지 보조 프로세스의 생존 확인과 원자적 명령 파일 기록을 `presentation/process_control.py`로 분리하고 회귀 테스트를 추가했다.
- 설치본/개발환경별 뉴스·매매일지 실행 명령 구성과 숨김 프로세스 실행을 `presentation/process_control.py`로 통합했다.
- 뉴스·매매일지의 정상 종료·대기·종료 요청·강제종료 순서를 `presentation/process_control.py`로 통합하고 단계별 테스트를 추가했다.
- 뉴스·매매일지 명령의 요청 번호 증가와 JSON 기록을 `JsonCommandChannel`로 통합했다. 뉴스는 순차 번호, 매매일지는 기존 시간 기반 번호를 유지한다.
- 매매일지에서 뉴스창으로 전달되는 요청 파일의 읽기·형식 검증·중복 차단을 `JsonRequestInbox`로 분리했다.
- 뉴스창의 실행 프로세스 참조·실행 여부·시작·종료를 `AuxiliaryProcessManager`로 묶었다. 매매일지는 다음 단계에서 동일 구조로 이전한다.
- 매매일지의 현재 세션 프로세스 참조·실행 여부·시작·종료도 `AuxiliaryProcessManager`로 이전했다. 이전 세션 PID 정리와 백업 복원 대기 규칙은 유지한다.
- 순위 조회 기준 시각·0.25초 보정·2.5초 전 저우선순위 양보 시각 계산을 `application/ranking_schedule.py`로 분리하고 경계 테스트를 추가했다.
- 부분 순위 응답의 최대 2회 빠른 재시도, 다음 회차 대기, 설정창 사용 중 반영 보류 결정을 `application/ranking_schedule.py`로 분리했다.
- 순위 응답의 종목별 순위 변경과 전체 응답 동일 여부 계산을 `application/ranking_schedule.py`의 순수 함수로 분리했다.
- TOP20 테마 출현 빈도·거래대금 합계·테마 그룹 정렬키 계산을 `application/theme_ranking.py`로 분리했다.
- 상단 상위 테마 거래대금의 합산·상위 3개 선택·종목코드/종목명 제외 계산을 `application/theme_ranking.py`로 분리했다.
- KRX/NXT 시간대별 실시간 구독 종목, 세션 재구독 경계, 저우선순위 조회 휴지, TOP20 수집 가능 판단을 `application/market_session_schedule.py`로 분리했다.
- 장 마감 분봉·일봉 확정 대상, 완료 봉 시각, 최대 2회·5분 간격 재시도와 최종 미확정 판정을 `application/market_data_finalization.py`로 분리했다.
- 순위 조회 뒤 분봉·일봉·기본정보·NXT 조회가 필요한 종목과 장외 후속 단계 선택을 `application/secondary_data_schedule.py`로 분리했다.
- 매매일지 차트의 가격축·시간눈금·종가/체결 문구·표시 범위·평균가 레이블 배치 계산을 `application/journal_chart_layout.py`로 분리했다.
- 매매일지의 체결 확인, 기간 체결·비용 조회, 분봉·일봉·시장지수 보완 worker를 `presentation/journal_workers.py`로 분리했다.
- 개인 매매원칙 문단 로더를 `application/personal_trade_rules.py`로 통합하고 선택 셀 표시 delegate를 `presentation/journal_delegates.py`로 분리했다.
- 전략팩 등록·추출·규칙 검토·승인·이전 버전 복원 대화상자를 `presentation/strategy_pack_dialogs.py`로 분리했다.
- 매매일지 기본 설정과 기본 차트 설정 대화상자, 이동평균선 기본값을 `presentation/journal_settings_dialogs.py`로 분리했다.
- 차트 따로보기의 개별 차트 설정과 전체 6개 차트 일괄 설정 대화상자를 `presentation/detached_chart_settings.py`로 분리했다.
- 매매일지 `MinuteChart`와 체결 꼬리표 도형을 `presentation/journal_chart_widget.py`로 분리했다.
- 최대 6개 차트의 패널·배치·비교 종목/지수·이미지 저장을 담당하는 따로보기 창을 `presentation/detached_chart_window.py`로 분리했다.
- 뉴스 조회·DB 화면 준비·AI 분석 작업 스레드를 `presentation/news_workers.py`로 분리하고, 뉴스창과 같은 180초 조회 주기를 공유하도록 했다.
- 네이버·DART·AI 연결, 필터, 표시 열·색상, 바로가기 설정창을 `presentation/news_settings_dialog.py`로 분리했다. 기존 설정 키와 저장 검증은 유지한다.
- 뉴스 목록 행, 최종 판단, 관련 기사와 AI 상세 HTML 계산을 `presentation/news_view_model.py`의 순수 함수로 분리하고 표시 회귀 테스트를 추가했다.
- 핵심 회귀 실행 목록에서 누락된 쉼표 때문에 중앙 AI 서비스와 프로세스 제어 테스트 이름이 합쳐지던 문제를 수정했다.
- 뉴스의 신규/최근/수동 미분석 후보와 단건·묶음 다음 실행 선택을 `application/news_auto_analysis.py`로 분리하고 순서·제외·한도 회귀 테스트를 추가했다.
- 뉴스 worker의 `wait`·`deleteLater` 종료 순서와 AI 빈 작업·일일 한도·중앙 서버 키 허용 판단을 `presentation/news_execution.py`로 분리하고 회귀 테스트를 추가했다.
- 매매일지의 12개 테이블 생성과 레거시 비용 종목코드 복구를 `persistence/journal_schema.py`로 분리하고 전체 테이블·반복 초기화 회귀 테스트를 추가했다.
- 체결 시점 시장 스냅샷 저장·조회와 외국인/기관·프로그램매매·뉴스 장후 보완을 `persistence/journal_snapshot_repository.py`로 분리했다. 기존 `JournalRepository` 호출과 import 호환성은 유지한다.
- 개인 원칙·구조화 원칙·전략팩·강의 추출 초안·버전 저장을 `persistence/journal_strategy_settings_repository.py`로 분리하고 전략팩 기본/초안 테스트를 핵심 회귀 묶음에 포함했다.
- 자동·수동 매매유형, 체결, 수동 묶음, 복기, 실제 비용 저장을 `persistence/journal_trade_repository.py`로 분리했다. 기존 `JournalRepository` API는 유지한다.
- 매매일지 분봉·일봉·시장지수 봉, 백필 상태와 메인 DB 읽기 전용 가져오기를 `persistence/journal_bar_repository.py`로 분리했다. `journal_database.py`는 호환 facade와 저장소 조립만 담당한다.
- 중앙 SQLite/PostgreSQL이 공유하는 분봉·일봉 열 순서, 입력 행 변환, API 결과 사전 변환과 limit/offset 보정을 `central_server/database_codec.py`로 분리하고 계약 테스트를 추가했다.
- 중앙 SQLite/PostgreSQL의 6개 테이블·4개 인덱스 생성문을 `central_server/central_schema.py`로 분리하고, 동일 객체 구성과 의도적인 자료형 차이를 계약 테스트로 고정했다.
- 뉴스·테마·설정·매매일지 중앙 동기화가 공유하는 배치 크기 제한, 분할 업로드, 문서 envelope와 SQLite 테이블 확인을 `infrastructure/central_sync_utils.py`로 통합했다.
- 네 종류의 중앙 콘텐츠 동기화 테스트와 공통 유틸리티 테스트를 핵심 회귀 묶음에 포함했다.
- 메인 창에 내장돼 있던 독립 UI 구성요소 네 개를 `presentation/main_window_components.py`로 옮기고 기존 import 호환성을 유지했다.
- 회귀 실행기는 Python 후보의 실제 실행 여부를 검사한다. 실행 실패 시 접근 제한을 먼저 재확인하도록 안내하며, Python 삭제로 잘못 기록한 환경 진단을 정정했다.
- 메인 표의 거래대금·시가총액 단위 및 상위 테마 HTML 표시를 순수 포맷 모듈로 분리하고 경계값·HTML 이스케이프 테스트를 추가했다.
- TOP20 거래대금 수집 표본을 차트 조회 날짜와 1분/5분/60분 표시 모드에 관계없이 갱신해, 화면 모드 때문에 다음 분 저장이 건너뛰어질 수 있던 경로를 막았다.
- 사용자 전략팩 결과의 `warnings`와 `unverifiable`가 뒤바뀌어 저장·표시될 수 있던 위치 인수 오류를 이름 인수로 수정했다.
- TOP20 거래대금의 30초 코호트 예약·교체, 구간 기준값, 1분 마감과 종료 시 부분 기록 상태를 UI 비의존 수집기로 분리하고 경계 회귀 테스트를 추가했다.
- 매매일지 분석이 체결 당시 실시간 스냅샷과 장후 보완된 뉴스·외국인/기관 자료를 구분하도록 공통 출처 판정 규칙을 추가했다.
- 매매 회차별 스냅샷 범위·진입 자료 선택·판정 불가 항목 계산을 `application/trade_snapshot_context.py`로 분리했다. 기존 분석 문구와 1분 경계 규칙은 유지한다.
- 회차별 자동분석 본문과 진입 스냅샷·시장 상태 표시 문구 조립을 `presentation/trade_review_formatting.py`로 분리했다. Qt 위젯과 무관하게 기존 문구 계약을 테스트한다.
- 하루 종합·연결 데이터 상태·대표 뉴스 표시 문구를 매매일지 창에서 포맷 모듈로 옮기고, 기본 강의와 사용자 전략팩의 참고자료 범위 계산을 `application/strategy_review_context.py`로 분리했다.
- 매매 회차 주변의 체결 스냅샷 조회와 누락 뉴스 장후 연결을 `persistence/journal_snapshot_service.py`로 분리했다. 체결 전 48시간~직후 5분, 최대 10건 규칙과 DB 오류 시 비차단 동작을 테스트로 고정했다.
- 기본분석·승인된 사용자 전략팩 후보 평가, 표시 방식별 최종 선택, 회차별 수동 유형과 과거 단일 유형 이전 규칙을 `application/trade_strategy_coordinator.py`로 분리했다.
- 수동 확정/자동 판정별 신뢰도·근거·주의사항과 스냅샷 기반 판정 불가 보정을 조립하는 회차 분석 실행을 `application/trade_episode_analysis_service.py`로 분리했다.
- 매매유형 요약, 회차 편집 행, 연결 데이터 상태와 전체 분석 문서를 `presentation/trade_review_view_model.py`의 불변 화면 모델로 분리했다. 매매일지 창은 완성된 값을 Qt 위젯에 반영한다.
- TOP20 수집시간 회귀 테스트가 고정 날짜 경과로 실패하지 않도록 최근 평일을 기준으로 검증하게 했다. 제품 통계 로직은 변경하지 않았다.

### Fixed

- 테마 NAS 즉시 보존 실패 시 로컬 변경을 표시하는 디스크 대기 표식을 유지하고 2초·10초·30초·60초 간격으로 자동 재시도한다. 앱 재실행 시에는 NAS pull보다 대기 테마 업로드를 먼저 수행한다.
- 뉴스 설정의 화면별 숨김 Qt 위젯이 삭제된 뒤 저장 코드가 다시 접근해 설정 파일을 갱신하지 못하던 오류를 수정했다. 뉴스 화면은 뉴스 옵션만, 연결 화면은 API 항목만 저장하고 숨긴 값은 기존 설정을 보존한다.
- NAS 연결 설정과 뉴스 설정의 AI 공급자·모델·일일 한도·DART 사용 여부를 같은 NAS 운영 설정 API에 연결하고, 성공한 값을 로컬 암호화 뉴스 설정에도 반영한다.
- 키움 시장지수 실시간 응답의 장 종료 특수 체결시각 `888888`을 실제 시각으로 오인해 `market_state` 키가 `T88:88`로 저장되던 문제를 수정했다. 새 수신분은 서버 수신 서울 시각의 분으로 저장하고 원본 `trade_time=888888`은 payload에 보존한다.
- 중앙 DB v3 마이그레이션이 기존 `T88:88` 시장상태 스냅샷과 관측 메타데이터를 해당 행의 `saved_at` 서울 시각 분으로 옮긴다.
- 마켓스테이트 보정 후 전체 핵심 회귀 477개를 통과했다.
- `2026.09.12-market-state-time-v1`을 NAS에 배포해 `/health`, 인증 API, WebSocket과 기존 `T88:88` 자료 보정 완료를 실제 확인했다.
- 새 NAS 이미지 내부의 PostgreSQL 통합 검사에서 중앙 스키마 v3와 주요 저장소 읽기·쓰기·롤백·검증자료 정리를 모두 확인했다.
- 실제 NAS 서버 컨테이너 중단 시 이 PC의 키움 API로 `ka00198` 20종목이 자동 전환되고, 서버 재시작 뒤 같은 클라이언트가 NAS 경로로 자동 복귀하는 것을 확인했다.
- 확인된 안정성 결함, 책임 분리, 과추상화 감사, 전체 회귀와 NAS 실검증을 마치고 `reports/REFACTORING_CLOSEOUT_REPORT.md`에 기능 보존 중심 리팩터링 종료선을 기록했다.
- NAS 독립 TOP20이 시장 필드가 없는 `ka00198` 응답만으로 KOSPI/KOSDAQ을 추측하지 않고 공식 KRX 종목 카탈로그를 중앙 저장해 시장별 지수를 계산하도록 했다.
- TOP20 지수 저장 실패 자료를 `server-data`의 원자적 outbox에 남겨 컨테이너 재시작 뒤에도 다시 저장하도록 했다.
- 시작 전에 실행되던 로컬 분봉 30일·일봉 250개 보존 정리를 첫 화면과 최초 순위 처리 뒤로 옮겼다. 실제 DB 복제본의 bootstrap 진입→화면 표시는 0.7258초에서 0.0744초로 줄었고 보존 정책은 유지한다.
- 매매일지 요약의 유형·회차 수정·복기·분봉 백필 상태를 묶음 단위로 읽어 실제 315묶음 조회를 0.4356초에서 0.0048초로 줄였다.
- NAS PostgreSQL의 마이그레이션·주요 저장소 왕복·롤백을 컨테이너 안에서 검증하고 자료를 정리하는 검사기와, 다른 PC에서도 인증 API·WebSocket·저장 연속성을 확인할 운영 검사기를 추가했다.
- 이번 NAS TOP20 복구 수정의 서버·Compose·Dockerfile 빌드 식별자를 `2026.09.12-top20-resilience-v2`로 일치시켰다.
- NAS 실제 배포 조합에서 `create_app()` 내부의 중복 `Path` import가 TOP20 outbox 경로 초기화를 막던 시작 오류를 제거하고, 키움 설정과 독립 TOP20을 함께 켠 회귀 테스트를 추가했다.
- `2026.09.12-top20-resilience-v2` NAS 이미지를 새로 빌드해 시작하고 `/health` 빌드 ID, 인증 API, WebSocket ready/pong과 전체 핵심 회귀 475개를 확인했다.
- 뉴스 프로세스가 실시간 순위·분봉으로 계속 바뀌는 메인 DB 파일을 뉴스/테마 변경으로 오인해 매분 전체 중앙 콘텐츠를 다시 올리고 내려받던 피드백 루프를 차단했다. 뉴스/AI DB 변경만 뉴스 동기화를 시작하며 테마 변경은 기존 전용 dispatcher가 담당한다.
- 중앙 실시간 스냅샷·분봉·시장 상태와 NAS TOP20 지수 저장이 일시 실패해도 미저장 자료를 보존해 재시도하고 수집 작업을 계속 실행하도록 보강했다.
- 조회 분봉의 추정 거래대금이 같은 시각에 이미 저장된 0B 실제 거래대금을 덮어쓰지 않도록 관측 출처를 확인한다.
- 완료로 표시된 NAS 차트 캐시가 비어 있거나 요청일보다 오래됐으면 키움 보완 조회를 허용하고, 일봉 응답에서는 요청일 이후 자료를 제외한다.
- 과거 단일 수동 매매유형 이전과 자동 판정 저장을 한 트랜잭션으로 묶고, 중앙 동기화에서 삭제한 수동 묶음·회차 유형이 다음 동기화 때 되살아나지 않도록 삭제 상태를 보존한다.
- 메인 화면의 반복 QThread 작업이 끝난 뒤 참조와 Qt 객체를 해제하고, 매매일지 PID 파일은 프로세스 시작 식별자가 일치할 때만 종료 대상으로 사용한다.
- 분봉 DB 저장 실패 시 메모리 대기 자료를 복원하고, 일봉/분봉 보완 저장에 실패한 종목은 완료 처리하지 않아 다시 시도할 수 있게 했다.
- 열 설정 복원 중 발생한 Qt 크기 변경 이벤트가 같은 설정을 DB에 반복 저장하지 않도록 차단했다.
- 핵심 회귀를 실제 앱 프로세스 경계에 맞춘 두 인터프리터로 실행해, 수백 개 Qt 테스트 객체가 한 프로세스 종료 시점에 누적돼 assertion 성공 후 종료 코드 1을 내는 현상을 격리했다.
- 이번 NAS 안정성 수정의 서버·Compose·Dockerfile 빌드 식별자를 `2026.09.11-stability-v1`로 일치시켰다.
- 메인 창을 생성 직후 닫을 때 예약된 API 설정창이 파괴된 창을 부모로 사용하지 않도록 종료 상태를 확인한다. GUI 테스트도 `close()` 뒤 Qt 지연 삭제를 처리해 15개 메인 화면 회귀가 종료 코드 0으로 끝나도록 고정했다.
- 매매일지 시장지수 보완이 저장 성공 전에 해당 날짜를 완료 처리해 같은 세션에서 재시도하지 못하고, 실행 중 다른 날짜 요청을 버리던 상태 흐름을 수정했다. 성공한 날짜만 완료 처리하며 실행 중에는 최신 대기 날짜 한 건을 종료 후 이어서 조회한다.
- 차트 따로보기 테스트가 닫힌 Qt 객체를 인터프리터 종료 때까지 남겨 전체 회귀 본문 성공 후 간헐적인 Windows 네이티브 종료 오류를 내던 문제를 테스트 teardown 정리로 해결했다.

- NXT 장에서 NXT 미지원 TOP20 종목까지 `_NX`로 구독해 전체 구독 승인을 방해할 수 있던 중앙 WebSocket 구독 범위를 보정했다.
- 테마 OCR 결과 미리보기 적용 시 `replace` 가져오기 누락으로 프로그램 오류가 뜨던 문제를 수정했다.
- 차트 따로보기에서 숨겨진 패널의 종목이 분봉 보완을 먼저 차지하거나, 이전 빈 응답 상태 때문에 현재 비교 종목을 다시 조회하지 못하던 문제를 수정했다.

## [1.1.20]

- 상세 내역: `docs/RELEASE_NOTES_v1.1.20.md`
- 이전 버전: `README.md`의 변경 내역 참조
