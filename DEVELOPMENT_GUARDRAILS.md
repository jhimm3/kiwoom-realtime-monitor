# 개발 불변 규칙

AI와 사람이 이 저장소를 수정할 때 아래 규칙을 기본 계약으로 적용한다.

## 데이터와 API

1. NAS 모드의 모든 Kiwoom REST 조회는 `CentralRestBroker`를 통과한다. 새 직접 HTTP 호출로 중앙 큐를 우회하지 않는다.
2. `ka00198` 실시간 종목조회순위가 대기 중인 분봉·일봉·역사 백필보다 항상 높은 우선순위를 갖게 한다.
20:05 이후 장후 보완은 30초 순위 schedule loop에서 직접 기다리지 않고 별도 owned task로 실행한다. 장후 분봉·일봉·수급 보완이 오래 걸려도 다음 `ka00198` 회차를 막지 않는다.
30초 경계의 첫 `ka00198`이 직전 스냅샷을 반환할 수 있으므로 경계 직후 5초까지 저우선순위 TR 시작을 보류한다. 0.25/0.5/0.75초 최신 회차 재조회가 장후 수급·봉 응답 저장 뒤에서 기다리지 않게 한다.
저우선순위 TR을 경계 밖으로 미룰 때는 job을 먼저 priority queue에 되돌린 뒤 짧게 yield한다. 단일 broker worker가 예약시간 전체를 sleep해 새 `ka00198`까지 막지 않는다.
3. 직접 연결도 `KiwoomRestClient`의 공통 호출 간격·재시도를 우회하지 않는다.
3-0. NAS 장애전환의 직접 REST 조회와 직접 WebSocket은 같은 로컬 `KiwoomRestClient`의 토큰·요청 잠금을 공유한다. 별도 클라이언트를 만들지 않는다. WebSocket `REG` 승인을 받은 뒤에만 실시간 구독 정상 상태를 공개한다. 로컬 실시간 수신을 시작한 뒤에는 NAS 복구 확인을 위해 주기적으로 로컬 구독을 끊지 않는다. NAS 서버 접속이나 구독 요청 접수만으로 복귀하지 않고, 현재 앱 요청 종목 전체의 중앙 상류 구독 승인이 확인된 뒤에만 로컬 WebSocket을 종료한다. 중앙 worker 내부에서 생성한 로컬 QThread의 신호는 이벤트 루프 없는 중앙 작업 스레드에 queue하지 않고 즉시 중계해 GUI 수신 객체의 thread로 전달한다.
상단 API 표시는 실시간 경로뿐 아니라 순위 REST의 실제 장애전환도 반영한다. 순위가 이 PC 키움 API에서 성공한 동안은 `로컬 전환`을 우선 표시하고, NAS 조회가 실제 성공한 다음 회차에만 `NAS`로 복귀한다.
3-1. 실전 조회(초당 5회)와 모의투자 조회(초당 1회)는 별도 한도다. 두 환경의 App Key/App Secret, `KiwoomRestClient`, 요청 잠금·최근 호출 시각, broker queue, WebSocket 연결을 공유하지 않는다.
4. 기존 `/api/v1` 경로, 필드 이름, 단위, 인증 방식의 의미를 임의 변경하지 않는다. 호환 불가 변경은 새 버전으로 병행한다.
5. 새 분석값은 먼저 `monitor/news/journal/central` DB와 기존 실시간 스냅샷으로 계산 가능한지 확인한다. 가능하면 새 Kiwoom TR을 추가하지 않는다.
6. 체결 당시 관측값과 장후·사후 보완값을 같은 근거로 취급하지 않는다. 스냅샷 분석은 `domain/snapshot_provenance.py`의 출처 판정을 사용한다.
7. 중앙 저장 실패는 이미 성공한 화면 조회를 실패로 바꾸지 않는다. 로컬 캐시·사용자 편집은 보존한다.
8. 시장/금액 단위를 명시한다. 로컬 거래대금은 주로 억원, 중앙 봉은 백만원이며 경계에서만 변환한다.
8-1. 뉴스 DB 파일의 mtime은 동기화 실행 신호일 뿐 전체 업로드 근거가 아니다. 중앙 콘텐츠 push는 `(collection, owner, key)` identity와 정규 document content hash의 영속 delta만 보내며, unchanged는 0건·한 문서 변경은 1건이어야 한다. 실패 delta는 재시작 뒤 남기고 pull 반영은 echo push하지 않는다. 빈 pull은 cursor·manifest를 다시 쓰지 않고 동일 hash 수신도 manifest를 다시 쓰지 않는다. 중앙의 동일 문서 upsert는 `updated_at`을 유지한다. 뉴스 동기화는 별도 뉴스 프로세스의 background 경계에 두어 최우선 실시간 순위 요청을 지연시키지 않으며, `test_central_content_sync.py`와 `test_central_server_database.py`가 이 계약을 보호한다.
8-2. 장중 1초 분봉·현재가·당일고가 저장은 Qt 메인 스레드에서 SQLite를 직접 쓰지 않는다. 메인 스레드는 최신 대기분을 전용 단일 writer queue에 넘기고, writer가 같은 DB의 쓰기를 순서대로 수행한다. 종료 시 queue를 비우며 저장 실패분은 그동안 도착한 최신 값을 덮지 않게 다시 대기열에 합친다. 이 경계 때문에 실시간 순위 조회·표 갱신 주기와 우선순위를 낮추지 않는다. `test_market_cache_writer.py`와 `test_main_window.py`가 drain·실패 복구를 보호한다.
8-3. 키움 WebSocket의 `REAL` 메시지에 `data`가 없거나 null이면 빈 event batch로 처리한다. 빈 프레임을 상류 연결 실패나 NAS 장애로 승격하지 않으며, 실제 오류가 없는 상태에서 로컬 페일오버를 시작하지 않는다. 체결·계좌·시장지수·프로그램·VI와 시장 관측 parser가 같은 null 방어를 유지한다.
8-4. NAS 모드의 모든 순위 기준·당일 분봉·확정 일봉·기본정보·NXT 가능 여부·신고가·역사적 신고가·후보 수급·시장지수 차트는 중앙 DB API를 먼저 읽는다. 새 TOP20 편입 종목과 실제 계좌 매수 체결 종목의 기본정보·NXT 가능 여부·등장 전 당일 분봉·외국인/기관 수급·역사적 신고가는 NAS 자율 수집기가 중앙 문서와 보완 표식을 확인한 뒤 실제 공백만 저우선순위 TR로 채운다. 실제 체결 종목 목록은 시장자료 수집 범위에만 합치며 TOP20 순위·지수 구성과 계좌별 매매일지 신원에는 섞지 않는다. 프로그램매매 `0W`는 중앙 실시간 원본을 보존하고 장후 누락분을 NAS가 확정한다. 장후 전체일 봉과 시장지수 확정은 별도 coverage로 관리한다. 대량 분봉의 로컬 SQLite 저장은 `MarketCacheWriter`에서 수행하며 GUI 스레드에서 직접 쓰지 않는다.
8-5. 정상 NAS 연결 상태에서 데스크톱 앱의 자동 시장자료 경로는 `/api/v1/kiwoom/query`를 호출하지 않는다. 당일 및 최근 2거래일 분봉도 중앙 DB GET으로 읽고 빈 저장 결과를 TR 보완 신호로 해석하지 않는다. 계좌별 과거 체결·비용 `kt00007/kt00015`, 사용자가 선택한 직접 모드, 실제 NAS 단절 뒤 허용된 로컬 장애전환은 이 시장자료 금지 범위와 분리한다.
8-5-0. NAS 연결의 메인 순위표는 순위 표시 직후 중앙 저장 분봉을 비동기로 먼저 읽어 1분·5분·60분·당일 거래대금을 복원한다. 중앙의 진행 중 현재 분봉과 앱 접속 뒤 0B가 겹치면 두 값을 합산하지 않고 더 진행된 한 벌을 기준으로 이어서 누적한다. PC 직접 연결의 진행 중 REST 분봉은 시점이 불명확하므로 기존 실시간 값을 유지한다.
NAS 재시작·단절 뒤 로컬 실시간 대체 수신에서 중앙 실시간으로 복구될 때도 현재 TOP20의 중앙 저장 분봉을 강제로 다시 읽어, 앱을 재시작하지 않아도 복구 구간의 1분·5분·60분·당일 값이 화면에 합쳐지게 한다.
8-5-1. NAS의 24시간 30초 `ka00198` 조회·membership 저장 경로는 종목 카탈로그 네트워크 갱신, NXT 적격성, 계좌 편입 종목 로드, 기본정보·분봉·신고가·수급 보완을 기다리지 않는다. NAS 재시작 직후에는 다음 30초 경계를 기다리지 않고 현재 회차를 즉시 수집한다. 수집·재조회 중 다음 30초 경계를 지났으면 정확한 경계 초를 놓쳤더라도 완료 직후 새 회차를 즉시 따라잡는다. 응답 `dt/tm`이 목표 회차보다 오래됐거나 전체 행이 20개보다 적거나 20행 안에 종목코드·종목명이 빈 자리가 있으면 0.25초 2회, 0.5초 2회, 이후 0.75초 간격으로 제한 재조회하며 미완성 회차는 저장하지 않는다. 앱도 NAS 저장 순위가 이전 회차이거나 20행 미만이거나 빈 자리가 있으면 같은 간격으로 중앙 DB만 다시 확인하며 정상 NAS 연결 중 키움 TR로 우회하지 않는다. 이 이중 재확인은 NAS가 2~3초 뒤 완성본을 저장하면 앱도 같은 회차 안에서 즉시 받아오기 위한 것이다. 제한 안에 최신 완성 회차를 받지 못하면 직전 스냅샷을 새 결과로 적용하지 않고 이미 표시된 정상 표를 유지하며, 다음 회차까지 `API: 재조회` 상태로 남기지 않는다. TOP20 실시간 거래대금 수집·구독은 해당 시장 관측시간에만 유지한다. TOP20 KRX 구독은 즉시 갱신하고 나머지는 중복되지 않는 background 작업에서 최신 순위로 따라잡는다. 부가 수집 실패나 지연은 순위 슬롯을 멈추지 않아야 하며 `test_slow_catalog_and_nxt_enrichment_do_not_block_next_ranking`으로 보호한다.
8-5-2. 키움 실전 토큰은 여러 WebSocket 로그인을 동시에 유지하지 못하므로 중앙 실시간은 단일 연결을 쓴다. 모든 TOP20·당일/익일 조건 코호트는 최소 한 개의 `0B` 체결 등록을 보장하고, NXT 가능 일반 코호트는 SOR 통합 코드를 쓴다. `0B`와 `0w`는 서로 독립된 타입별 200개 한도로 계산한다. `0B`의 남는 상세 venue 자리는 TOP20, 실제 보유·매수 편입 종목, 나머지 앱 요청 순으로 배정하고 `0w`도 같은 우선순위에서 최대 200종목을 등록한다. 각 그룹은 최대 100개 item이며 `REG`·`REMOVE`는 0.25초 이상의 공통 간격을 둔다. 전체 목록은 `refresh=0`으로 교체하고 꼬리 그룹은 `REMOVE`한다. 모든 `REG` 승인 뒤에만 상류 준비 완료로 공개한다.
8-5-3. `_AL` 0B·0w는 KRX가 아닌 SOR 통합시세 원본으로 저장한다. 화면용 통합 분봉은 같은 분의 SOR 한 벌을 우선하고 없을 때만 KRX+NXT 한 벌을 사용하며 세 흐름을 함께 더하지 않는다. 실제 승인 REG item이 SOR↔상세로 전환되면 재활성 source의 첫 누적값을 기준점으로 사용하고 이전 source의 늦은 틱을 집계하지 않는다. strict KRX 연구는 SOR 원본을 허용하지 않는다. 분봉 보완 때 SOR 실시간 거래대금과 KRX/NXT 조회 추정값의 차이·차이율을 별도 기록한다.
8-5-4. 상한가 표시는 `ka10001.upl_pric` 단독 캐시를 현재 기준으로 단정하지 않는다. 추적 종목의 `0g` 상한가·하한가·기준가를 한 묶음으로 중앙 저장·전달하고, 현재가가 제한가격에 닿아도 0B 등락률이 가격제한폭과 모순되면 서로 다른 기준 구간으로 보고 화면 강조와 새 상한가 사실 생성을 막는다.
8-5-5. NAS 연결 메인 표의 시가총액은 현재 실행에서 받은 `0B` FID 311을 최우선으로 쓴다. 앱 재시작 뒤 아직 새 체결이 없는 종목은 중앙 `central_realtime_latest`의 마지막 0B에서 시가총액과 관측시각만 복원한다. 이 경로로 과거 현재가·등락률을 되살리지 않는다. PC 직접 연결도 마지막 0B 시가총액을 기존 로컬 `stocks.market_cap`에 백그라운드 저장해 재시작 뒤 복원하되 `fundamentals_updated_at`은 갱신하지 않아 다음 거래일 `ka10001` 확인을 막지 않는다. 저장된 0B 시가총액도 없는 신규 종목만 당일 `ka10001` 기본정보를 사용한다. 복원 요청 중 새 0B가 도착하면 새 값이 우선한다.
8-6. 뉴스 언론사 식별은 원문 URL의 정규화된 `publisher_domain`과 매핑 가능한 `publisher_name`을 함께 사용한다. 화면 제공처 제외는 목록 숨김이고 NAS 처리 제외는 별도 운영 정책이다. NAS 처리 제외 기사도 제목·링크와 source observation은 보존하되 새 BODY/RULE/자동 AI 작업을 만들지 않는다. 기존 완료·대기 작업을 소급 삭제하지 않으며, 파생 언론사 필드 보정만으로 새 기사 revision을 만들지 않는다.
8-7. 중앙 종목 뉴스의 관련성 판정은 저장된 `fulltext` 원문이 있으면 이를 함께 사용한다. 원문 evidence는 그대로 보존하고, 화면·규칙·AI 입력에서는 저작권 문구 뒤의 포털 추천·랭킹·댓글 영역을 제거한 정제본을 사용한다. 정제 뒤 본문이 비면 검색 요약으로 보완한다. 장중 가격·지수·거래량을 중계하는 제목은 원문 뒤쪽에 과거 계약이나 일반 기대가 있어도 새 기업 사건으로 승격하지 않는다. 제목 또는 원문 도입부에서 당일 공시·계약·실적처럼 확인 가능한 새 사건을 제시할 때만 관련 뉴스로 보존한다. 공급계약 금액은 공급·수주 표현과 같은 문장 안에서만 추출하며 주가와 시가총액을 계약금액으로 사용하지 않는다.
8-8. 뉴스 상세의 비AI 요약은 정제된 `fulltext`에서 최대 3개 원문 문장만 선택한다. 새 문장을 생성하거나 원문을 대체하지 않으며, 포털 푸터·저작권 꼬리와 900자를 넘는 비정상 단일 문장은 선택하지 않는다. 안전한 핵심 문장이 없으면 요약을 생략하고 저장 본문만 표시한다. `summary_only` 자료에는 추출 요약을 표시하지 않는다.
8-9. 중앙 query-set 신규 수집은 검색 설명이 없는 제목뿐인 결과를 저장하지 않는다. 정확한 KRX 종목명, 증권·시장 문맥, 기업의 재무·공시·계약 사건 문맥이 모두 없는 명백한 검색 잡음도 기사 이력 생성 전에 제외한다. 본문을 읽기 전 단계이므로 이 필터는 필요한 기사를 잃지 않도록 넓게 유지하고, 수집 실행 문서에는 원건수·저장건수·제목뿐인 건수·무관 건수를 기록한다. 기존 저장 이력은 참조 무결성을 위해 이 정책만으로 소급 삭제하지 않는다.
8-10. 비AI 호재·악재 판정은 제목·검색 요약을 우선한다. 두 영역에 방향 근거가 없을 때만 정제 원문 도입부 중 대상 회사가 직접 등장한 문장에서 확정성이 높은 실적·계약·자본 사건을 보조 근거로 쓴다. 업종 배경, 다른 회사 사건, 언론사 법적 고지로 기존 방향을 뒤집지 않는다. 수급·공매도·거래대금 순위와 다종목 시장 요약은 관련 뉴스에서 제외하되, 제목에 유상증자·실적 발표처럼 원인이 되는 새 기업 사건이 명시되면 보존한다.
8-11. 뉴스창에서 현재 선택한 한 종목은 앱과 NAS가 60초 freshness를 공유해 1분 이상 지난 경우 NAS 공급자 조회를 다시 수행한다. NAS에 누적된 전체 `news_watchlist`의 자율 갱신과 공통 query-set 수집 주기는 각각의 운영 설정을 유지하며, 선택 종목 주기를 전체 등록 종목에 확대하지 않는다.
8-12. 종목별 과거 뉴스 목록은 최신 저장 본문을 읽어 현재 비AI 규칙으로 조회 시 파생 판정을 다시 계산한다. 이 화면용 재분류는 원문·기사 revision을 덮어쓰거나 BODY·AI 작업을 다시 예약하지 않는다. 뉴스 상세의 `AI 원문 분석` 표시는 실제 저장 AI 결과가 있을 때만 사용하고, 공급계약 구조화 근거는 해당 event가 있는 기사에만 표시한다.
8-13. 앱의 명시적 종목 뉴스 조회는 NAS 뉴스 작업기의 현재 우선 종목을 갱신한다. 실행 중인 작업을 취소하지 않고 다음 claim부터 해당 종목 BODY·후속 작업을 먼저 고른다. NAS 정기 watchlist 갱신은 사용자 우선 종목을 덮지 않는다. 비선택 BODY는 watchlist 최신, query-set 최신 순으로 처리하고 유입 사이에는 기존 누적분을 계속 소진한다.
8-13-1. 개인 NAS 연결의 데스크톱 앱은 중앙 `news_article/news_ai/news_ai_shared` 전체 카탈로그를 시작 시 또는 60초 동기화에서 로컬로 복제하지 않는다. 선택 종목 목록은 `/api/v1/news/search`, 선택 기사 근거는 `/api/v1/news/history/*`로 필요한 범위만 읽는다. 테마와 계좌별 매매일지 뉴스 연결은 기존 증분 콘텐츠 동기화를 유지하며, 이미 로컬에 저장된 뉴스는 삭제하지 않는다.
8-14. 매매일지의 장후 분봉 보완은 분봉 행 존재 여부를 전체일 완료 근거로 사용하지 않는다. 중앙 `minute-bars.coverage.complete`가 참일 때만 `after_close_confirmed`와 `확정`을 기록한다. 시행일 이후의 기존 `확정` 기록에 완료 표식이 없고 마지막 봉이 19:59보다 이르면 `일부`로 내려 자동 재수집한다. 이 판정은 무체결 분의 존재를 요구하지 않으며 NAS의 별도 coverage 문서를 신뢰한다. `kind=minute`만 있는 구형 coverage는 완료로 재사용하지 않고 전체 연속조회를 다시 실행해 `window_closed=true`, `session_finalized=true`로 승격한다.
8-15. 자동 모의운용 후보는 일반 콘텐츠 동기화로 게시하지 않는다. 전용 인증 API가 256 KiB 상한,
등록 family·정규 설정, 기존 `final_candidate/v1` hash와 별도 package hash, final batch/run/result 계보,
사전 동결 eligibility policy/receipt, 현재 scientific implementation hash와 mock binding을 확인한다.
`COMPLETED`만으로 합격시키지 않고 미정 기준은 BLOCKED다. 게시 저장은 runtime lease, execution intent,
Kiwoom TR·주문 transport를 호출하지 않는다.
8-16. NAS 저장량 진단은 뉴스·시장 누적자료·연구·계좌·기타별 추정 용량과 건수를 읽기 전용으로 제공한다. 자동 삭제 기본값은 무제한/OFF다. 분봉·일봉·순위·뉴스 원문처럼 과거 재현에 쓰는 자료는 매매일지·주문/체결·연구 export·최종평가·후보·모의운용의 보호 참조, dry-run, 삭제 원장이 구현되기 전에는 기간이나 용량만으로 삭제하지 않는다.
실행 중 서비스와 같은 PostgreSQL을 검사하는 통합 검사는 Shadow 후보처럼 앱이 폴링하는 사용자 가시 자료를 commit한 뒤 삭제하지 않는다. 해당 검증은 단일 트랜잭션 안에서 읽고 rollback해 앱 알림·후보 선택·자동운용 입력으로 노출되지 않게 한다.
통합 검사의 임시 인증 vault는 운영 PostgreSQL의 `credential_vault_state` 복구 fence를 공유하지 않는다. 임시 키 파일과 그 복구 표식은 검사 프로세스 안에 격리하고, 운영 `master.key`를 복사하거나 운영 fence를 삭제해서 검사를 통과시키지 않는다.

## 변경 범위

9. 전체 파일 재작성보다 최소 패치를 우선한다. 관련 없는 사용자 변경을 정리하거나 되돌리지 않는다.
10. 신규 기능은 기존 대형 UI 클래스 안에 끼워 넣기 전에 독립 application/infrastructure 모듈로 만들고 UI는 호출만 하게 한다.
11. 한 요청에서 리팩터링과 기능 변경을 섞지 않는다. 구조 변경은 동작 보존 테스트를 먼저 추가한다.
12. `MODULE_MAP.md`에서 수정 범위를 고르고, 관련 파일·직접 호출자·대응 테스트만 먼저 읽는다.
13. 메인·뉴스·매매일지 프로세스 경계를 유지한다. 뉴스/OCR/AI/차트 계산이 메인 실시간 이벤트 루프를 막지 않게 한다.
13-1. OCR 모델 경로는 실행 소스 위치가 아니라 `AppPaths.for_current_user().data_dir/ocr_models`를 사용한다. 모바일 detector와 한국어 recognizer가 있으면 로컬 파일을 명시하고 기본 server detector로 바꾸지 않는다. Paddle 모델 준비·predict는 QThread interruption을 확인하지 못할 수 있으므로 앱 종료가 OCR 완료를 무기한 기다리지 않게 제한시간 뒤 OCR 보조 worker만 정리한다.

### 추상화 비용 기준

- 단순 전달만 하는 Manager/Service/Handler/Controller를 새로 만들지 않는다.
- 새 계층은 독립된 정책, 상태, 객체 수명, 저장/외부 시스템 경계 중 하나 이상을 실제로 소유해야 한다.
- 파일 분리 전후에 기능을 이해하는 데 필요한 파일 수와 호출 깊이를 비교한다. 영향 범위가 줄지 않고 호출 단계만 늘어나면 기존 위치를 유지한다.
- 이미 만든 계층이 얇아 보여도 작업 도중 성급히 재통합하지 않는다. `AUDIT_REPORT.md`에 후보와 근거를 기록하고 현재 리팩터링 종료 후 한 번에 감사한다.
- 순수 계산을 기존 응집도 높은 모듈에 추가할 수 있다면 이름만 다른 새 Service/Manager보다 기존 모듈 확장을 우선한다.

### 증거 기반 버그 수정

모든 오류·예외·장애·데이터 불일치·간헐적 지연·성능 저하는 다음 순서를 지킨다.

1. 사용자가 실제 실행한 소스·빌드·프로세스·설정·데이터 위치를 먼저 확인한다. 다른 워크트리, 오래된 프로세스, 배포 전 이미지 결과를 현재 코드의 동작으로 해석하지 않는다.
2. 실제 코드 흐름, 호출자, 저장소, 스레드·프로세스 및 외부 시스템 경계를 끝까지 추적한다.
3. 오류를 같은 조건에서 재현하고 단계별 시간·입출력·상태 전이를 기록한다. 재현이 간헐적이면 원인을 판별할 수 있는 계측부터 추가한다.
4. 로그, 실행 결과, DB 상태·query plan·wait event·blocking owner, 외부 응답과 테스트 중 해당 문제에 맞는 직접 증거로 원인 가설을 검증한다.
5. 후보가 여러 개면 각 후보를 확인하거나 반증한다. 시간상 함께 발생했다는 사실만으로 인과관계를 단정하지 않는다.
6. 원인이 확인되기 전에는 수정부터 시도하거나 큰 구조 변경을 하지 않는다. 완화책이나 우회로 증상이 줄어도 그것을 근본 원인 해결로 보고하지 않는다.
7. 확인된 원인에만 최소 수정하고, 원래 재현 조건과 같은 지표로 변경 전후를 비교한다.
8. 해당 문제와 인접 기능의 회귀를 함께 확인하고 운영 문제라면 실제 실행 빌드와 운영 로그에서도 결과를 확인한다.
9. 확인된 사실, 반증된 후보, 아직 검증하지 못한 가설, 수정 후 검증 범위를 구분해 보고한다. 원인을 확정하지 못했으면 해결 완료로 표시하지 않고 필요한 추가 증거를 남긴다.

핵심 흐름은 `추측 → 수정`이 아니라 `가설 → 검증 → 원인 확인 → 최소 수정 → 재검증`이다.

## DB와 사용자 데이터

14. 스키마 변경은 명시적 마이그레이션 버전, 이전 DB fixture, 전후 조회 테스트와 함께 관리한다.
15. 테이블/필드 삭제는 데이터 이전과 검증 전에는 하지 않는다. `themes/stock_themes` 같은 호환 구조를 이름만 보고 제거하지 않는다.
16. Raw 데이터, 파생 데이터, 사용자 원본을 구분한다. 파생 재계산이 사용자 복기·수동 유형·전략팩을 덮어쓰면 안 된다.
17. 로컬 SQLite 파일을 네트워크 공유로 여러 프로세스가 직접 열게 하지 않는다. NAS 공유는 HTTP/WebSocket API를 사용한다.
18. 비밀키와 접속 토큰을 로그·DB 문서·일반 백업·고정 테스트 fixture에 넣지 않는다. NAS 자격증명은 `server-secrets` 전용 암호화 저장 경계에만 보존하며, master.key와 암호문을 포함하는 별도 접근 제한 복구 백업만 예외다. 이 디렉터리는 Git·Docker build context·코드 배포·일반 DB 백업에서 제외하고, 배포 시 `.env`, `postgres-data`, `server-data`와 함께 보존한다. 테스트 비밀은 실행 중 임시 생성하고 종료 시 제거한다. 계좌 신원 HMAC 키를 암호화 master로 재사용하지 않고 OAuth 토큰은 vault에도 저장하지 않는다.
19. 과거 시장 재현에 쓰는 시점별 Raw 스냅샷은 append-only를 기본으로 한다. 동일 종류·대상·관측시각의 정정 외에는 과거 행을 최신 상태로 덮어쓰지 않는다.
20. 순위·테마·봉을 결합할 때 각각의 관측시각과 수집상태를 확인한다. 최신 테마 소속을 과거 시점의 사실로 단정하지 않는다.
20-1. 현재 테마의 단일 로컬 원본은 `theme_profiles/profile_themes/profile_stock_themes`다. 일반 설정 백업, Google Drive 테마 백업, NAS 테마 스냅샷 중 하나만 구형 `themes/stock_themes`를 기준으로 만들지 않는다. 구형 표와 version 1~3 파일은 읽기 호환 경계로만 보존한다.
20-2. 설정 키나 테마·뉴스 AI 테이블 계약을 추가·변경·삭제할 때는 파일 설정 백업, Google Drive, NAS 설정/콘텐츠 동기화를 각각 확인한다. 임의 테이블을 자동 탐색해 외부로 보내지 않으므로 새 테이블은 `settings_backup.py`/`theme_backup.py`/`news_ai_backup.py`, `google_drive_sync.py`, `central_settings_sync.py`/`central_content_sync.py` 중 해당 경계와 회귀 테스트를 명시적으로 갱신한다. 삭제 전파는 스냅샷 교체 또는 tombstone 계약이 있는지 별도로 검증한다.
20-3. 일반 설정 백업과 Google 설정 파일은 `central_settings_sync.is_shared_setting`의 공통설정 의미를 따른다. 테마 DB는 별도 전체 프로필 백업으로 유지하고, 창 위치·크기·열 너비·로컬 경로·동기화 실행 상태를 다른 PC에 덮어쓰지 않는다.
주기적 중앙 설정 동기화는 `(owner,key)`의 원격 document와 로컬 document가 실제로 다를 때만 `app_settings`·`app_column_settings`를 POST한다. 변경이 없는 매분 동기화는 GET 비교만 수행하고 PostgreSQL 쓰기를 만들지 않는다.
20-4. Google Drive의 설정/테마/둘 다 선택은 주 백업 범위만 고르며 뉴스 AI 분석 결과는 세 선택 모두 별도 파일로 보존한다. NAS 공통 설정에는 메인 표의 표시 여부·순서를 포함하되 모니터별 열 너비는 포함하지 않고, 매매일지에 연결한 뉴스 관계를 기사·AI 결과와 함께 보존한다.
20-5. 테마 NAS 교체 실패는 로컬 편집을 성공으로 유지하되 디스크 대기 표식을 지우지 않는다. 재시작 시 오래된 NAS 테마를 pull하기 전에 대기 로컬 스냅샷을 먼저 전송한다.
20-6. NAS 모드의 AI 공급자·모델·일일 한도와 DART 사용 여부는 NAS 운영 설정이 단일 원본이다. NAS 연결 설정과 뉴스 설정은 `central_operational_settings.py`를 함께 사용하고, 한 화면만 별도 값으로 저장하지 않는다.
20-7. 중앙 시장 조회 큐는 30초 실시간 순위 경계를 최우선으로 보호한다. 경계 직전에는 새 저우선순위 TR을 시작하지 않으며, TOP20·계좌 추적 종목의 기본정보와 신고가용 일봉은 NAS가 저장 여부와 KST 거래일 최신성을 확인한 뒤 한 번 준비한다. 앱이 같은 자동 보완 TR을 다시 만들지 않는다.

새 로컬 SQLite 저장소는 특별한 읽기 전용 URI·초단기 timeout 요구가 없다면 `persistence/sqlite_connections.py`의 읽기 연결 또는 쓰기 트랜잭션 경계를 사용한다. 예외가 발생해도 연결 종료와 쓰기 롤백이 보장돼야 하며, 여러 저장소를 한 번에 일괄 교체하지 않고 관련 회귀가 있는 기능부터 순차 적용한다.

## 분석과 주문

21. 분석·분류·복기 코드는 실제 주문 실행과 분리한다. 분석 결과가 자동 주문을 발생시키지 않는다.
21-1. 수동 모의주문은 명시적 기능 플래그와 인증 요청을 모두 요구한다. run별 멱등키를 저장하고 응답 유실·중복 요청을 자동 재전송하지 않으며, 후보·전략 생성기가 수동 주문 API를 호출하지 않는다.
21-2. 자동 모의주문은 계좌별 영속 RUNNING control revision과 승인 gate v2를 O1 intent 저장 트랜잭션에서 다시 확인한다. STOPPED는 ENTER/EXIT 모두 막고 새 broker recovery만으로 해제하지 않는다. 명시 재개와 중지 이후 대사가 필요하다. ENTER와 reduce-only EXIT를 구분하고 terminal 주문은 미체결로 세지 않되 체결·잔고 대사가 진행 중이면 모두 차단한다. 운영 복구·Decision은 현재 저장된 `mock_automation_risk_snapshot/v1`의 snapshot ID와 reconciliation revision을 함께 확인한다. 비용 누락·체결 상세 공백·계좌 잔고 불일치는 0으로 대체하지 않는다. 지속 runner와 UI가 연결되기 전에는 자동주문을 켜지 않는다.
22. 새로운 전략 규칙은 입력 데이터, 계산식, 결측 처리, 신뢰도 근거를 단위 테스트로 고정한다.
23. “없음/0/미확인”을 같은 값으로 취급하지 않는다. 데이터 미수신은 0 매수·0 거래와 구분한다.

## 완료 조건

24. 수정 후 최소 관련 테스트와 핵심 회귀 묶음을 실행한다. 실행 불가능하면 이유와 미검증 범위를 숨기지 않는다.
25. API 변경은 `API_CONTRACT.md`, DB 변경은 `DB_SCHEMA.md`, 모듈 이동은 `MODULE_MAP.md`, 사용자 영향은 `CHANGELOG.md`를 함께 갱신한다.
26. 기능개발서의 미래 계획을 현재 구현으로 단정하지 않는다. 현재 사실은 `ARCHITECTURE_CURRENT.md`, 불일치는 `AUDIT_REPORT.md`에 기록한다.
27. NAS 서버 소스·의존성·Dockerfile·Compose가 바뀌면 **같은 변경에서 반드시 NAS 빌드 식별자도 올린다.** 아래 세 곳은 한 묶음이며 값이 정확히 같아야 한다.
    - `src/kiwoom_monitor/central_server/app.py`의 `SERVER_BUILD`
    - `deploy/synology/docker-compose.yml`의 `server.image`
    - `deploy/synology/server.Dockerfile`의 빌드 식별자 검증 문자열
28. NAS 빌드 식별자는 `YYYY.MM.DD-변경명-vN` 형식을 사용하고, 서버 동작이 달라지는 매 변경마다 새 값으로 바꾼다. 같은 이미지 태그로 서버 코드를 다시 배포하지 않는다.
29. NAS에 저장소 전체를 동기화한 뒤 Container Manager에서 서버 이미지를 다시 빌드한다. 컨테이너 재시작만으로 새 소스가 반영됐다고 판단하지 않으며 `/health`의 `server_build`가 이번 변경값과 같은지 확인한다. 다르면 작업을 완료로 보고하지 않는다. `.env`, `postgres-data`, `server-data`는 코드 배포 과정에서 삭제하거나 덮어쓰지 않는다.
29-1. Synology Compose의 기본 Docker subnet과 gateway는 `KIWOOM_DOCKER_SUBNET`·`KIWOOM_DOCKER_GATEWAY`로 고정한다. HTTPS 쓰기 신뢰 프록시는 같은 gateway 값 하나만 사용한다. 재생성 때 달라질 수 있는 자동 할당 주소를 별도 `CREDENTIAL_TRUSTED_PROXIES` 값으로 복제하지 않고, 대역 충돌 시 Docker 네트워크를 먼저 확인한 뒤 subnet과 gateway를 함께 변경한다.
29-2. NAS 서버의 요청별 Uvicorn access log는 Container Manager stdout에 쌓지 않고 `server-data/logs`의 자정 회전 파일에 기록한다. 회전 파일은 제한된 일수만 보존하며, 콘솔에는 요청마다 반복되지 않는 서버 상태·경고·오류를 남긴다.
29-3. NAS→키움 실제 REST 전송 감사 로그는 `kiwoom_monitor.kiwoom_api`에 본문 없이 namespace·TR 코드·연속조회 여부·소요시간만 기록한다. 중앙 DB/API 조회와 broker cache hit를 키움 호출로 기록하지 않는다.
29-4. TOP20 순위가 키움 응답 뒤 늦게 보이면 `조회후_ms`와 `저장_ms`를 먼저 분리한다. PostgreSQL dataset 저장이나 조회가 1초 이상 걸릴 때는 연결, snapshot upsert, metadata, observation revision, commit/close 단계별 시간 또는 조회 연결·query 시간을 경고 로그에 남겨 TR 지연과 DB 지연을 혼동하지 않는다.
30. 초기 ENV에서 이관된 구형 계좌 profile은 vault activation receipt가 없거나 `account_ref`만 있는 부분 activation이어도 검증된 최신 account binding이 남아 있으면 연결 해제에 그 binding을 사용한다. `operation_id`, `request_id`, keyed digest, account/run/environment/committed 시각이 모두 있는 완성 activation만 기존 receipt로 finalize한다. run ID가 없거나 UUID가 아닌 구형 임의 문자열이면 profile/account에서 결정적인 migration run ID를 만들며 새 계좌 신원이나 주문 runtime을 생성하지 않는다.

Qt GUI 테스트에서 `close()`는 객체 파괴 완료를 뜻하지 않는다. 창·대화상자를 반복 생성하는 테스트는 `deleteLater()` 후 `DeferredDelete` 이벤트 처리까지 수행해 C++ 객체가 Python 종료 시점에 몰려 파괴되지 않게 한다. 테스트 본문이 `OK`여도 네이티브 종료 코드가 0이 아니면 통과로 보고하지 않는다.

## 이 PC의 NAS 드라이브 접근

- 사용자 Windows 세션에는 NAS 공유가 `X:`로 연결되어 있고, 프로젝트 루트는 `X:\kiwoom-monitor`다.
- Codex의 기본 제한 실행 계정은 `desktop-ts9efil\codexsandboxoffline`이므로 사용자 계정에 매핑된 `X:`가 보이지 않을 수 있다. 기본 실행에서 `Get-PSDrive`, `net use`, `Test-Path X:\` 결과가 비어 있어도 **NAS 연결이 끊겼거나 X:가 없다고 결론 내리지 않는다.**
- X: 확인이 필요하면 먼저 승인된 제한 밖 **읽기 전용 명령**으로 다시 실행한다. 이때 사용자 계정 `desktop-ts9efil\pc-1`과 `X:\kiwoom-monitor`가 보이는지 확인한다.
- 사용자에게 매번 드라이브 권한을 새로 설정하라고 안내하지 않는다. 이것은 NAS 권한 부족이 아니라 Windows 세션별 드라이브 매핑 가시성 차이다.
- NAS에 쓰기 전에는 실제 대상이 정확히 `X:\kiwoom-monitor`인지 확인하고, `.env`, `postgres-data`, `server-data`를 제외한다. 파일 복사는 필요한 범위를 명시하고 기존 파일을 백업한 뒤 수행한다. NAS에는 `release`, `.codex-backups`, 운영 검사 산출물처럼 Git 작업본에 없는 파일도 있으므로 저장소 루트에 삭제형 미러(`/MIR`)를 사용하지 않는다.
- NAS 배포 여부는 X: 존재만으로 판단하지 않는다. NAS 파일의 빌드 식별자와 실행 서버 `/health.server_build`를 각각 확인한다.

## 이 PC의 Python 실행 환경

- 2026-09-09 승인된 제한 밖 실행으로 `C:\Users\pc-1\AppData\Local\Programs\Python\Python313\python.exe`의 Python 3.13.15가 정상임을 재확인했다. 이전의 본체 삭제 진단은 잘못된 판단이었다.
- 원본 프로젝트 `C:\Users\pc-1\Documents\ChatGPT\kiwoom-realtime-monitor\.venv`도 정상이다. 같은 실행에서 PySide6 6.11.1과 FastAPI 0.141.1 import를 확인했다.
- 회귀 실행 도구는 `현재 작업 트리 .venv → Git 공통 디렉터리로 찾은 원본 프로젝트 .venv → PATH → 사용자 Python 3.13` 순서로 찾고 각 후보의 `--version` 실행 성공까지 확인한다.
- 소스가 editable install되어 있지 않으므로 직접 실행할 때는 프로젝트의 `src`를 `PYTHONPATH`에 포함해야 한다. `run_core_regression.ps1`은 이를 자동 설정한다.
- 재설치나 가상환경 재생성은 필요하지 않다. 실행이 제한되면 승인된 제한 밖 실행으로 기존 가상환경을 사용한다.
- Codex 제한 환경에서는 설치 폴더를 읽어 파일 존재를 확인해도 프로세스 실행이 `액세스가 거부되었습니다`로 차단될 수 있다. 이것은 Python 미설치가 아니다.
- `python` 명령 실패만으로 미설치라고 결론 내리지 않는다. 알려진 경로 존재 여부와 전체 경로 실행을 확인하고, 실행 권한 제한이면 승인된 실행으로 재검증한다.
- `Unable to create process using ...`는 접근 제한에서도 발생한다. 오류를 숨긴 폴더 조회나 제한 환경의 빈 검색 결과로 삭제를 단정하지 않는다. 실제 경로를 오류가 보이도록 조회하고 승인된 실행 결과까지 확인한다.
- `dist/KiwoomMonitor/_internal/python313.dll`은 패키징된 앱 런타임 라이브러리이며 개발 테스트용 `python.exe` 대체물이 아니다.

## 이 PC의 테스트 앱 구성

- 테스트 앱의 실행 진입점은 현재 워크트리의 `scripts/run_test_app_with_data.py`다.
- 이 스크립트는 워크트리의 `src`를 `sys.path`와 `PYTHONPATH` 선두에 넣으므로 메인 프로세스와 자식 뉴스·매매일지 프로세스는 모두 현재 워크트리 코드를 사용한다.
- 실행기는 `C:\Users\pc-1\Documents\ChatGPT\kiwoom-realtime-monitor\.venv\Scripts\pythonw.exe`, 데이터 경로는 `C:\Users\pc-1\Documents\ChatGPT\kiwoom-realtime-monitor\data`를 재사용한다.
- 따라서 원본 프로젝트의 `.venv`와 `data`가 명령줄에 나타나는 것은 정상이다. 이를 원본 소스나 옛 실행파일이 실행된 증거로 해석하지 않는다.
- 사용자가 테스트 앱이 워크트리에 연결되어 있다고 알린 상태에서는 실행 소스 불일치를 반복 추측하지 않는다. 실제로 진입점이나 `SOURCE_ROOT`가 달라졌다는 증거가 있을 때만 실행 경로 문제를 제기한다.
