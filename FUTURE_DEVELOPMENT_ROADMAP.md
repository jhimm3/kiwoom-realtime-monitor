# 향후 앱 개발 로드맵

이 문서는 과거 시장을 재현하고 실제 매매와 비교할 수 있는 시뮬레이션 기반을 우선한다. 미래 계획이며 현재 구현 사실은 `ARCHITECTURE_CURRENT.md`를 따른다.

## 1. NAS 시장자료 기반 완성

- NAS가 평일 08:00~20:00에 앱과 무관하게 `ka00198` TOP20을 30초마다 조회한다.
- 새 순위의 20종목은 다음 30초 구간에 적용하고, 계속 편입되면 구독을 유지한다.
- 현재/다음 코호트의 `0B`를 구독해 종목 1분봉과 TOP20 거래대금 지수를 저장한다.
- 당일 한 번이라도 TOP20에 들어온 모든 종목을 보존한다. 마지막 20종목만 남기지 않는다.
- KOSPI/KOSDAQ 시장 상태와 수집 중단·미확정 상태를 실제 0과 구분해 저장한다.
- 장후 20:05 또는 다음 거래일 07:40 이전에 편입 종목의 당일 분봉 전체와 최근 250일 일봉을 저우선순위로 보완한다.
- 앱은 완료된 NAS 봉을 우선 읽고 coverage가 없거나 실제로 누락된 구간만 키움 TR로 요청한다.
- 중앙 분봉·일봉·순위·TOP20 이력은 자동 삭제하지 않는다.
- NAS 저장 용량 상한을 GB 단위로 설정할 수 있게 한다. `0`은 무제한이며, 양수(예: `100GB`)이면 실제 사용량이 상한을 넘을 때 가장 오래된 삭제 가능 자료부터 정리한다.
- 용량 정리 시 매매일지·체결 회차에 연결된 차트 원본, 스냅샷, 뉴스·AI 분석 등 복기 근거는 기간과 무관하게 보호한다. 재생성 가능한 캐시·파생 자료를 먼저 정리하고, 삭제 우선순위와 보호 참조 관계를 스키마·운영 화면·테스트로 명시한 뒤 도입한다.

## 2. 시점별 테마·뉴스 기반

- 테마/섹터 종목 관계를 일별 또는 유효기간 방식으로 버전 관리한다.
- 뉴스 게시·수집·AI 분석 시각을 보존하고 당시 TOP20 종목과 결합할 수 있게 한다.
- 현재 테마를 과거 자료에 소급 적용하지 않는다.

## 3. 시뮬레이션용 읽기 모델

- 대상 universe는 우선 해당 거래일 TOP20에 한 번이라도 편입된 종목으로 한다.
- 순위, 테마/섹터, 뉴스, 시장 상태, 일봉, 분봉, 거래대금, 신고가 거리를 시각 기준으로 결합한다.
- 결측은 0으로 만들지 않고 미수집/부분/장후 보완으로 구분한다.
- 조회 모델과 전략 계산기를 분리해 원본 데이터를 다시 쓰지 않는다.

## 4. 패턴·전략 연구

- 시장 수급 상태판과 거래대금 집중 구간
- 일봉 매물대/Open Space
- 1분봉 Base/M/W 및 돌파·눌림 타점
- 종목 압축 화면과 조건 탐색
- 승률, 기대수익, 손익비, MFE/MAE 비교

## 5. 실제 매매일지 비교

- 동일 시점의 모의 진입과 실제 체결 회차를 비교한다.
- 실제 매매유형, 전략팩 원칙 준수, 수급·뉴스·시장상태 차이를 함께 복기한다.
- 분석 결과와 실제 주문 실행은 계속 분리한다.

## 보류

- 임시 기반으로 Yahoo 지연 시세의 `MNQU26.CME`, `CLV26.NYM` 5분봉·일봉 누적을 시작한다.
- 전월물·차월물을 동시에 수집하고 거래량 우위를 확인해 자동 롤오버하되, 월물 간 절대가격 차이를 시장 등락으로 계산하지 않는다. 대표 방향값은 각 월물의 전일 종가 기준으로 이어간다.
- 계약 만기 전후에는 논리 상품과 실제 계약을 잇는 명시적 롤오버 이력을 추가한다. 과거 계약 자료를 새 계약으로 덮어쓰지 않는다.
- 정식 데이터 공급원을 정하면 `central_external_bars` 계약은 유지하고 수집기만 교체한다.
- 지연·비공식 자료이므로 실제 주문 판단에는 사용하지 않고 과거 시장 환경 비교에만 사용한다.
