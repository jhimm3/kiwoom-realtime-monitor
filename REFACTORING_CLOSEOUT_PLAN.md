# 리팩터링 종료 계획과 잔여 작업 원장

이 문서는 리팩터링 종료까지의 **유일한 진행 체크리스트**다. 기능 로드맵은
`FUTURE_DEVELOPMENT_ROADMAP.md`, 현재 구조는 `ARCHITECTURE_CURRENT.md`를 따른다.

## 진행 원칙

- 현재 단계가 끝나기 전에는 다음 단계나 신규 기능으로 이동하지 않는다.
- 작업 중 발견한 사항은 `진행 차단`, `현재 단계 관련`, `최종 보류`로만 분류한다.
- 진행 차단이 아니고 현재 단계와 직접 관련 없는 요청은 아래 보류 원장에 기록하고 즉시 구현하지 않는다.
- 원인을 재현하거나 로그·테스트·실측으로 확인하지 못한 항목은 수정하지 않는다.
- NAS 배포는 중간에 반복하지 않고 4단계에서 누적 소스를 한 번 동기화·재빌드한다.
- 각 단계 종료 시 이 문서의 상태, 검증 결과, 다음 한 항목을 갱신한다.

## 남은 진행 단계

### 1. 종료 범위 고정과 안정성 결함 정리 — 완료

- [x] 중앙 스냅샷·분봉·시장 상태·TOP20 지수 저장 실패 시 재시도
- [x] 조회 추정 거래대금이 같은 시각의 0B 실제 거래대금을 덮어쓰지 않도록 보호
- [x] 빈/오래된 NAS 차트 완료 캐시 재사용 차단
- [x] 과거 수동 유형 이전과 자동 판정 저장 원자화
- [x] 중앙 동기화에서 삭제한 수동 묶음·회차 유형의 재생성 차단
- [x] 반복 QThread 완료 객체 해제
- [x] PID 시작 식별자 확인 없이 다른 프로세스를 종료할 위험 차단
- [x] 메인 분봉 저장 실패 시 대기 자료 복원과 보완 완료 오판 차단
- [x] 열 설정 복원 중 중복 DB 저장 차단
- [x] 핵심 회귀 470개와 메인 GUI 회귀 17개, 종료 코드 0

### 2. Astra 과추상화·시작 성능 최종 감사 — 완료

분석만 수행하며 바로 코드를 바꾸지 않는다.

- [x] entry point → MainWindow → manager/controller/repository → UI 표시 경로 기초 실측
- [x] 시작 단계의 동기 파일 I/O·DB 조회·네트워크·worker/timer 생성 경로 확인
- [x] 실제 앱 실행의 단계별 시각 로그로 매번 발생하는 지연과 조건부 지연 구분
- [x] 기능별 `*_worker_controller.py` 중 단순 위임과 실제 수명 책임 구분
- [x] `MainWindow` worker 호환 속성과 중복 호출 깊이 확인
- [x] `stock_news_window.py`와 매매일지 종료 경계의 실제 위험 확인
- [x] 유지/통합/보류 판정 및 최소 변경 후보만 선정

### 3. 감사로 확인된 항목만 선택 수정 — 완료

- [x] 실제 병목·버그로 확인되고 현재보다 확실히 단순해지는 항목만 사용자와 범위 확정
- [x] 항목별 재현 테스트 → 최소 수정 → 인접 회귀 순서 준수
- [x] 선택되지 않은 항목은 삭제하지 않고 최종 보류 원장에 유지

선택 범위: 뉴스 프로세스가 실시간 메인 DB 파일 변경을 콘텐츠 변경으로 오인하여 매분 뉴스·AI·테마 약 1.8만 건을 다시 push/pull하는 피드백 루프만 수정한다. 시작 일봉 정리, import, 조건부 종료 대기, controller 통합은 이번 단계에서 수정하지 않는다.

### 4. 기존 보류 항목 전부 종결 — 완료

사용자 요청에 따라 기존 보류 원장의 아래 10개를 모두 조사한다. 조사 결과가
`변경 불필요`여도 근거와 회귀 범위를 기록하면 완료로 본다. 구조가 더 나빠지는
통합·분리는 강제로 수행하지 않는다.

#### 4A. 데이터 의미와 저장 신뢰성

- [x] NAS 독립 TOP20 KOSPI/KOSDAQ 분류의 실제 `ka00198` 응답·종목 카탈로그 근거 확인
- [x] `ka00198`에는 시장 필드가 없음을 확인하고 공식 KRX 종목 카탈로그로 분류 연결; 미등록 종목은 추측하지 않고 미확인 유지
- [x] 메모리 저장 재시도의 강제 종료 손실을 확인하고 디스크 영속 outbox 비용 검증
- [x] TOP20 지수 최소 outbox와 프로세스 재시작 복구 회귀 추가
- [x] PostgreSQL 실제 통합 검사기 작성: 마이그레이션·주요 CRUD·롤백·검증 행 정리
- [x] SQLite/PostgreSQL 중복 CRUD 감사: 공통 codec 밖의 연결·트랜잭션·placeholder 차이는 실제 방언 경계이므로 유지

#### 4B. 시작·종료·조회 성능

- [x] 분봉 30일·일봉 250개 유지 정리의 SQL/실행 시점별 비용 비교 후 창 표시 30초 뒤 유지보수로 이동
- [x] 뉴스/매매일지 종료 대기와 QThread 잔류를 로그·프로세스·회귀로 확인; 현재 재현이 없어 변경 불필요 판정
- [x] 실제 매매일지 DB로 반복 조회(N+1) 프로파일링: 315묶음 0.4356초 → 일괄 조회 0.0048초
- [x] 요약 지원자료·백필 상태 일괄 조회로 최소 수정하고 관련 회귀 실행

#### 4C. 과추상화·대형 UI 최종 판정

- [x] 작은 worker controller를 호출 깊이·수명 결함 반복 여부로 재확인
- [x] `main_window.py`·`stock_news_window.py`의 변경 집중도와 독립 책임 재확인
- [x] controller 통합과 대형 UI 추가 분리는 호출 깊이만 늘릴 가능성이 커 현재 유지로 종결

#### 4D. 호환 명칭과 테마 레거시

- [x] `local_server`/`personal_server`/NAS 사용자 문구와 내부 값의 실제 사용처 감사
- [x] 테마 이중 저장 경로의 읽기·쓰기·복원 우선순위와 실제 DB 행 존재 확인
- [x] 화면은 로컬/NAS로 유지하고 내부 값·구형 테마 테이블은 설정/백업 호환 경계로 보존

### 5. 최종 회귀·문서·NAS 누적 배포 — 완료

- [x] 2.0.0 최종 기준 핵심 회귀 496개, MainWindow·설정 GUI 26개, 뉴스 프로세스·창 17개 실행
- [x] API/DB/모듈/사용자 변경 문서 최종 일치 확인
- [x] 다음 누적 서버 빌드 ID 세 곳 `2026.09.12-market-state-time-v1` 일치 확인
- [x] 새 이미지에서 `check_postgres_integration.py`로 PostgreSQL v3·주요 CRUD·롤백 실제 경계 검증
- [x] `X:\kiwoom-monitor`에 누적 소스 동기화 (`.env`, `postgres-data`, `server-data` 보존)
- [x] NAS 이미지 새로 빌드하고 프로젝트 시작
- [x] `/health.server_build`, 인증 DB 조회, WebSocket 실검증
- [x] NAS 단절 시 동일 클라이언트의 로컬 `ka00198` 20종목 장애전환과 NAS 복구 실검증
- [x] 완료 항목과 아래 미실행 보류 항목을 `reports/REFACTORING_CLOSEOUT_REPORT.md`에 최종 보고

### 6. NAS 실환경 운용 검증 — 후속 운영 항목(리팩터링 종료 차단 아님)

- [ ] NAS 장시간 운용 중 메모리·디스크·수집 연속성 확인
- [x] NAS 단절→로컬 장애전환→NAS 복구 경로 확인
- [ ] 다른 PC에서 동일 NAS API·DB 조회와 동시 사용 확인
- [x] 환경상 즉시 수행할 수 없는 장시간·다른 PC 검증의 방법과 완료 기준을 최종 보고서에 기록

## 최종 보류 원장

아래 항목은 잊지 않되 증거나 선행 조건 없이 현재 리팩터링에 끼워 넣지 않는다.

| 항목 | 보류 이유 | 다시 검토할 조건 |
| --- | --- | --- |
| NAS 독립 TOP20의 KOSPI/KOSDAQ 분류 | **4A로 승격** | 실제 응답/카탈로그 확인부터 진행 |
| 저장 재시도의 디스크 영속 outbox | **4A로 승격** | 강제 종료 손실 재현부터 진행 |
| PostgreSQL 실제 통합 검증과 SQLite/PostgreSQL 중복 CRUD 추가 정리 | **4A로 승격** | 테스트 DB 통합 검증부터 진행 |
| NAS 장시간 운용·단절/복구·다른 PC 접속 검증 | **6단계로 승격** | 누적 배포 후 실환경 검증 |
| 시작 일봉 250개 유지 정리 | **4B로 승격** | 대안 비용 비교 후 판정 |
| 뉴스/매매일지 종료 대기와 worker 정리 추가 변경 | **4B로 승격** | 종료 재현·계측 후 판정 |
| 매매일지 반복 조회(N+1)와 설정/DB 중복 읽기 | **4B로 승격** | 실제 DB 프로파일링 후 판정 |
| 작은 worker controller 통합 | **4C로 승격** | 유지 결론 포함 최종 종결 |
| `main_window.py`·`stock_news_window.py` 추가 분리 | **4C로 승격** | 변경 집중도 근거로 최종 종결 |
| `local_server`/`personal_server`/NAS 명칭과 테마 이중 저장 레거시 | **4D로 승격** | 호환 자료 조사 후 판정 |
| README 버전·릴리스 노트 정리 | **2.0.0 릴리스 작업으로 완료** | 다음 배포 버전 확정 시 반복 |
| 시장 수급·Open Space·Base/M/W·압축 화면·시뮬레이션 | 리팩터링이 아닌 신규 기능 | 4단계 종료 후 `FUTURE_DEVELOPMENT_ROADMAP.md` 순서로 진행 |
| NAS 저장 용량 상한·보호 보존 정책 (`0`=무제한, 양수=GB) | 사용자 데이터 삭제 정책과 매매일지 참조 보호 스키마가 필요한 신규 운영 기능 | 재생성 가능 자료의 삭제 우선순위와 매매일지 연결 자료 보호 관계를 확정한 뒤 구현 |

## 현재 위치

- 현재 단계: **기능 보존 중심 리팩터링 종료 및 2.0.0 릴리스 준비 완료**
- 바로 다음 작업: **신규 기능은 `FUTURE_DEVELOPMENT_ROADMAP.md` 순서로 진행; 장시간 운용·다른 PC 접속은 후속 운영 검증으로 누적**
- NAS 상태: **`2026.09.12-market-state-time-v1` 배포·시작 완료**. `/health` 빌드 ID, 인증 API, WebSocket ready/pong, 최근 마켓스테이트 100건의 `T88:88` 0건, PostgreSQL v3 통합, NAS 단절·로컬 전환·NAS 복구를 확인했다. 장시간 운용과 다른 PC 접속 검증만 남음
- 후속 누적 변경: **`2026.09.12-market-metadata-gaps-news-ai-v1` NAS 배포·운영 검사 완료**. 신고가/기본정보/NXT 영속 저장, NAS 실시간 원본 단절 TOP20 공백, 종목 관점 뉴스 AI 계약을 포함한다. `/health.server_build`, 인증 API, WebSocket ready/pong과 새 메타데이터 조회 경로를 확인했다. 토요일이라 실제 장중 단절→공백→재개 표시는 회귀 테스트로 검증했고 다음 거래일 운영 확인만 남는다.
- 최신 NAS 누적 배포: **`2026.09.12-shared-backup-contract-v1` 배포·검증 완료**. 이전 `news-ai-backlog-v1`의 과거 뉴스 자동 재분석 폭주 차단·AI 공급자 HTTP 상태 보존에 더해 공통 표 표시/순서, 매매일지 뉴스 연결, 전체 테마 메타데이터 중앙 보존을 포함한다. 테마 전송 실패의 디스크 대기·자동 재시도와 NAS/뉴스 설정 화면의 공통 운영 설정 연동도 누적했다. 클라이언트에서는 ETF·ETN 미분류 때문에 KRX 전체 목록을 반복 저장하던 조건도 제거했다. `/health.server_build`, 인증 콘텐츠 API, WebSocket ready/pong과 최근 순위 스냅샷을 확인했다.
