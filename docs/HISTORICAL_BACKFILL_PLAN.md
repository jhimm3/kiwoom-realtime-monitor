# 과거 자료 확보 실행 기획

갱신일: 2026-09-24 · 상태: **원주가 분봉 연구 기준 확정·연결 검증 진행** · 상위 문서: [개발 로드맵](../FUTURE_DEVELOPMENT_ROADMAP.md)

2026-09-24 연구 입력 결정: 타점·장중 체결·위험 연구에는 당시 실제 거래가격인 원주가 1분봉·5분봉을 사용한다. 긴 기간의 특징과 거래일 간 수익률은 키움 수정 일봉과 기업행동 사건 기록을 별도로 사용한다. 원주가 분봉과 수정 일봉을 같은 절대 가격축으로 결합하지 않는다. 기업행동일을 가로지르는 보유 시뮬레이션은 수량·현금 변화를 처리하거나 해당 사례를 제외·표시한다. 아래 수정분봉 검증은 공급자 가격 차이를 설명하는 감사 결과이며, 키움 기준 수정분봉 전체 재구성은 타점 연구의 선행 조건이 아니다. 과거 원주가 분봉의 시각·공급자 기준과 누락 범위를 검증한 뒤 연구 조회 경로에 연결한다.

2026-09-25 운영 NAS 대조 시작: `scripts/audit_historical_nas_minute_alignment.py`는 로컬 CREON 원주가 정규장 1분봉(보통 구간 종료시각)과 NAS 저장 키움 KRX 1분봉(보통 구간 시작시각)을 읽기 전용으로 대조한다. CREON 15:30 마감 체결은 일반 분봉처럼 1분을 빼면 가짜 15:29 봉이 되므로 두 원천의 15:30 체결을 같은 시각에 맞춘다. 이 수정 뒤 2026-09-22 `035720` 정규장 381봉의 OHLCV가 모두 일치했고 NAS 응답에는 정규장 외 234봉이 더 있다. `347850`은 CREON 정규장 381봉에 대해 NAS 저장 봉 0건이다. 두 NAS 조회의 전체일 coverage는 모두 미완료다. 운영 `central_minute_bars`에 CREON을 덮어쓰거나 15:30 이후 봉을 D03 정규장 입력에 섞지 않는다.

같은 날 `scripts/audit_historical_five_minute_clock.py`로 CREON 5분봉 원주가의 종료시각을 확인했다. 2023-09-22 세 종목·2024-08-28 네 종목은 모두 09:05~15:20의 5분 구간 76개와 15:30 별도 종가 단일가 체결 1개, 총 77개였다. 2024-08-29에는 같은 네 종목 중 두 종목만 1분봉으로 전환했고 하루 안에서 두 해상도가 섞이지 않았다. 15:25 관측이 없는 것은 해당 표본의 종가 단일가 시각 경계이지 자동 데이터 누락 판정이 아니다. 5분봉과 1분봉은 수집 범위를 날짜별로 분리했으므로 같은 날 OHLCV 집계 일치는 검증하지 못했다. 기존 D03 1분 전략 어댑터의 15:30 체결 재생은 15:20~15:30 경매 단계로 수정했고, 이미 동결한 파생 입력은 불변으로 유지한다. 새 성과 비교에는 수정된 새 파생 입력 ID를 사용한다.

2026-09-25 D03 월별 파생 입력을 새 ID(`historical-strategy-be5976b6...`, 개발 패키지 `historical-development-f65533cc...`)로 재생성했다. TRAIN 12일·VALIDATION 4일만 실행하며 OOS 1일은 봉인했다. 개발일 16개의 일반 분봉 244,252개와 모집단 16개는 revision이 같고 657개 종가 체결의 재생 시각·시장 단계만 정정된 것으로 감사했다. 기존 v1 세션 프로필 문서와 새 날짜별 시간표 확장 문서를 모두 정확히 검증해 예전 불변 입력도 재현할 수 있게 했다. 다음 봉 모형의 장마감 미체결 주문은 다른 종목의 봉이 먼저 도착하더라도 다음 세션 경계에서 검열해 예약 현금과 종목 슬롯을 해제한다. 다른 종목 봉의 revision은 검열 주문의 체결 근거로 기록하지 않는다. 월별 표본은 수집 중인 희소 사례이므로 이 구조 실행만으로 전략을 선택하거나 성과를 주장하지 않는다.

최종 동일 구현 hash의 옛/새 TRAIN 눌림 대조에서 옛 입력은 후보 50·제출 67, 새 입력은 후보 51·제출 68·장마감 주문 검열 1이다. 두 입력 모두 67체결·50종료 거래·순손익 -1,976원이며 100개 FILL/RISK_FILL 사건 순서와 12일의 날짜별 종료 거래·손익이 같다. 검증 원장은 `data/research/historical-development-results/clockfix-control-old-input-current-code/clockfix-control-comparison.json`이다. 주문 수명 수정 후 나머지 TRAIN 돌파·VALIDATION 두 Family도 같은 구현 hash로 재실행해 네 구조 실행이 모두 `ELIGIBLE`인 것을 확인했다. 최종 결과표는 `data/research/historical-development-results/monthly-2024-09-to-2025-12-clockfix-v2/SUMMARY-boundary-fix.md`이며 전략 우열 판단에는 사용하지 않는다.

특수 거래일은 고정 15:30 규칙을 쓰지 않는다. [KRX 2025-11-13 공지](https://kind.krx.co.kr/external/2025/10/30/000102/20251030000137/99303.htm)는 정규장 10:00~16:30을 명시한다. `347850` 당일 379개 CREON 원분봉에서 15:30은 일반 봉, 16:30은 별도 종가 체결이며 NAS 저장 봉은 0개였다. 2021-11-18·2022-11-17·2023-11-16의 삼성전자 5분봉 각 77개도 10:05~16:20 및 16:30 격자와 일치했다. 2024-11-14 삼성전자는 해당일 5분 대신 1분봉 381개다. 공통 시간표는 확인된 2021~2025 수능일 다섯 날짜만 예외로 두고, 역사 어댑터와 감사 도구는 거래일의 종가 시각을 참조한다. 이 시각 검증만으로 공급자 간 가격 기준·종목 전체 완전성이나 OOS 성과를 확인한 것은 아니다.

## 목표와 범위

기존 주도후보를 유지하며 대신증권의 최근 약 2년 1분봉·최근 약 5년 5분봉과 네이버 웹사이트의 과거 뉴스를 확보한다. 지금까지 쌓은 자료와 연결해 LLM 분석·학습 및 기존 연구 엔진에 공급할 사례를 만든다. 표본 수집기는 현재 저장소의 `scripts/probe_historical_backfill.py`에 구현했으며 원본 후보 DB는 수정하지 않는다.

## 확인한 출발 자료

원본: `C:/Users/pc-1/Desktop/kiwoom_history_backfill/data/kiwoom_history.sqlite3`

아래는 이번 읽기 전용 집계 시점의 파일 값이다. 원본 크기·수정시각의 변화가 없음을 확인했다. 총행수와 최소/최대 날짜가 모든 날짜의 완전성을 뜻하지 않는다.

| 항목 | 실제 확인값 |
|---|---|
| `candidate_days` | 2019-01-02~2026-09-18, 1,895일, 하루 50개, **94,750 종목·일**, 2,945종목 |
| `daily_bars` | 2019-01-02~2026-09-18, **5,910,806행**, 4,302종목 |
| `minute_bars` | 2025-09-01~2026-09-18, 257일, **4,562,200행**, 1,895종목 |
| 분봉 작업 `done` | 94,750건 중 **81,901건은 저장 0행**, 12,849건만 1행 이상 |
| 인접 `news_history.sqlite3` | 기사 1,615행, 발행값 2020-01-02, FLASH/일반 웹뉴스 검색, 제목·요약·링크, 본문 컬럼 없음 |

후보는 최종 일봉의 거래대금·상승률·고가 상승·거래량 증가 등을 조합한 사후 수집 모집단이다. 실제 과거 Top20나 단순 Top40로 이름을 바꾸지 않는다. 점수·선정 이유를 보존하고 이 후보를 다시 만드는 작업부터 시작하지 않는다.

2026-09-23 원본 DB와 수집 코드를 다시 확인했다. `daily_bars`는 키움 `ka10081`을 `upd_stkpc_tp=1`로 요청한 수정주가 계열이며 종목 일봉을 다시 수집하지 않는다. 카카오 `035720`의 2021년 5대1 분할 전 2021-04-08~14 가격도 약 10.8만~11.2만원으로 현재 주식 단위에 맞게 보정되어 있음을 실제 행으로 확인했다. 다만 DB의 `adjust_type`·`adjust_rate`는 5,910,806행 모두 `NULL`이므로 액면분할·병합·감자 등 기업행동 사건 목록이 별도로 확보된 것은 아니다. 키움 공식 REST `ka10081`의 현재 응답 계약에는 가격·거래량·거래대금 등만 있고 사건 종류·조정비율 필드가 없다. `upd_stkpc_tp`는 요청의 수정주가 적용 여부다. 과거 백필 코드가 응답에 없는 같은 이름과 `upd_rt`를 읽으려 해 두 저장 컬럼이 비었다. 영웅문 차트의 기업행동 표시는 이 공개 REST 일봉 응답과 별도 자료로 취급한다. 대신 CREON `StockChart`는 일봉 필드 18 `수정주가일자`와 19 `수정주가비율`을 제공하므로 날짜 필드 0과 세 필드만 요청하는 경량 보완 수집이 가능하다. 이 두 필드는 사건 종류를 제공하지 않으므로 액면분할·병합·감자 등 명시적 분류가 필요한 연구만 KRX/KIND 등 검증된 별도 사건 원천을 추가한다. 초기 장기 수익률·일봉 특징에는 수정주가를 사용하고, 대신 원주가 분봉과 일봉 수정주가를 같은 가격 수준으로 직접 비교하지 않으며, 장중 체결가격은 원주가 분봉, 거래일 사이 수익률과 장기 특징은 수정주가 일봉을 사용한다.

2026-09-24 NAS 운영 API와 CREON 관리자 세션을 실제 대조했다. `005930`·`000660`·`035720`·`042660`의 PC 일봉 7,580행은 NAS `central_daily_bars`와 OHLCV 전부 일치했고, NAS에는 이 PC 파일보다 오래된 2016년 일봉도 있었다. 그러나 `347850`의 PC 2025-10-31 분봉 381행은 NAS `central_minute_bars`에 KRX/NXT/SOR/전체 어느 조회로도 없었다. NAS의 키움 분봉이 전 종목·전 기간 완전하다고 보거나 PC 파일 전체를 재적재하지 않는다. 특히 `347850`의 2025-10-31 PC 1분봉 09:00 가격은 240,000~242,000원이고, 같은 분의 CREON 원주가와 일치한다. CREON 수정주가는 60,060~60,561원으로 달랐으며 비교 가능한 379분 모두 OHLCV가 달랐다. 키움 수집 요청의 `upd_stkpc_tp=1`만으로 저장된 **1분봉**을 수정주가라고 단정할 수 없다. 같은 날 PC·NAS 키움 일봉은 시가 60,072원으로 보정된 수준이다. 실시간/기존 1분봉을 수정주가로 덮어쓰면 장중 원주가 경로가 훼손되므로, 직접 NAS 적재 시 수정 기준과 공급자를 별도 키로 보존하고 연구 조회에서 명시적으로 선택한다. CREON 분봉 시각은 구간 종료, 키움 PC/NAS 분봉 시각은 구간 시작으로 확인돼 같은 분 비교에는 CREON 시각에서 1분을 뺀다.

같은 날 추가로 10개 기업행동 종목의 사건 전후 각 1일과 일반 종목 4일을 비교했다. 키움 1분봉과 CREON 원주가 1분봉의 공통 7,538분 중 가격 OHLC가 같은 것은 5,466분, 거래량까지 같은 것은 5,430분이다. 사건 전 10일의 공통 3,312분은 가격이 모두 일치했지만 사건 후 10일의 2,746분 중 가격 일치는 674분뿐이었다(7일은 일치 0분, 3일은 전부 일치). 일반 종목 4일의 공통 1,480분은 가격이 전부 일치했다. `347850`의 2025-11-13은 키움 분봉 약 32만 원 대 CREON 원주가 약 8만 원, `406820`의 2026-01-07은 약 2.1만 원 대 5,270원이다. 두 날짜를 관리자 CREON에서 새로 재조회해 기존 원응답 464분과 OHLCV 전부 일치시켰다. 가격 차이는 원응답 파일 오류가 아니다. 반면 두 종목의 사건 전 키움 **일봉** 종가는 CREON 원주가 일봉 종가의 약 1/4이었다. 따라서 키움 일봉·1분봉의 수정 기준은 서로 다르고, 키움 1분봉도 기업행동 후 일률적으로 원주가가 아니다. 키움의 정확한 분봉 조정 산식은 이 표본만으로 확정하지 않으며 사건별·날짜별 가격 기준을 검증해야 한다.

차이의 호출 조건도 키움 REST 실조회로 재현했다. `347850`의 **동일한 2025-11-13 10:00 분봉**은 `ka10080`에서 `upd_stkpc_tp=0`이면 시가 80,000원, `=1`에 `base_dt=20251113`이면 319,616원, `=1`에 `base_dt=20251114`이면 다시 80,000원이다. `406820`의 2026-01-07 09:02도 당일 기준 21,063원→다음날 기준 5,270원, `203400`의 2025-12-01 09:00도 5,040원→2,800원으로 같은 현상을 보였다. `ka10081` 일봉은 `347850` 2025-11-05 종가를 `base_dt=20251105, upd_stkpc_tp=1`에서 320,000원, `base_dt=20251113` 또는 `20260918`에서 80,096원으로 돌려준다. PC 수집기는 일봉을 마지막 날짜 기준으로 한 번에 요청했지만, 분봉은 각 후보 **당일을 `base_dt`로** `upd_stkpc_tp=1` 요청했다. 이 서로 다른 기준일 선택과 키움 API의 기준일 의존 응답이 PC 일봉/분봉의 가격 기준 차이를 직접 만들었다. `base_dt`와 `upd_stkpc_tp`의 공식 공개 설명은 입력값만 명시하고 기업행동 당일 조정 산식까지 설명하지 않으므로 공급자 내부 산식을 단정하지 않는다. NAS의 `autonomous_top20` 당일 분봉 보완도 같은 `base_dt=day`, `upd_stkpc_tp=1` 조합을 사용하므로 기업행동일의 운영 분봉 가격 오염 가능성을 별도로 수정·검증해야 한다.

2026-09-24 원주가끼리 재검증했다. 키움 REST `ka10080`을 `upd_stkpc_tp=0`으로 직접 재조회하고 보관된 CREON `StockChart` `Adjustment=raw` 원응답의 종료시각에서 1분을 빼 같은 봉으로 정렬했다. `347850` 2025-10-31은 공통 379분 중 OHLC 379·OHLCV 377 일치, 2025-11-13은 378·378 일치, `406820` 2026-01-07은 85·85 일치, `203400` 2025-12-01은 376·372 일치했다. 합계 **공통 1,218분의 OHLC 전부 일치**, OHLCV는 1,212분 일치하며 6분은 거래량만 달랐다. 두 공급자의 포함 시각도 완전히 같지는 않아 키움에만 6분, CREON에만 3분이 있었다. 따라서 앞선 큰 가격 차이는 비교 대상이었던 저장 키움 분봉의 수정 옵션/기준일 차이로 설명되며, 이 표본에서 키움 원주가와 CREON 원주가의 **가격** 차이는 발견되지 않았다. 이는 선정한 3종목·4거래일의 1분봉 검증이며 전 종목·전 기간이나 일봉/거래량의 동일성을 보증하지 않는다.

수정 가격의 연구 기준은 사용자가 실시간 수신에 쓰는 **키움 가격 기준**으로 잡는다. CREON `Adjustment=adjusted` 값을 키움 수정주가로 이름만 바꿔 쓰지 않는다. `347850` 2025-10-31 원일봉 시가 240,000원·키움 수정일봉 시가 60,072원에서 조정계수 `0.2503`을 얻어 CREON 원분봉 가격에 적용하자, 키움 `ka10080` 수정 옵션 1/기준일 2025-11-14의 공통 379분 OHLC가 모두 일치했다. `406820` 2025-12-23은 원일봉 시가 14,860원·수정일봉 시가 3,718원의 **반올림된 두 값의 비율**로 변환하면 공통 270분 중 264분만 맞았다. 키움 원/수정 분봉의 가격쌍에서 허용되는 단일 계수 구간은 `0.2501998668~0.2502001334`로 `0.2502`를 포함하며, CREON 원분봉과 키움 원분봉 가격은 이 270분 모두 일치했다. 즉 일봉 한 가격의 단순 비율은 공급자의 실제 조정계수와 달라질 수 있다. 원응답과 가격 기준 차이의 검증 결과를 보존한다. 현재 타점 연구에는 원주가 1분봉·5분봉을 쓰므로 파생 수정분봉을 만들 필요가 없다. 실시간 체결봉도 실제 거래가격으로 유지한다. 향후 수정분봉이 필요한 연구가 생길 때만 날짜별 조정 구간·키움 기준일·계수 출처·봉 단위 검증을 갖춰 별도 파생값으로 게시한다. 직접 대조 불가능한 과거 5분봉은 정확한 수정분봉으로 표시하지 않는다. 백테스트의 당시 판단에는 그 시점까지 알려진 기업행동만 사용해 미래 정보 유입을 차단한다.

2026-09-24 원천 완전성을 세 로컬 SQLite에서 읽기 전용 재집계했다. 수집 작업에 올라간 개별 주식 2,637개는 모두 키움 수정 일봉 행이 있고, CREON 원본 1분봉 보유 2,617개·5분봉 보유 2,448개다. 1분봉이 없는 20개 중 16개는 5분봉만 있고, `008290`·`046070`·`082660`·`099520` 4개는 두 주기 모두 없다. CREON 수정계수 작업은 2,634개 완료·3개 공급 불가이며, 개별 주식 조정 경계는 1,162종목의 2,106건이다. **2,106건 모두 사건일의 키움 일봉은 존재**하지만 CREON 사건일 원본 분봉/5분봉이 실제 있는 것은 1,416건이다. 690건 중 689건은 각 종목의 보유 봉 날짜 범위 밖이고 1건(`015590`, 2026-05-11)은 범위 안이지만 사건일 봉이 없다. DART 제목 근거에 의한 자동 유형 분류는 987건, 수동 검토 980건, 미매칭 139건이다. DART 분류 결과는 사건의 의미 근거이고 키움 수치 조정계수가 아니다. CREON 원일봉 기초정보는 후보일 위주 80,809 종목·일만 저장돼 있으며 조정 사건일과 겹치는 것은 354건이다. 따라서 현재 자료만으로 **모든 종목·모든 날짜의 키움 기준 정확한 조정계수가 이미 저장·검증됐다고 할 수 없다.** 보유 봉 구간의 키움 계수 추정·봉 단위 대조와 미검증 범위 표식이 별도 작업이다.

같은 날 저장된 CREON 원일봉 종가 80,809개를 키움 수정 일봉 종가와 결합하고, CREON 조정 경계 사이에 단일 곱셈 계수가 존재하는지 원 단위 반올림 허용 구간의 교집합으로 검사했다. 4,080개 종목·사건 구간 중 **84개가 불일치**했다. 2026-09-14 이후 후보일 170개를 제외하면 4,070개 구간 중 10개(`013890`, `017670`, `036220`, `062970`, `068270`, `092190`, `096250`, `101970`, `207940`, `244920`)가 여전히 불일치한다. 최근 74개 구간의 원인을 단순 스냅샷 노후화로 보지 않는다. `005930` 2026-09-18 키움 원일봉 종가 260,000원은 기준일을 9월 18/19/23일로 바꿔도 같지만, 키움 원분봉 15:30 종가와 CREON 원분봉/원일봉 종가는 모두 261,000원이었다. `000660`도 각각 키움 일봉 1,849,000원 대 양사 마감 분봉·CREON 일봉 1,857,000원이다. 내부 일봉/마감 분봉 불일치의 이유는 아직 확인하지 못했으므로 이 날짜의 일봉 비율을 검증된 분봉 조정계수로 사용하지 않는다. 잔여 10개 중 `036220`·`092190`·`096250`·`101970`·`244920`의 CREON 필드 19 원응답은 해당 조회 기간 모두 `100`으로, 추출기 누락이 아니라 CREON 사건 신호만으로 키움 수정가격 변화를 설명할 수 없는 표본이다. `101970` 2024-02-06을 키움 API에서 재조회하면 원일봉 종가 11,470원·수정일봉 종가 642원이며 CREON 원일봉 종가는 12,000원이다. 따라서 **전 종목의 키움 기준 수정주가 재구성 검증은 실패**했고, 공급자별 원일봉/마감 분봉 시각·가격 차이와 CREON이 포착하지 못한 키움 조정 경계를 별도로 해결해야 한다. 기존 379분·270분 성공 표본을 전체 통과로 확대하지 않는다.

CREON `StockChart` **일봉**의 원주가/수정주가도 관리자 세션에서 같은 날짜를 직접 조회했다. `347850` 2025-11-05 종가는 원주가 320,000원·CREON 수정주가 80,080원·기존 키움 수정 일봉 80,096원이고, `406820` 2025-12-23은 각각 19,310원·4,830원·4,831원이다. 삼성전자 2025-09-01은 세 값이 67,600원으로 일치한다. 즉 기업행동 전 키움 수정 일봉과 CREON 원주가의 큰 차이는 가격 기준 차이이며 CREON 일봉 자체가 더 정확하다는 증거가 아니다. 키움/CREON **수정 일봉끼리도** 소폭 차이(두 사건 표본에서 16원·1원)가 있으므로 공급자별 조정계수·반올림 차이를 검증하지 않고 동일 값으로 간주하지 않는다.

`조회날짜`는 API를 실행한 오늘 날짜가 아니라 요청에 지정한 `base_dt`라는 점을 구분한다. 2026-09-24 같은 실행일에 키움 `347850` 2025-11-13 10:00 분봉을 `base_dt=20251113`과 `20251114`로 요청하자 옵션 1의 가격이 319,616원과 80,000원으로 달랐다. 반대로 CREON 수정 1분봉의 **같은 2025-10-31 종목·분 380개**를 요청 기간 `2025-10-31` 단일일과 `2025-10-31~2025-11-14`로 각각 다시 받아 대조한 결과 OHLCV 불일치 0개였다. 이 표본은 CREON의 요청 종료일 변경이 같은 봉을 재기준화하지 않았다는 뜻이지, 향후 새 기업행동 발생 후 수정 이력 값까지 영구 불변이라고 증명하지는 않는다.

2026-09-24 불일치 구간을 격리하기 위해 `scripts/audit_kiwoom_adjustment_candidates.py`로 세 원본 SQLite를 **읽기 전용** 재검사하고 `data/historical_collection/kiwoom-adjustment-candidate-audit-20260924.json`에 종목·CREON 조정 경계별 판정 원장을 만들었다. 정확한 유리수와 반열린 원 단위 반올림 허용 구간을 사용한 결과 80,809 일봉 쌍·4,080구간 중 불일치 84구간, 일봉 기준 4자리 계수가 하나인 후보 2,948구간, 계수 다수 가능 608구간, 관측 1일뿐인 432구간, 4자리 계수가 없는 8구간이다. 후보 2,948구간은 **분봉 검증 완료가 아니며**, 현재 원장에 분봉 검증 완료로 표시한 구간은 0개다. 불일치 84구간은 향후 파생 수정분봉 입력에서 제외 대상으로 원장에 표시했다. 나머지의 분봉 직접 대조와 기준일 검증도 수정분봉이 실제 필요해질 때의 조건이며 원주가 분봉 연구를 막지 않는다. 2026-09-14 이후의 170쌍을 빼고 같은 산식으로 재검사한 4,070구간 중에도 10구간이 불일치했다. 이전 81·7 집계는 부동소수점 경계에서 상한 제외 조건을 놓친 과소계산으로 정정한다. CREON 봉이 전혀 없는 4종목도 별도 제외 대상으로 원장에 명시했다. 원본·NAS 운영 DB는 바꾸지 않았다.

2026-09-24 **분봉 직접 검증**: 저장된 키움 후보일 1분봉과 일봉 계수 후보가 겹치는 비단위계수 711종목·일을 CREON 원1분봉의 종료시각에서 1분을 빼 대조했다. 공통 234,646분 중 708종목·일은 키움 보관분 전체가 CREON **원주가**와 일치했다. 나머지 3종목·일(공통 764분)은 원주가와도 일봉 계수 후보와도 일치하지 않아 별도 기준일/계수 확인 대상으로 남겼다. 따라서 이 보관 분봉만으로 사후 기준의 수정계수를 검증했다고 표시하지 않는다. 상세 원장은 `data/historical_collection/kiwoom-adjusted-minute-overlap-audit-rounding-v2-20260924.json`이다.

키움 `ka10080` 수정 옵션 1을 사건 이후 `base_dt`로 **직접 재조회**한 6종목·일에서는 CREON 원분봉에 날짜별 계수를 적용한 공통 **2,025분의 OHLC가 모두 일치**했다: `347850` 379분·`0.2503`, `010170` 378분·`0.9583`, `475460` 242분·`0.3335`, `406820` 270분·`0.2502`, `059120` 379분·`0.5003`, `183300` 377분·`0.2041`. 마지막 종목은 두 번의 조정 사건이 있어 첫 사건 직후 기준일의 `0.5093`도 같은 377분에서 별도 일치했다. `010170`·`475460`·`406820`은 CREON 경계일 당일을 기준으로 조회하면 원주가, 다음 거래일을 기준으로 조회하면 조정값이 되어 **기준일 경계**가 직접 확인됐다. 이 결과는 명시된 6거래일·공통 봉의 검증이지 해당 종목의 모든 날이나 5분봉의 검증은 아니다. 원장: `data/historical_collection/kiwoom-adjusted-minute-verified-samples-20260924.json`과 각 표본의 실조회 JSON.

반올림 산식도 단정하지 않는다. `059120`은 4자리 계수 `0.5003`에서 동률 올림이면 379분 중 364분, 동률 짝수 선택이면 379분 모두 일치한다. 반대로 `183300`의 첫 단계 `0.5093`은 동률 올림이면 377분 모두, 동률 짝수 선택이면 373분만 일치한다. 공급자의 실제 계수가 4자리보다 정밀하면 다른 조합도 가능하다. 같은 80,809 일봉 쌍에 동률 짝수 모델을 적용하면 불일치 84구간은 유지되지만 단일 4자리 후보는 2,856구간으로 줄고 4자리 후보 없음은 103구간이 된다(앞의 2,948·8은 동률 올림 가정). 특히 `406820`은 일봉 4자리 계수 판정에서는 보류지만 분봉 270개로 `0.2502`가 직접 검증됐다. 따라서 일봉 후보 원장을 분봉 적격 목록으로 그대로 쓰지 않고, 기준일·실제 분봉·가격 정밀도 검증을 추가한다. 원본·운영 DB는 변경하지 않았다.

## 1. 수집 범위와 원본 보호

| 구간 | 확보·연결할 자료 | 연구 해상도 |
|---|---|---|
| 최근 2년 | 대신 1분·5분, 기존 키움 봉·실제 보유 NAS 관측, 뉴스 | 실제 있는 1분/1초 범위와 5분을 구분 |
| 2년 이전~최근 5년 | 대신 5분, 기존 일봉·후보, 확보 가능한 뉴스 | 5분 또는 일 단위 |
| 5년 이전 | 기존 일봉·후보와 확보 가능한 뉴스 유지 | 확보된 해상도만 사용 |

1분 2년과 5분 5년은 중첩 자료이며 7년의 독립 자료가 아니다. 중첩 1분/5분을 두 개의 독립 학습 사례로 세지 않는다. 5분에서 1분·1초 내부 경로를 합성하지 않는다.

후보일 전의 재료·가격 배경과 이후 결과를 볼 수 있게 선행/후행 구간을 설정으로 둔다. 같은 종목의 겹치는 구간은 합친다. 단순 후보 당일 수집만으로 전일·주말 재료를 놓치지 않게 한다. 제공기간은 수집 시작일·실제 응답으로 고정한다.

기존 `minute_bars`의 키는 `(code, ts)`라 공급자와 1분/5분을 구분하지 못한다. 새 봉을 여기에 바로 덮어쓰지 않는다. 표본 수집용 저장소를 분리하고 실제 앱 입력을 연결할 때 기존 DB 계약과 마이그레이션 경계를 정한다. 공급자·종목·거래소·세션·주기·수정기준·봉 시각을 원본 식별에 포함한다.

## 2. 대신증권 표본과 본 수집

공식 담당자의 2026-09-01 안내는 주식 1분 약 2년·5분 약 5년이다. 종목별 실제 도달 날짜·상장기간·중단 구간은 아직 API 응답으로 확인하지 않았다. [공식 제공기간 안내](https://money2.daishin.com/e5/mboard/ptype_basic/Basic_018/DW_Basic_Read_Page.aspx?boardseq=60&m=9508&p=8827&page=1&searchString=&seq=29008&v=8636)

2026-09-22 현재 이 PC에 CREON/CYBOS Plus가 설치되어 `CpUtil.CpCybos`, `CpSysDib.StockChart`, `CpUtil.CpCodeMgr` COM 등록을 32비트에서 확인했다. CREON은 로그인되어 있었지만 관리자 권한으로 실행되어 일반 권한 조회에서는 `IsConnect=0`이었다. 같은 관리자 권한의 32비트 PowerShell에서는 `connected=true`를 확인했고 `scripts/daishin_stockchart_probe.ps1`로 실제 조회했다. 별도 32비트 Python 설치는 표본의 선행 조건이 아니다.

표본은 정상 거래 종목, 거래정지/상장기간이 짧은 종목, 2년·5년 경계가 필요한 종목을 기존 후보에서 고른다. 설치된 CYBOS/CREON 접속 환경·Python/COM 호환성·로그인을 확인한 뒤 주문 없는 차트 조회만 수행하는 수집 경로를 만든다.

`CpSysDib.StockChart`의 분봉은 기간 요청과 연속조회로 공급자가 제공하는 최과거까지 내려간다. 1분봉의 최초 완전 거래일을 경계로 정하고, 그 이전 거래일만 5분봉으로 저장한다. 5년은 목표 하한일 뿐 고정 중단점이 아니므로 더 오래 제공되면 `Continue=false`가 될 때까지 받는다. 기간 요청이 최신 거래일을 빠뜨리는 실제 응답을 확인했으므로 개수 요청으로 최신 구간을 겹쳐 받아 키 중복 제거로 보강한다. 제공 잔여 요청량·대기시간을 사용하고 키움 NAS 실시간 큐에 대신 백필을 넣지 않는다. [연속조회 안내](https://money2.daishin.com/e5/mboard/ptype_basic/Basic_018/DW_Basic_Read_Page.aspx?boardseq=60&m=9508&p=8827&page=1&searchString=&seq=26051&v=8636), [StockChart 도움말](https://money2.daishin.com/e5/mboard/ptype_basic/HTS_Plus_Helper/DW_Basic_Read_Page.aspx?boardseq=284&m=9508&p=8839&page=1&searchString=StockChart&seq=102&v=8642)

중첩 구간의 키움·대신 값을 비교할 때 단위·수정기준·세션·시간 의미를 먼저 맞춘다. 대신 1분을 5분으로 집계한 결과와 제공 5분을 비교하되 원본 둘은 보존한다. 값 차이를 무조건 어느 한쪽 오류로 정하지 않는다.

본 수집은 가장 오래된 제공구간이 밀려나는 점을 고려해 표본 확인 후 진행한다. 원본 응답/정규화 결과/작업 상태를 구분하고 구간별 체크포인트·재시도·중복 방지·확보 보고를 둔다.

삼성전자 KRX 정규장·무수정 연속조회에서 1분봉은 2024-08-29 09:01부터 2026-09-21 15:30까지 190,102개를 확보했다. 5분봉 원응답은 2021-08-11 09:05까지 도달했고, 정책 경계 전날인 2024-08-28까지 57,710개만 DB에 반영했다. 경계일은 2024-08-28의 5분봉 77개와 2024-08-29의 1분봉 381개로 이어진다. 5분봉 `15:15` 거래량은 1분봉 `15:11~15:15` 합과 정확히 일치하므로 봉 시각은 구간 종료시각인 `interval_end`로 기록한다. 공급자 원시 날짜·시각과 전체 NDJSON 응답도 별도로 보존한다.

2026-09-24 관리자 32비트 CREON 세션에서 부족해 보이는 구간을 다시 조회했다. 개별 주식 2,637개 작업은 `complete` 2,617개·`failed` 20개다. 실패 20개를 1분봉으로 다시 요청하자 17개는 0건, `008290`·`046070`·`082660`은 StockChart 종목코드 거부가 재현됐다. 완료 작업 중 5분봉 저장이 0건인 185개는 원응답상 44개가 1분봉 보유기간과 전부 겹쳐 저장 제외된 것이고, 141개는 당시 원응답이 0건이었다. 후자 141개의 1분봉 최초일 **전날까지** 5분봉을 각각 다시 요청해 모두 0건을 확인했다. 후보 DB `stocks.reg_day`와 비교하면 185개 중 181개는 1분봉 최초일이 상장일과 일치한다. 예를 들어 `475040`은 2026-06-30, `475230`은 2025-07-28, `475400`은 2024-10-24 상장이다. 사용자가 표에 적은 `다음날 상장`은 재조회 종료일의 **다음 거래일**에 상장했다는 뜻이며, 현재 날짜의 다음날이라는 뜻이 아니다. 나머지 `109070`·`178780`·`208860`·`257370`은 기존 상장 종목이지만, 키움 일봉의 2021-08-01부터 CREON 1분봉 최초일 전날까지 양(+) 거래량 날짜가 모두 0개다. 1분봉 최초일은 각각 2025-03-26, 2025-01-15, 2025-07-07, 2024-10-10이며 뒤 두 거래재개 사례 중 `257370`의 2024-10-09는 휴장일이어서 실제 첫 거래는 10-10이다. 따라서 이 네 종목도 5분봉 대상 구간에는 거래 공백이 확인되지만, 이전 시기의 거래 이력까지 없다고 확대 해석하지 않는다. 재조회 결과는 `data/historical_collection/creon-missing-interval-probe-results-20260924.ndjson`에 보존한다. 표본 `004450`의 2025-07-03 1분봉 136개는 새 CREON 응답과 기존 DB의 시각·종가·거래량이 전부 일치한다. 월별 연구 후보 16사례의 666 종목·일 중 21개가 300봉 미만이지만, 이 21개의 보관 원응답과 DB도 전부 일치하고 작업은 완료·마지막 원응답은 `provider_has_more=false`였다. 정상 유동 종목의 381봉은 15:20 다음 봉이 15:30으로 표시되는 종가 단일가 구간을 포함한다. 이 검사는 기록된 대상·조회시점의 CREON 반환과 저장 일치를 검증한 것이며, 공급자 원천의 전 종목·전 날짜 완전성을 보증하지 않는다.

1분봉 0건 재확인 대상 17개는 후보 DB 키움 일봉의 마지막 양(+) 거래량 날짜가 모두 저장된 CREON 5분봉의 최종일과 같고, 모두 2024-08 이전이다. 예를 들어 `000300`은 2024-02-23, `005030`은 2023-04-11, `005110`은 2024-03-27, `016790`은 2024-02-29, `036690`은 2024-03-21, `041590`은 2024-03-28 이후 2026-09-18까지의 키움 일봉 거래량이 0이다. 따라서 이 17개의 최근 1분봉 0건은 확인된 거래 공백과 맞으며, 분봉 수집기의 누락으로 분류하지 않는다. 키움 일봉이 존재해도 거래량 0인 날짜를 체결 가능한 봉으로 합성하지 않는다. 반면 CREON 종목코드 거부 3개는 이 그룹과 다르다. `008290`은 키움 일봉에서 2026-09-18까지 거래량이 있고, [KRX KIND 회사개요](https://kind.krx.co.kr/corpdetail/totalinfo.do?isurCd=00829&kisComCd=322385&method=searchTotalInfo&repIsuCd=KR7008290009)가 상장폐지로 표시한다. `046070`도 키움 일봉의 2026-09-18 양(+) 거래량과 [2026-09-08 KRX 상장폐지 일정 공시](https://spn.stockplus.com/news/api/v1/disclosure_views/koscom/504207)의 정리매매 기간(09-10~09-18)·상장폐지일(09-21)이 일치한다. 두 종목의 CREON 거부를 거래 없음으로 해석하거나 상장폐지가 거부의 직접 원인이라고 단정하지 않고, 대체 공급자 자료 확인 대상으로 분리한다.

신규 상장으로 설명되는 완료 작업도 별도로 구분한다. `031210`은 후보 DB 상장일과 CREON 1분봉 최초일이 모두 2025-03-14, `061090`은 모두 2025-11-14, `064400`은 모두 2025-02-05이다. 세 종목의 보관 5분봉 0건은 상장 전 5분봉 구간과 일치한다.

개별 주식 2,637개 중 CREON 분봉 두 주기가 모두 없는 종목은 `008290`·`046070`·`082660`·`099520` 네 개다. 사용자가 확인한 현 상태는 앞의 세 종목이 상장폐지, `099520`이 거래정지다. 키움 일봉의 마지막 양(+) 거래량 날짜는 앞의 세 종목이 모두 2026-09-18, `099520`이 2021-03-25다. 이 현 상태를 이유로 과거 후보·일봉 기록을 삭제하지 않는다. **분봉 기반 D03 사례에서 결과 거래일에 연속 1분봉 한 쌍이 없으면** 기존 `--exclude-noncontinuous-minute-candidates` 정책으로 해당 종목·일 사례만 제외하고 제외 원장에 남긴다. 상장폐지·거래정지 상태 자체를 과거 모든 날짜에 소급 적용하지 않는다. 종목 단위로 2,633개에 1분봉 또는 5분봉이 있다는 사실은 각 거래일의 봉 완전성을 보증하지 않는다.

이 공백 사유 조사에서는 이미 모은 DART 원장의 범위를 먼저 확인하지 않은 절차상 누락이 있었다. `historical_stock_adjustment_disclosures`는 전체 공시 이력이 아니라 `historical_stock_adjustments`의 CREON 수정계수 사건별로 사건 전 180일~후 14일만 조회한 결과다. 분봉 실패 20종목 중 13개에만 이 DART 작업이 있고 7개는 없으며, 상장폐지 세 종목(`008290`·`046070`·`082660`)은 모두 수정계수 사건·DART 작업이 0건이다. 따라서 이 원장에 공시가 없다는 사실은 상장폐지·거래정지 근거가 없다는 뜻이 아니다. 상태 사유가 필요한 별도 검증에는 해당 날짜의 KRX 공시와 DART 공시를 직접 조회하고 출처를 기록한다.

2026-09-24 사용자 요청으로 개별 주식 후보 2,637종목의 **이벤트 날짜 DART 공시 원장**을 추가했다. `scripts/collect_candidate_event_disclosures.py`는 후보 원본 DB를 읽기 전용으로 사용하고, 기존 수정계수 사건의 완료 DART 원응답은 재요청하지 않고 연결했다. 추가 조회 날짜는 후보 DB의 상장일, 키움 일봉에서 5거래일 이상 연속 거래량 0으로 전환된 첫날과 이후 거래량 양(+) 재개일, 사용자 확인 상폐 종목 3개의 마지막 거래일(상폐일이 아닌 검색 기준일)이다. 거래량 0 전환은 **거래정지 확정이 아닌 검색 단서**다. 4,587개 사건 단서의 최종 작업 상태는 기존 공시 재사용 1,993·새 조회 완료 2,531·공식 빈 응답 63이며 실패·미매핑은 없다. 각 DART 조회의 전체 공시 목록·접수일·원응답 파일·작업 상태를 `historical_market_context.sqlite3`의 `historical_candidate_event_jobs`/`historical_candidate_event_disclosures`와 `data/historical_collection/context/dart_candidate_events/`에 저장한다. 사건일과 공시 접수일을 분리해 미래 공시를 과거 연구 판단에 섞지 않는다. 목록 API의 접수일은 날짜 단위이므로 같은 날 장중 의사결정에 해당 공시를 사용할 수 있는지는 별도 시각 근거가 확인될 때까지 미확정으로 둔다. DART 목록 검색에 사건 이유가 없을 수 있으며 법적 상폐일·거래정지 사유 최종 확정에는 KRX 공시 대조가 남는다. [OpenDART 공시검색 API](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019001)의 상태 `020` 요청 제한은 중단 상태로 기록하고 재개한다.

선행 3건 검증에서 `008290` 7건, `046070` 7건, `082660` 4건의 DART 목록을 실제 저장했다. `008290`의 [2026-09-08 주권매매거래정지 공시](https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260908900687) 제목에는 상장폐지 사유 발생이, `046070`의 [같은 날 거래정지 해제 공시](https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260908900677)와 `082660`의 [거래정지 해제 공시](https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260908900609) 제목에는 상장폐지에 따른 정리매매 개시가 표시된다. 이는 새 이벤트 조회가 이전 수정계수 DART 수집에서 빠진 공시를 찾았음을 보여 주지만, 제목만으로 CREON 종목코드 거부의 내부 원인을 단정하지 않는다.

일봉 거래량 0 연속 5일 기준은 짧은 정지 공시를 빠뜨릴 수 있다. `scripts/collect_candidate_exchange_disclosures.py`는 같은 2,637개 종목에 대해 [OpenDART 공시검색의 거래소 공시 유형 `I`](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019001)를 2019-09-24부터 2026-09-24까지 종목별 조회했다. 별도 `historical_candidate_exchange_jobs`/`historical_candidate_exchange_disclosures`와 `context/dart_exchange/` 원응답에 저장하며, 우선주 코드는 종목명으로 검증된 동일 발행회사의 보통주 DART 회사번호에만 연결한다. 최종 원장은 완료 2,620종목·공식 빈 응답 17종목, 거래소 공시 332,723건이고 실패·미매핑은 없다. `046070` 2025-01-01~2026-09-24 시험 조회에서는 거래소 공시 37건 중 제목에 거래정지·상장폐지·정리매매가 들어간 13건을 확인했다. 이 집계는 공시 **목록 확보**이며 실제 거래정지·상장폐지 효력일의 자동 분류 완료를 뜻하지 않는다. DART 접수일이 실제 매매정지 효력일과 같다고 가정하지 않는다.

후속 효력일 수집은 `scripts/collect_candidate_exchange_effective_dates.py`가 거래소 제출 거래정지·해제와 상폐 관련 공시의 [OpenDART 원문 ZIP](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019003)을 받아 `historical_exchange_document_fetch`/`historical_exchange_effective_events`에 접수일과 명시된 효력 날짜·시각을 따로 기록한다. 정정 공시의 변경 전 기간, 발행회사의 상폐 신청·결정은 실제 효력일로 승격하지 않는다. 명시 날짜가 없는 공시는 원문과 `parsed_no_explicit_transition` 상태로 보존한다. `scripts/reconcile_candidate_exchange_effective_dates.py`는 키움 일봉의 거래량과 효력일을 대조하되 거래량만으로 장중 정지를 부정하거나 공시 날짜를 덮어쓰지 않는다. `scripts/build_historical_exchange_case_context.py`는 월별 개발 사례의 후보·결과일과 겹치는 공식 사건만 TRAIN/VALIDATION 읽기 전용 동반 자료로 투영한다. 접수 시각을 검증하지 못했으므로 장중 매매 신호·주문 가능성 판단에는 자동 사용하지 않는다. 원문과 대조 원장은 다음 불변 시장 맥락 게시본에 포함하며, 이전 게시본은 그대로 보존한다.

실수집 결과 관련 원문 4,220건 중 효력일 추출 2,692건, 명시 전환일 없음 1,523건, DART 원문 API 상태 `014` 5건이다. 추출된 사건 3,083건 중 접수일과 효력일이 다른 것은 1,562건이고 시각까지 명시된 것은 1,749건이다. [일봉 대조 보고서](../data/research/candidate-exchange-effective-dates-reviewed-20260924.json)의 검토 상태는 상폐 예정일 뒤 동일 코드 거래 1건, 사후 접수 정지 공시 1건, 그 외 탐지된 상충 없음 3,081건이다. `046070`의 2025-10-20 상폐 예정일은 2026-09-10 거래와 양립하지 않으므로 실제 최종 상폐로 확정하지 않는다. `008290`의 2026-09-08 접수 원문에는 09-09 정지·09-21 상폐일, `046070`의 같은 날 접수 원문에는 09-10 재개·09-21 상폐일이 명시돼 접수일과 효력일의 차이를 원문으로 재확인했다. [월별 개발 사례 동반 자료](../data/research/historical-monthly-exchange-case-context-20260924.json)는 봉인 OOS를 제외한 16개 사례에서 겹치는 6개 사건을 보존한다. `no_detected_contradiction`은 공식 효력의 독립 확증이 아니라 현재 일봉에서 반증을 찾지 못했다는 의미다.

갱신한 시장 맥락 DB는 `PRAGMA integrity_check=ok`이고 새 DART ZIP 4,215개 원본 SHA-256과 일치한다. NAS 불변 run `20260924T130303Z-2b6cc96dd69a`로 DB와 전체 원응답 18,449개를 게시했다. NAS 복사본 DB 740,904,960바이트의 SHA-256 `2b6cc96dd69a08658d212091afedd85b4e21a261f93a0aee7a723774918f3f90`은 manifest·`latest.json`과 일치하며 원응답 누락은 0개다. 기존 run은 보존했다. 공급 불가 기존 시장 맥락 6건과 DART 원문 `014` 5건 때문에 게시 manifest의 `coverage_complete=false`는 유지한다.

2026-09-24 NAS 불변 게시: 로컬 시장 맥락 DB의 `PRAGMA integrity_check=ok`와 참조 원응답 14,234개를 확인했다. `X:\kiwoom-monitor\deploy\synology\server-data\historical-market-context\v1\runs\20260924T114555Z-7b48edc19038`의 DB SHA-256 `7b48edc19038a0cc318d82d4ea76528a6903939a16ec22fe2fef44bd0270f4bd`는 manifest와 일치했고 `latest.json`이 이 run을 가리키는 것도 재조회했다. 최초 staging 이름 변경은 SMB `WinError 5`로 실패했으나 복사본 해시 검증 후 같은 staging의 재시도는 성공했다. 실패 당시의 점유 주체는 미확정이다. 이 맥락 DB에는 기존 수집의 공급 불가 6건이 `coverage_complete=false`로 별도 남는다.

`008290`의 PC 키움 백필에는 2025-09-01~2026-08-24 사이 14거래일의 1분봉 3,442행이 별도로 존재한다. 정리매매가 진행된 2026-09-10~18 분봉은 이 PC 파일에 없다. 연구 입력에서 이 종목 전체를 무거래로 처리하거나, 해당 14일을 전 기간 완전한 분봉으로 표시하지 않는다.

## 2-1. 시장 지수·VI·과거 종목 규모 확대 (2026-09-23 기획)

확정한 기본 수집원은 세 갈래다. **대신 StockChart 일봉**에서 후보 종목의 당시 시가총액(필드 13)과 키움 `dstr_stk` 수치에 대응하는 상장주식수(필드 12)를 받고, **대신 StockChart 지수 봉**에서 KOSPI·KOSDAQ 1분·5분·일봉과 거래대금을 받는다. **과거 VI 발동**만 KRX 기록을 조회한다. 시총·주식수 입력 자체에는 DART를 쓰지 않는다. 별도 기업행동 보완은 대신 필드 18·19의 수정주가 발생일·비율을 1차 신호로 삼고 해당 주도후보 종목·사건만 DART 공시로 분류한다. 각 자료의 실제 최과거일·결측·단위는 수집 응답으로 확정한다. **NAS에 `상장주식수` 필드를 새로 만들지 않는다.** 대신 원본의 필드 12와 공급자 정보는 과거 수집 원응답에 남기고, 연구 입력에서는 기존 주식수 필드에 날짜별 값으로 연결한다.

연구의 당시 시장 상태를 복원하기 위해 KOSPI(`U001`)와 KOSDAQ(`U201`) 종합지수를 별도 원장으로 수집한다. 종목 후보 2,945개의 원장과 독립적인 두 지수 작업이며, 실행 중인 종목·뉴스 수집기의 진행 상태를 초기화하거나 우선순위를 빼앗지 않는다. 대신 StockChart는 업종(`U`) 코드를 받으며 날짜·시간·OHLC·거래량·거래대금 필드가 있다. 이 PC의 관리자 권한 32비트 CREON에서 두 코드 모두 1분·5분·일봉 5행씩 실제 응답했고 2019-01-02~10 일봉도 정상 응답했다. 표본 원응답은 `data/historical_collection/daishin-index-probe-20260923-01.ndjson`과 `-02.ndjson`에 있다. 지수 가격은 소수점이고 0거래량·0거래대금 분봉도 있으므로 종목 봉의 정수 가격 파서를 재사용하지 않는다. 거래대금 필드는 원시 수치가 들어오지만 단위·시장 전체 집계 의미는 KRX 대조 전까지 확정하지 않는다. [대신 StockChart 공식 도움말](https://money2.daishin.com/e5/mboard/ptype_basic/HTS_Plus_Helper/DW_Basic_Read_Page.aspx?boardseq=284&m=9508&p=8839&page=1&searchString=StockChart&seq=102&v=8642), [대신 지수 코드 Q&A](https://money2.daishin.com/e5/mboard/ptype_basic/Basic_018/DW_Basic_Read_Page.aspx?boardseq=60&m=9508&p=8827&page=1&searchString=&seq=19891&v=8636)

| 자료 | 목표 구간·저장 의미 | 수집 전 검증 |
|---|---|---|
| KOSPI·KOSDAQ 1분봉 | 최근 2년 또는 공급자 실제 최과거까지, 원본 거래대금과 단위 보존 | 두 코드에서 실제 응답, 지수 가격 소수점, 시간 의미, 첫·마지막 일자 및 거래대금 비영값 확인 |
| KOSPI·KOSDAQ 5분봉 | 1분봉 최과거일 **전날부터** 최근 5년 또는 더 오래 제공되는 최과거까지 | 1분/5분 중복일 제외, 1분 집계와 5분 거래량·거래대금 표본 대조 |
| KOSPI·KOSDAQ 일봉 | 최소 최근 7년 목표, 제공되면 그 이전도 보존. **2019-01-02 실응답 확인** | 원본 거래대금·종가·거래량과 KRX 일별 지수 시세 표본 대조. 분봉 합과 직접 동일하다고 가정하지 않음 |
| 일별 VI 발동 | 후보 종목 중심으로 날짜·종목·유형(동적/정적)·발동/해제시각·거래소를 원본으로 저장 | KRX [변동성완화장치 발동종목 현황](https://data.krx.co.kr/contents/MDC/STAT/issue/MDCSTAT224.jsp)의 과거일 조회 가능 범위·대량 추출 조건·응답 단위 표본 확인 |
| 과거 종목 시총·상장주식수 | 종목·거래일 기준 실제 당시 값. 첫 저장 대상은 기존 후보 종목·일에 한정하고 인접 배경일은 별도 검토 | 대신 일봉 StockChart의 필드 12·13과 KRX [전종목 시세](https://data.krx.co.kr/contents/MDC/MDI/outerLoader/index.cmd?screenId=MDCSTAT015) 비교. 조정주가와 과거 상장주식수를 혼합 산출하지 않음 |
| 과거 유통주식수·비율 | 시총·상장주식수 확보와 독립된 **선택적 후속**. 보고서 기준일과 공개일을 함께 보존한 공시 스냅샷 | 대신 StockChart 필드 12의 상장주식수는 유통주식수가 아님. [OpenDART 주식의 총수 현황](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020002)의 `distb_stock_co`(발행주식수−자기주식수)는 확보 가능하지만, 대주주 등 고정 보유분까지 제외한 시장 유동주식 비율과도 정의가 다름 |

지수는 점수(float)를 정수 종목 가격으로 변환하지 않고, 현재 단일 CREON 공급자 원장에서 `(provider, code, interval_seconds, bar_time)`로 봉을 식별한다. 거래대금은 제공 필드의 실제 단위가 확인될 때까지 원시값과 공급자 단위를 함께 저장하고, 시장 전체 거래대금인지 지수 구성종목 거래대금인지도 표본 대조로 확인한다. 1·5분과 일봉은 시간 해상도가 다르므로 일봉 7년을 분봉 완전성으로 해석하지 않는다.

시총은 일봉의 해당 거래일 값이 있으면 높은 신뢰도로 복원할 수 있으나, 그날 장중 각 분의 시총은 별도 관측이 없으면 알 수 없다. 사용자가 삼성전자·SK하이닉스에서 **키움이 `유통주식수`라고 표시하는 `dstr_stk` 값과 대신 StockChart의 `상장주식수` 값이 같음**을 확인했다. 삼성전자 5,846,279천주는 [삼성전자 IR의 2026-06-30 보통주 5,846,278,608주](https://www.samsung.com/global/ir/stock-information/listing-Info/)와 반올림 범위에서 일치한다. 같은 IR은 자기주식 82,086,705주를 별도로 기록하므로 두 API 값의 일치는 **두 공급자의 수치 호환성**을 보여 주지만 실제 유통주식수와 같다는 근거는 아니다. 사용자 결정에 따라 과거 학습의 기존 주식수 입력에는 대신 필드 12를 날짜별 값으로 연결한다. NAS의 새 `상장주식수` 필드는 만들지 않고 대신 원응답과 출처만 과거 수집 원장에 보존한다. 따라서 **이 학습용 주식수 입력을 위해 DART를 필수 수집원으로 추가하지 않는다.** 기존 앱의 키움 `dstr_stk` 해석과 `price × dstr_stk`로 계산한 유통시총 명칭은 별도 검증 대상으로 남기며 이번 역사 백필에서 조용히 변경하지 않는다. [키움 공식 `ka10001` 예제](https://github.com/Kiwoom-Securities/Kiwoom-REST-API/blob/main/examples/%EA%B5%AD%EB%82%B4%EC%A3%BC%EC%8B%9D/%EC%A2%85%EB%AA%A9%EC%A0%95%EB%B3%B4/get_domestic_stock_info.py)의 요청에는 `stk_cd`만 있고 과거 기준일이 없다. 현재 중앙 서버는 조회한 날의 `stock_fundamentals` 스냅샷을 별도로 남기므로 그 수집 시작 이후의 관측 이력은 재사용할 수 있지만, `ka10001` 재조회로 수집 이전 날짜의 값을 복원할 수는 없다. 공시 유통주식수는 보고서 결산기준일의 값이며, 그 정보가 시장에 알려진 시점은 제출·공개일이다. 과거 연구 입력에는 `effective_on`, `published_at`, `observed_at`, `source_ref`를 분리해 미래에 공개된 수치를 과거 판단에 넣지 않는다. 일별 유동비율은 검증된 과거 공급원이 없으면 채우지 않고 `unavailable`로 둔다. VI 기록은 확보한 2019-01-02~2026-09-22 파일 범위를 기록하고 그 이전 제공 가능 여부는 별도로 확인한다.

실행 순서는 두 지수의 1분·5분·일봉 및 거래대금 소량 표본 → 별도 지수 원장·재개 수집 → KRX VI 과거일 표본 → 후보 종목의 대신 일봉 시총·상장주식수 표본이다. 이 네 기본 항목에는 DART가 필요 없다. DART는 주도후보의 수정주가 사건 유형 분류에만 제한적으로 사용하며, 유통주식수가 실제 연구 입력으로 필요해질 때의 공시 조회는 별도 후속으로 둔다. 현재 실행 중인 종목 봉과 뉴스 수집은 계속 둔다. 각 원본의 범위·누락·단위가 검증된 뒤에만 연구 입력에 연결한다.

2026-09-23 실수집 상태: `scripts/collect_historical_market_context.py`와 `scripts/daishin_market_context_backfill.ps1`을 CREON 관리자 세션에서 실행했다. KOSPI·KOSDAQ 각각 1분봉 194,880행(2024-08-30~2026-09-22), 5분봉 58,464행(2021-08-12~2024-08-29)을 보존했다. 기간 조회가 빠뜨린 2026-09-22 1분봉 각 390행은 별도 최신 건수 조회로 보강했다. 일봉은 KOSPI 12,401행(1980-01-04~2026-09-22), KOSDAQ 7,420행(1997-01-03~2026-09-22)이다. 두 지수의 2026-09-22 일봉 종가는 마지막 1분봉 종가와 일치한다. 과거 KOSPI 일봉 일부의 거래대금은 0이므로 거래대금 완전성은 날짜별로 별도 검사한다.

후보 원장은 거래일 1,895개마다 50종목, 총 94,750 종목·일 관계와 고유 코드 2,945개다. 이는 당시 실시간 Top40 순위 자료가 아니라 사후 선정 후보이며, 시총·주식수는 해당 후보일만 파생 DB에 저장한다. 대신 일봉 조회 원응답에는 후보일 사이 날짜가 포함될 수 있다. 첫 3종목 표본은 후보일 56개를 모두 저장했고 시총이 `종가 × StockChart 필드 12 주식수`와 반올림 오차 내에서 일치했다. 현재 개별 주식 대상 2,637코드 중 `stock_daily`는 2,634개 완료·3개 공급 불가로 원장이 끝났다. 원래 2,945코드 중 비주식 308개는 `excluded`로 보존한다. 상태는 `data/historical_collection/context-stock_daily-state.json`과 `data/historical_market_context.sqlite3`의 `context_jobs`에서 확인한다.

사용자가 받은 KRX VI CSV 8개를 `scripts/import_krx_vi_history.py`로 원본 해시와 함께 적재했다. 2019-01-02~2026-09-22의 477,605원본 행 중 파일 경계 중복 979개를 제거한 476,626개 발동을 저장했고 발동·해제시각, 동적/정적 유형, 가격과 괴리율을 보존한다. 이 8개 파일은 KOSPI·KOSDAQ 1,897거래일을 포함한다. 원본은 `data/historical_collection/krx_vi`에 해시 이름으로 보존한다.

`data/historical_market_context.sqlite3`는 기존 앱의 NAS 실시간 DB 스키마와 분리된다. 수정주가 사건 보완도 같은 분리 DB에 저장한다. 최초 후보 원장의 고유 코드 2,945개를 작업으로 만들었지만 현재는 개별 주식 2,637개만 `stock_adjustment` 대상으로 유지하고 CREON StockChart 필드 0·18·19만 조회한다. 동일 발생일·비율이 여러 일봉에 반복되면 한 사건으로 중복 제거하되 CREON NDJSON 원응답은 그대로 보존한다. 실제 사건마다 발생일 180일 전부터 14일 후까지 해당 기업의 OpenDART 목록 공시를 조회한다. `주식분할`, `주식병합`, `감자`, `무상증자`, `유상증자`, `회사분할/분할합병`, `합병` 제목 근거가 한 유형이면 분류하고, 여러 유형이면 `manual_review`, 없으면 `unmatched`로 남긴다. DART 공시 원행·접수번호·조회창과 분류 근거를 보존하며 전 종목 공시를 수집하지 않는다. 과거 수집기 모니터의 `주도후보 수정주가·DART 기업행동` 패널에서 시작·정지·진행 상태를 확인한다. 실제 CREON 표본에서 필드 18은 각 일봉 날짜, 필드 19는 해당 구간에 적용된 누적 수정계수로 확인됐다. 따라서 100이 아닌 모든 행을 사건으로 보지 않고 시간순 인접 거래일의 계수가 바뀌는 경계만 사건으로 저장한다. 최초 경계 판별을 위해 목표 시작일보다 31일 앞에서 조회하되 저장은 후보 원장의 시작일 이후로 제한하며, 이전 계수·새 계수·변화율을 함께 남긴다. 작은 연말 계수 변화가 실제로 확인되어 DART 분류 유형에는 배당도 포함한다. 모든 처리 가능한 후보 종목 작업이 완료되면 `scripts/watch_historical_market_context_publish.py`가 `scripts/publish_historical_market_context_to_nas.py`를 실행해 DB와 CREON/KRX 원본을 `X:\kiwoom-monitor\deploy\synology\server-data\historical-market-context\v1`에 불변 스냅샷으로 게시한다. 게시 상태는 `data/historical_collection/context-publish-watch.json`에 남긴다. 일시 실패·대기 작업이 남으면 자동 게시하지 않는다. CREON StockChart가 명시적으로 거부한 코드만 `unavailable`로 종결하고 원오류·원응답을 보존하며, 게시 manifest의 `coverage_complete=false`와 상태별 건수로 누락을 공개한다. 연구 입력 연결은 날짜별 누락과 공급자 단위 검증 뒤 별도로 진행한다.

2026-09-23 06:05 KST에 기존 분봉 수집기와 시총·주식수 수집기가 동시에 CREON 연결을 잃었다. 관리자 권한의 32비트 `CpUtil.CpCybos.IsConnect`도 `false`였고 CREON 프로세스가 실행 중이지 않았다. 대신증권 [공식 Plus 안내](https://money2.daishin.com/e5/mboard/ptype_basic/Basic_018/DW_Basic_Read_Page.aspx?boardseq=60&ismy=0&m=9508&p=8827&page=1&searchString=&seq=28254&v=8636)는 06:05 전후 모듈 갱신 때문에 Plus 재시작이 필요하며 사용자 프로그램을 종료한 뒤 Plus에 재접속하라고 설명한다. 두 수집기가 정확히 이 시각에 중단된 사실은 확인됐지만, CREON 프로세스 종료의 내부 트리거는 Windows 이벤트 로그에서 확인되지 않았다. 09:02 로그인 후 `IsConnect=true`를 확인하고 두 작업을 원장에서 재개했다. 다음 06:05 이후에는 먼저 Plus 재접속을 확인한 뒤 수집기를 다시 시작한다.

2026-09-23 재조회 및 수집 범위 확정: 원본 주도후보는 2,945코드지만 사용자 결정에 따라 CREON 종목 분봉·시총/주식수·수정주가 대상은 코스피/코스닥 개별 주식(`stocks.market_code` 0/10) 2,637개다. ETF 184·ETN 119·리츠 5개, 합계 308개는 `excluded`로 분리한다. 이미 받은 비주식 원응답·봉·사건은 삭제하지 않으며 향후 연구 입력에서는 원천 시장 분류로 제외한다. 이전 122개 코드 거부 중 119개는 ETN에 주식 접두사 `A`를 붙인 요청이 원인이었다. 대신 공식 규칙은 ETN 접두사 `Q`이고 CREON 현재 종목명 조회에서 그중 118개가 `Q`로 인식됐다. 그러나 이번 수집 범위가 개별 주식이므로 ETN을 재수집하지 않는다. 나머지 거부 3개(`008290`, `046070`, `082660`)는 주식 `A` 코드로 오늘 다시 요청해도 `유효하지 않은 종목코드`가 재현됐고 현재 CREON 코드 조회에서도 이름이 없다. 왜 현재 목록에 없는지는 별도 확인 대상이다. 추가 실패 17개는 재요청 승인 후에도 1분봉 0건이었고, 그중 16개는 이전 표적 수집에서 과거 5분봉을 확보했다. 현재 개별 주식 기준 분봉 완료 2,617/2,637·실패 20, 1분봉 보유 2,617·5분봉 보유 2,448이다. 시총/주식수와 수정주가 작업은 각각 완료 2,634/2,637·공급 불가 3이다. 앞선 22개 표적 5분봉 재조회는 17개에서 661,048행을 로컬에 추가했으며 원응답은 `data/historical_collection/daishin-retry`에 있다. 발견된 수정주가 사건 DART 분류 작업 3,097건은 완료됐지만 유형 확정 989·수동 검토 984·미매칭 1,124로 나뉜다. 이 총수 중 개별 주식 사건은 2,106건(유형 확정 987·수동 검토 980·미매칭 139), 비주식 사건은 991건이므로 학습 사례에 시장 분류 필터가 필요하다.

재개 직후 기존 분봉 수집기는 `034220`의 최근 1분봉 보완 이후 46GB `market_bars` 범위 집계에 머물렀다. `EXPLAIN QUERY PLAN`에서 기존 `WHERE code=?`가 전체 covering index `SCAN`을 수행하고, `provider='daishin_creon'`을 붙이면 `(provider,code)` `SEARCH`로 바뀌는 것을 확인했다. 종목별 `_ranges` 두 쿼리에 공급자 조건을 추가하고 기존 프로세스를 종료한 뒤 멱등 원장에서 다시 시작했다. 연결 단절 원인과 재개 후 느린 집계 원인은 별개다.
### 투자자별·프로그램 순매수: 과거 학습 수집 보류

2026-09-23 사용자 결정: **개인·외국인·기관 순매수와 프로그램매매를 이번 과거 학습 백필·D07 실행 범위에서 제외한다.** 확인된 소스는 일별 집계 중심이고 프로그램 일별 조회기간은 짧아, 목표인 과거 장중 타점의 시간별 입력으로 적합하지 않다. 기존 앱의 장중 수급·프로그램 조회와 이미 저장된 스냅샷은 그대로 유지한다. 아래는 재검토 시 참고할 조사 기록이며 추가 표본 조회나 수집 작업을 뜻하지 않는다.

개인·외국인·기관의 **종목별 일별** 순매수는 대신 `CpSysDib.InvestorsbyStock`가 종목코드·사용자 지정 시작/종료일·수량/금액·거래소를 받아 제공한다. 전체 투자자 응답에는 개인·외국인·기관계가 같은 날짜 행에 있고, 개별 투자자 요청에는 매도/매수 수량·금액이 있다. 문서상 연속조회는 가능하지만 최과거일과 대량 요청의 실제 반환량은 실측하지 않았다. KRX의 `투자자별 거래실적(개별종목)`도 일별 교차검증 경로다. [대신 공식 도움말](https://money2.daishin.com/e5/mboard/ptype_basic/HTS_Plus_Helper/Basic_Read.aspx?boardseq=294&page=1&searchString=&seq=275), [KRX Data Marketplace](https://data.krx.co.kr/contents/MDC/MAIN/main.jspx)

종목별 **일별 프로그램매매**는 대신 `DsCbo1.CpSvrNew8119Day`가 매도·매수·순매수 수량/금액을 제공하지만 공식 입력 선택지는 최근 5일·1개월·3개월·6개월뿐이고 연속조회가 없다. 따라서 이것만으로 5년치를 소급할 수 없다. `CpSysDib.CpSvr8113`은 종목별 현재 프로그램매매 현황이며 날짜 입력이 없어 과거 백필 수단이 아니다. [대신 일별 추이 도움말](https://money2.daishin.com/e5/mboard/ptype_basic/HTS_Plus_Helper/DW_Basic_Read_Page.aspx?boardseq=284&m=9508&p=8839&page=1&searchString=&seq=263&v=8642), [대신 종목별 현황 도움말](https://money2.daishin.com/e5/mboard/ptype_basic/HTS_Plus_Helper/Basic_Read.aspx?boardseq=294&page=1&searchString=&seq=274)

앱은 현재 `ka10045`로 외국인·기관의 해당일 수급을, `ka90008`로 해당일 종목 프로그램 시간대별 흐름을 조회·스냅샷 저장한다. 개인 수급 원장은 아직 없고 기존 스냅샷만으로 수집 전 날짜를 복원할 수 없다. 1분·5분 가격봉의 과거 제공기간을 투자자별/프로그램 수급에도 그대로 적용하지 않는다. 일별 최종치를 분봉에 나누어 과거 장중 값으로 채우지 않는다.

## 3. 네이버 증권 사이트 뉴스

목표 경로는 네이버 개발자 뉴스 검색 API가 아닌 **네이버 웹사이트가 쓰는 응답**이다. 이전 대화와 외부 `backfill_news.py`를 다시 확인해 날짜 지정 검색 경로 `s.search.naver.com/p/newssearch/3/api/tab/more`를 복원했다. 2020-01-02 써니전자 한 페이지에서 구조화 기사 10건을 다시 읽어 별도 표본 DB에 저장했다. 검색 날짜는 `published_precision=date`로 보존하며 정확한 시각처럼 자정으로 바꾸지 않는다.

현재 네이버 증권 종목 페이지의 `/api/domestic/detail/news`도 별도로 확인했다. 최신 목록은 기사 ID·매체·분 단위 발행시각·요약·관련기사 묶음을 제공하지만, 삼성전자 기준 20개씩 100페이지까지만 응답하고 101페이지부터 HTTP 400이며 100페이지도 2026-09-16까지만 도달했다. 따라서 이 최신 목록만으로 2년·5년 백필을 구성하지 않는다.

기존 외부 `backfill_news.py`는 사이트 속보·공지·해외뉴스와 일반 네이버 웹뉴스 검색을 함께 사용한다. 종목 웹검색이 곧 증권 종목별 목록은 아니다. 작업 상태·기사 ID 중복 제거·원응답 보존 기반은 재사용 검토한다.

현재 표본 수집기는 날짜 지정 검색 뒤 언론사 원문에서 JSON-LD `datePublished`, `article:published_time`, `<time>`과 화면의 `data-date-time`을 읽어 `published_at`을 보강한다. `published_at_source`, 원문 문자열, 실제 확인 URL, 확인 상태·시각도 함께 저장한다. 날짜만 확인되면 `published_precision=date`를 유지하며 자정으로 만들지 않는다. 2020-01-02 써니전자 10건 표본에서는 5건의 초 단위 원문 시각을 찾았고, 나머지는 차단 2건·시각 없음 2건·기사 없음 1건으로 상태를 남겼다.

현재 앱과 네이버 검색 API의 정규 필드명은 `published_at`이며 API 원응답의 `pubDate`를 여기에 변환한다. 장기 스키마에서는 원문 게시시각인 `publisher_published_at`, 유통시각, 최초 관측시각을 더 명확히 분리할 수 있도록 이번 표본의 출처 필드를 보존한다. 언론사 원문이 403/로봇 확인이면 우회하지 않고, 제목이 검증된 네이버 기사 링크만 대체 경로로 사용한다. HTTP 200이어도 삭제 안내 페이지만 있으면 `article_unavailable`로 구분한다. 원문 BODY 단계에서는 본문과 시각을 한 번에 읽어 기사당 중복 요청을 만들지 않는 방향을 유지한다.

목록에서 찾은 기사와 본문을 확보한 기사를 구분한다. `description`이 없다는 이유만으로 사이트 목록의 기사 메타데이터를 버리지 않는다. 기존 실시간 검색 API의 필터를 다른 소스에 그대로 적용하지 않는다.

기사 ID/URL, 매체, 제목, 발행/수정/수집시각, 시각 정밀도, 본문 상태, 원문 링크를 보존한다. 기사-종목/후보/테마 관계는 별도로 연결해 한 기사가 여러 종목에 나온 횟수를 독립 사건 수로 세지 않는다. 당시 상호는 종목코드·상호변경 근거로 연결한다.

NAS 참조 DB의 `stock_aliases`는 종목코드별 상호와 `valid_from`/`valid_to`, KIND 근거 URL을 가진다. 후보일에는 유효한 당시 상호를 기본 검색어로 사용하고, 이전 대화에서 정한 상호변경일 앞뒤 14일에는 구·신 이름을 모두 작업으로 만든다. 기본 이름과 경계 보완 이름은 `name_source`로 구분하고 KIND `source_ref`를 보존한다. 현재 94,750개 후보 종목·일에서 기본 작업 94,750개와 경계 보완 340개, 합계 95,090개가 생성됐다.

원문 조회는 언론사 원문과 제목이 검증된 네이버 보관 링크의 시도를 각각 기록한다. `published_at_found`만 현재 학습 적격이며 `time_not_found`, `blocked`, `article_unavailable`, `title_mismatch`, `fetch_error`, `not_fetched`는 원자료를 버리지 않고 학습 제외 사유와 함께 남긴다. 원문이 없거나 시각이 없는 건은 별도 집계할 수 있다.

발행시각이 검증된 기사는 기존 인증 API의 `news_article` collection으로 증분 반영한다. NAS는 이를 `collector_id=naver_historical_web`, `collection_scope=historical_backfill` revision으로 저장하고 기존 BODY 작업기로 원문을 읽는다. 따라서 별도 수집 DB는 원응답·실패·재개·감사 원장이고, 앱이 읽는 기사와 원문 상태는 기존 NAS 뉴스 revision DB에 들어간다. 같은 기사·종목 재전송은 기존 content hash 경계에서 중복 revision을 만들지 않는다. 발행시각 미확인 자료는 수집 DB에는 보존하지만 이 운영 반영 대상에서 제외한다.

2026-09-24 PC 전처리 검토: 로컬 과거 뉴스 DB에는 본문이 아니라 기사 메타데이터·발행시각 확인 결과만 있다. `nas_news_imports`에는 완료 기사-종목 연결 399,010건과 고유 기사 276,485건이 있었으며, NAS 로그 09:00~09:03 KST 표본의 처리량은 BODY 분당 11~18건·RULE 분당 7~9건이었다. PC 최근 기사 5건의 본문 추출은 0.16~0.47초였지만 전 기간·언론사의 처리량이나 차단률로 일반화하지 않는다. 기존 PC 발행시각 확인기는 원문 HTML을 이미 읽으므로 신규 수집에서는 같은 응답으로 본문까지 추출·보존하면 중복 요청을 줄일 수 있다. 기수집분은 별도 본문 재조회가 필요하다. PC가 본문·원문 게시시각·버전이 명시된 일반 분류와 수주 규칙 결과를 준비하는 방향을 검토한다. NAS 운영 PostgreSQL이나 공유 SQLite에 직접 쓰지 않고, 기사 `(stock_code, identity, content_hash)`와 처리 버전을 확인하는 인증 일괄 API가 기존 기사/본문/판정 revision과 BODY/RULE 작업 완료를 한 트랜잭션에서 기록해야 한다. NAS가 이미 실행 중인 작업은 경합 없이 건너뛰고, 동일 결과 재전송은 중복 revision을 만들지 않아야 한다. 기존 NAS 실시간 뉴스 처리는 유지하며 과거 기사 소량 표본에서 본문·게시시각·일반 분류·핵심문장·수주 판정을 현행 NAS 처리와 대조한 뒤 확대한다. 검토 당시 이 경로는 구현 전이었으며 아래 2026-09-24 구현 갱신을 따른다.

Npay 공식 도움말에는 서비스 화면 밖 개인 프로그램에서 증권정보를 재가공해 이용하는 것을 제한하는 안내가 있다. 뉴스 목록·본문의 보관과 학습 이용에 해당하는 범위를 확인해 수집 방식에 반영한다. 사이트 접근과 이용 범위는 별도 확인 항목이며 이 문서는 해당 이용 가능성을 확정하지 않는다. [공식 이용 안내](https://help.pay.naver.com/faq/content.help?faqId=17106)

## 표본 실행 위치

수집 결과는 앱 원본 DB와 분리한 `data/historical_intelligence.sqlite3`에 저장한다. 이 파일은 원응답, 기사 메타데이터·종목 연결·원문 확인 시도, 공급자·주기·거래소·세션·수정기준을 가진 봉, 재개 가능한 뉴스 작업을 분리해 보존한다.

```powershell
.\.venv\Scripts\python.exe scripts\probe_historical_backfill.py candidates
.\.venv\Scripts\python.exe scripts\probe_historical_backfill.py news-seed --database <참조DB> --name-transition-days 14 --reset-jobs
.\.venv\Scripts\python.exe scripts\probe_historical_backfill.py naver-history 004770 "써니전자" 2020-01-02 --pages 1
.\.venv\Scripts\python.exe scripts\probe_historical_backfill.py daishin-sample 005930 --interval 1 --count 20
```

마지막 명령은 CREON Plus 로그인과 같은 Windows 권한 수준에서 실행한다. 이 PC에서는 CREON이 관리자 권한이므로 실행 터미널도 관리자 권한이어야 한다. 삼성전자 연속조회와 해상도 경계는 확인했으며, 전체 후보 확대 전에는 시세 작업 재개 원장과 종목별 완전성 보고를 추가한다.

닫힌 SQLite 스냅샷은 `scripts/publish_historical_intelligence_to_nas.py`로 `X:\kiwoom-monitor\deploy\synology\server-data\historical-intelligence\v1` 아래에 게시한다. 종목 CREON 원응답 `daishin`·`daishin-retry`는 메인 DB 게시가 끝난 뒤 `scripts/publish_historical_daishin_raw_to_nas.py`로 `historical-intelligence\daishin-raw-v1`에 별도 게시한다. 원응답 매니페스트는 연결된 메인 DB run, 파일별 크기·SHA-256을 기록하고, NAS 복사본을 다시 읽어 대조한 뒤 `latest.json`을 바꾼다. 각 run은 불변 디렉터리이고 `latest.json`만 새 run을 가리킨다. SQLite는 NAS 공유에서 직접 갱신하거나 검증하지 않고 로컬 snapshot/검증 복사본을 사용한다.

진행률은 같은 NAS 경로의 `STATUS.md`에서 확인한다. 종목별 뉴스 완료/대기/실패·목록 기사·발행시각 상태·운영 뉴스 DB 반영 건수, 네이버 증권 FLASH/WORLD의 날짜·원천 진행률과 현재 페이지·기사 수, 대신 종목 작업·주기별 봉 수를 분리해 표시한다. `status.json`은 같은 내용의 기계 판독본이다. 종목별 뉴스와 대신 수집기의 기존 상태 게시도 별도 시황뉴스 DB를 함께 읽으며, 시황뉴스 수집기는 날짜 하나가 끝날 때마다 문서를 갱신한다. 묶음 종료 시 새 DB 스냅샷을 게시한다.

로컬 프로세스와 NAS 게시 상태는 프로젝트 루트의 `수집기_모니터.cmd`에서 함께 확인한다. 모니터는 관리자 권한으로 열리며 뉴스·대신의 작업 원장 진행률, 현재 작업, heartbeat, 최근 오류와 NAS 상태 갱신 시각을 표시하고 정지한 수집기를 재시작한다. CLI 확인은 일반 PowerShell의 `./scripts/show_historical_collectors.ps1`을 유지한다. 뉴스 수집은 전체 원장을 한 실행에서 소진하도록 `./scripts/run_historical_news_collector.ps1 -Jobs 100000`, 대신 수집은 CREON과 같은 관리자 PowerShell에서 `./scripts/run_daishin_collector.ps1 -Jobs 10000`으로 직접 시작할 수도 있다. `Jobs`는 공급자 조회 건수 제한이 아니라 한 프로세스가 작업 원장에서 처리할 최대 종목 수이며, 실제 대기 작업이 먼저 소진되면 즉시 끝난다.

뉴스 검색의 네트워크 세션 자체가 사용할 수 없으면 해당 작업을 `pending`으로 되돌리고 시도 횟수를 소비하지 않은 채 수집기를 실패 종료한다. 이 경우 프로세스가 살아 있는 것처럼 실패 작업을 계속 넘기지 않는다. 로컬 생존 상태는 작업마다 갱신하고, 운영 뉴스 증분 반영은 기본 10작업마다 수행한다. NAS 진행률은 기본 100작업마다 게시하되 운영 중에는 작업 원장만 빠르게 집계하고 17GB대 기사·원문시도 전체 집계는 최종 스냅샷에서 수행한다. `STOP_NEWS`를 이용한 정상 중지는 빠른 마지막 상태만 게시하고 대형 불변 스냅샷을 만들지 않는다.

검색 페이지는 시작 간격 0.7초를 공유하는 4개 작업자로 응답 대기를 겹치며, 원문 확인은 같은 언론사 도메인에 한 요청만 허용하는 16개 작업자로 수행한다. 0.5초 간격에서는 시간당 반복된 403의 60초 제한 대기가 이론상 추가 요청량보다 커 실제 처리량이 낮아졌다. 원문 대기열은 같은 언론사 기사가 연속돼도 대기 항목이 작업자 스레드를 차지하지 않으며, 서로 다른 언론사는 즉시 병렬 처리한다. 검색과 원문 처리는 한 작업 안에서도 파이프라인으로 진행한다. 검색 또는 원문 결과와 SQLite 저장은 메인 흐름에서 순서대로 확정해 재시작 원장과 중복 제거 계약을 유지한다. 검색 HTTP 403/429가 발생하면 모든 검색 작업자가 60초 제한 대기를 공유하고 막힌 페이지만 최대 10회 재시도한다. 제한 상태와 페이지는 heartbeat에 기록한다. 연속 제한이 그 횟수를 넘을 때만 작업을 `pending`으로 반환하고 래퍼가 다시 시작하므로, 일시 제한 때문에 이미 성공한 수십 페이지를 처음부터 다시 요청하지 않는다.

후보 전체 대신 수집기는 1분봉을 공급자가 주는 최과거까지 먼저 저장한 뒤 그 최과거일의 전날을 5분봉 `ToDate`로 전달한다. 따라서 5분봉은 처음부터 1분봉보다 오래된 구간만 내려받는다. DB import의 `before_date` 경계도 유지해 공급자 응답 이상이나 경계 오차가 있어도 한 거래일에 두 해상도가 섞이지 않게 한다.

대신 작업 원장이 이미 만들어진 재시작에서는 기존 봉 전체를 다시 `GROUP BY`하지 않는다. 새로 원장에 들어온 종목이 있을 때만 해당 코드의 기존 1분·5분봉을 확인해 과거 수집 결과를 승계한다. 중단된 작업의 부분 봉은 양쪽 해상도가 있더라도 기존 원장 상태를 기준으로 다시 실행한다.

일별 검색의 마지막 부분 페이지와 프로세스 시작 비용을 줄이기 위해 밀도 50% 이상인 같은 종목·검색어·월의 대기 일자를 날짜 범위 작업으로 묶는다. 완료된 일별 표본의 평균 페이지 수로 묶음 크기를 산정해 예상 80페이지 이하로 제한하고, 표본이 없으면 일 2페이지를 가정한다. 평균 50페이지 이상인 대량 조합은 범위 재검색 비용이 더 크므로 일별 작업을 유지한다. 실제 응답이 100페이지 상한에 닿으면 해당 범위를 날짜로 이분해 재개한다. 기존 일별 작업은 삭제하지 않고 `grouped` 상태와 범위 구성원을 보존한다. 검색 결과 이미지 경로의 원 게시일 또는 원문에서 확인한 `published_at`이 범위 안에 있을 때만 원래 날짜 작업에 귀속하며, 날짜를 확인하지 못한 기사를 임의로 범위 시작일에 넣지 않는다. 따라서 묶음 검색은 요청 효율만 바꾸고 기사·종목·후보일 관계와 학습 적격 기준은 바꾸지 않는다.

## 4. 수집 상태와 완료 기준

표본 단계에서 필요한 최소 상태 기록은 다음과 같다. 기존 작업 원장이 있으면 확장하고 동일한 기능의 원장을 중복 생성하지 않는다.

| 기록 | 필요한 의미 |
|---|---|
| 요청 | 공급자·대상·구간·주기·세션·수정기준·설정 버전 |
| 실제 확보 | 첫/끝 시각·행수·페이지/연속조회 위치·중복·자료 없음의 근거 |
| 품질 | 부분 구간·오류·정정·시각 정밀도·본문 확보·조회 한도/절단 |
| 재개 | 마지막 확정 체크포인트·재시도 횟수·오류 사유 |
| 결과 | 처리 완료 / 자료 없음 / 부분 확보 / 확보 확인 / 오류 |

HTTP 성공·작업 `done`·구간 최소/최대 날짜만으로 완전 확보를 선언하지 않는다. 거래 없는 구간과 조회 실패는 별도 근거로 구분한다. 재시작해 같은 구간을 다시 읽어도 행수와 학습 표본이 중복되지 않아야 한다.

첫 산출물은 후보별 확보 현황과 소수의 시세·뉴스 연결 사례다. 이를 보고 종목 수·날짜 범위·요청량·저장량·예상 시간을 산정한다. 전체 범위 종료 시 부족한 구간도 목록에 남긴다.

## 5. 기존 연구와 LLM 입력 연결

```text
후보 날짜·종목·선정 이유
  + 원천별 봉·실제 관측
  + 기사·사건·테마 근거
  → 입력 시각·해상도·모집단·결과 기간을 명시한 사례
  → LLM 해석/사례 검색/학습 예제
  → 기존 연구 엔진의 정책 비교·평가
```

실제 수신 기록의 재생과 지금 모은 과거 자료의 복원을 구분한다. `available_at`은 실제 수집·분석 시각으로 보존하고 과거 발행/봉 시각으로 조작하지 않는다. 과거 복원 연구에는 별도의 가정·입력 범위·버전을 둔다.

사후 일봉 후보에는 원래 장중 선정시각이 없다. 해당 거래일의 일봉이 완성된 뒤 계산한 장마감 후보군으로 정의하고 당시 아침 Top20 입력처럼 넣지 않는다. 후보일 당일 장중 가격·VI·뉴스는 후보를 설명하는 사후 분석에만 쓰며, 실전 입력을 가정하는 연구에서는 후보일 장마감까지의 정보로 후보군을 확정하고 바로 다음 거래일의 분봉만 타점·결과 구간으로 사용한다. 순위가 필요한 기존 전략은 당시 실시간 순위 자료가 없으면 미지원이며 가짜 순위를 만들어 통과시키지 않는다. 5분 사례는 지원하는 정책·관찰 지평으로 제한한다.

2026-09-24 키움 수정 일봉 재조회는 개별 주식 주도후보 2,637코드 모두 완료됐다. 수집기는 각 코드 저장 직후 NAS 일봉을 다시 조회해 로컬 후보 DB의 2019-09-24 이후 기대 거래일과 비교했고, 최종 원장의 2,637개 `missing_after`는 모두 0이다. [완료 원장](../data/historical_collection/kiwoom_candidate_daily_7y_status.json)은 16:38 KST `phase=complete`다. [후보 연구 창의 기업행동 날짜 보고서](../data/research/needed_candidate_events_20260924.json)는 개별 주식 894종목의 CREON/DART 알려진 사건 1,447건을 추렸다(분류 682·수동 검토 667·미매칭 98). 이 보고서의 창은 후보일 이전 249거래일부터 다음 일봉까지이며, 키움 고유 사건 누락 가능성이 있어 완전한 기업행동 원장으로 간주하지 않는다.

후보 안의 실패·무반응·미거래도 보존하고 비후보를 포함한 전체 시장 수익성으로 일반화하지 않는다. 학습 입력과 이후 결과를 분리하고 사건·기간 중복을 고려한 시간순 검증 및 미사용 평가 구간을 유지한다.

사례 연결이 완료돼도 모델 가중치 학습 완료는 아니다. 원문 확보 → 품질 검토 → 사례 구성 → 학습/검색 방식 선택 → 평가 순서로 진행한다. 타점 세부 규칙은 이 자료와 사용자의 학습을 바탕으로 이후 조정한다.

### D06 첫 LLM 학습 준비 사례 계약

`scripts/prepare_historical_learning_cases.py`는 검증된 `historical_reconstruction/v1`을 `historical_learning_cases/v1` 불변 묶음으로 변환한다. 한 사례 안에서도 사후 후보 선정은 `sample_selection`, 발행시각이 검증된 기사 제목·검색 요약은 `model_input`, 아직 실행하지 않은 LLM 해석은 `interpretation.status=not_generated`, 다음 거래일 봉에서 계산한 가격 변화는 `outcome_label`로 분리한다. 결과 라벨은 모델 입력이 아니며, 기사 `published_at`을 실제 수집 `available_at`으로 바꾸지 않는다.

이 계약은 과거 당시의 strict 재생 입력이 아니다. 후보는 장중 실시간 순위 기록이 아니라 완성된 일봉으로 사후 계산한 장마감 후보군이고 뉴스도 사후 수집했으므로 `strict_point_in_time_case=false`, `strict_backtest_input=false`다. 장중 선정시각을 추정해 만들지 않으며, 다음 거래일 연구에서만 후보일 장마감을 명시적 선택 기준시각으로 사용한다. 원문 전체가 아니라 제목과 검색 요약만 있는 현재 범위도 `content_scope`에 명시한다. 모델 호출이나 가중치 학습은 수행하지 않으며 `model_weight_training_ready=false`를 유지한다.

최종 첫 불변 표본 `data/research/historical-learning-cases/2026-09-18-v2`는 후보 50사례를 보존한다. 발행시각 검증과 후보일 이전 조건을 통과한 뉴스 근거가 있는 사례는 22개, 다음 거래일 결과 봉이 있는 사례는 2개, 결과 공백은 48개다. 원문 차단 1,295관계, 시각 없음 74관계, 후보일 뒤 발행 21관계 등은 입력에서 제외하고 이유별 개수로 남겼다. 기사 의미 관련성은 아직 사람 검토 전이며 `semantic_relevance_reviewed=false`다. 이는 사례 파이프라인과 누락 처리를 검증한 표본이며 RAG 품질, 미세조정 준비 완료, 전략 성과를 뜻하지 않는다.
### D06 기사 의미 검토 대기열

`scripts/prepare_historical_news_review_queue.py`는 학습 준비 사례의 기사 관계를 `historical_news_review_queue/v1`로 변환한다. 공급자·언론사·기사 식별자가 같은 기사는 한 검토 항목으로 합치되 연결된 사례·종목·검색어·수집 revision은 각 관계에 남긴다. 기존 `assess_stock_news` 결과는 사람이 먼저 볼 순서를 위한 `rule_hint`이며 의미 관련성 정답, LLM 판정, 테마 확정으로 취급하지 않는다.

첫 대기열 `data/research/historical-news-review-queues/2026-09-18-v1`은 2,035개 종목-기사 관계를 1,422개 고유 기사로 묶어 613개의 반복 검토를 줄였다. 종목별 규칙 힌트 중 하나라도 관련으로 나온 기사는 105개다. 전체 항목은 아직 `pending`이고 `rule_hint_is_ground_truth=false`, `llm_used=false`, `human_review_complete=false`, `model_weight_training_ready=false`다. 같은 파일은 NAS `server-data/historical-intelligence/v1/news-review-queues/historical-news-review-queue-578f89c206d709720184aa753649dc3840f949a066819b476fc27ce576311805`에도 불변 게시했다. 사람 판정이 입력된 항목만 별도 불변 결과가 되며, 이후 사건 ID를 기준으로 같은 사건이 학습과 평가에 동시에 들어가지 않게 분할한다.

`scripts/historical_news_review_workflow.py export`는 우선순위 기사 또는 전체 기사를 UTF-8 CSV로 내보내며 기존 파일을 덮어쓰지 않는다. `import`는 대기열 ID·hash·순번·항목 ID를 검증하고 완료된 행만 `historical_news_review_decisions/v1`으로 동결한다. 허용 판정은 `relevant`, `not_relevant`, `uncertain`이다. 관련 판정은 `canonical_event_id`가 필수이고 테마명이 있으면 `theme_profile_name`도 필요하다. 무관·보류에는 사건·테마를 붙이지 않는다. 일부만 검토한 결과는 `partial_review_result=true`이고 모든 결과는 `model_weight_training_ready=false`다.

첫 편집 작업본 `data/research/historical-news-review-work/2026-09-18-priority-v1.csv`에는 규칙상 우선 검토 105행이 들어 있다. 현재 사람 판정은 0건이라 불변 결정 결과는 아직 만들지 않았다. 실제 판정 뒤 사건 ID를 기준으로 같은 사건이 학습과 평가에 동시에 들어가지 않게 분할한다.

앱의 전략 연구 창 `과거 뉴스 검토`는 최신 불변 대기열과 결합된 작업표를 자동 선택한다. 기사 제목·검색 요약·원문 URL·연결 종목과 규칙 점수를 함께 표시하고, 판정 행을 임시 파일 교체 방식으로 저장한다. 관련 판정은 사건 ID를 자동 제안하되 사용자가 기존 사건 ID를 선택하거나 수정할 수 있고, 테마명은 현재 테마 저장소의 프로필 이름과 함께 기록한다. 현재 목록에서 사라진 과거 프로필 이름도 작업표에서 지우지 않는다.

`scripts/plan_historical_news_event_split.py`는 불변 사람 판정 결과에서 관련 기사만 골라 `historical_news_event_split/v1`을 만든다. 같은 `canonical_event_id`의 여러 기사는 최초 발행시각을 기준으로 하나의 사건이 되며 TRAIN·VALIDATION·OOS 사이에서 나뉘지 않는다. 시간대 표기가 달라도 timezone-aware 실제 시각으로 정렬한다. 무관·보류는 입력 정답으로 섞지 않고 제외 수량으로 남긴다. 일부만 검토한 원천으로도 계획 구조는 검증할 수 있지만 `partial_review_source=true`, `model_weight_training_ready=false`를 유지하며 OOS 검토 payload를 계획에 넣지 않는다.

같은 기능은 과거 뉴스 검토 화면의 `사건 분할 계획`에서 실행한다. 최신 불변 판정에 관련 사건이 3개 이상일 때만 TRAIN과 VALIDATION 사건 수를 입력받고, 선택 가능 상한을 조정해 OOS에 최소 1개 사건이 반드시 남게 한다. 계획은 `data/research/historical-news-event-splits/<plan_id>.json`에 불변 저장한다.

`scripts/prepare_historical_news_development_inputs.py`는 결정 dataset ID·파일 hash와 사건 분할 plan ID가 정확히 맞을 때만 `historical_news_development_inputs/v1` 디렉터리를 만든다. `train.jsonl`과 `validation.jsonl`에는 제목·검색 요약·검증 발행시각·연결 사례를 model input으로, 사람 확정 관련성·사건 ID·테마 프로필과 테마명을 target으로 분리한다. OOS 기사와 target은 어떤 출력 파일에도 넣지 않으며 TRAIN과 VALIDATION에 같은 사건 ID가 있으면 거절한다. 이 묶음도 RAG나 미세조정이 실행됐다는 뜻이 아니므로 `llm_used=false`, `model_weight_training_ready=false`를 유지한다.

같은 생성은 과거 뉴스 검토 화면의 `개발 입력 생성`에서 실행할 수 있다. 화면은 최신 불변 사람 판정에 dataset ID와 결정 파일 hash가 일치하는 사건 분할만 후보로 삼고, 일치하는 계획 중 가장 최근 저장본을 사용한다.

`scripts/prepare_historical_news_blind_validation.py`는 개발 입력의 VALIDATION을 모델 실행용 `historical_news_blind_validation/v1` 요청으로 투영한다. `requests.jsonl`에는 `sample_id`와 `model_input`만 있고 `human_target`, 관련성, canonical event ID, 테마 프로필·테마명은 포함할 수 없다. RAG와 미세조정 실행은 이 같은 요청 집합 ID를 사용하고, 채점 단계만 별도로 원본 validation target을 읽는다. 이 요청은 학습 자료가 아니므로 `training_allowed=false`다.

외부 방법의 JSONL 예측은 `scripts/prepare_historical_news_method_results.py`가 `historical_news_method_results/v1`으로 저장한다. 블라인드 요청의 모든 `sample_id`가 정확히 한 번 있어야 하며, 방법 종류·공급자·모델·구현 버전·RAG index 또는 미세조정 artifact ID를 결과 hash에 결합한다. `scripts/evaluate_historical_news_method.py`만 개발 입력의 VALIDATION target을 다시 읽어 사건 pairwise 군집, 정규화 테마 집합, 프로필 정확도, 기권 포함 coverage를 채점한다. 사건 ID 문자열 자체는 방법별 임의 표기이므로 직접 비교하지 않는다. 현재 입력은 관련 기사만 포함하므로 관련성 분류 평가는 별도 음성 표본 계약 전까지 지원하지 않는다.

### D03 첫 입력 계약

`scripts/export_historical_reconstruction.py`는 선택한 후보일의 후보·현재 확보된 대신 봉·뉴스 근거를 `historical_reconstruction/v1` 불변 디렉터리로 만든다. `manifest.json`과 `records.jsonl`의 hash·ordinal·revision ID를 `load_historical_reconstruction`이 다시 검증한다. 이 형식은 기존 `top20_membership` 입력으로 위장하지 않으며 `strict_top20_replay_supported=false`를 필수로 둔다.

종목·일별 해상도는 실제 저장 행을 기준으로 1분 우선, 없으면 5분, 둘 다 없으면 `unavailable`이다. 한 사례에 1분과 5분을 중복 입력하지 않고 5분 내부 경로를 합성하지 않는다. 뉴스는 검증된 발행시각뿐 아니라 원문 차단·시각 미확인 같은 상태도 관계 근거로 보존하되 `publication_time_verified`와 학습 제외 사유를 유지한다.

### 해상도별 학습 계약

백필은 최근 약 2년의 1분봉, 그 이전을 포함한 약 5년의 5분봉, 약 7년의 일봉으로 가용 범위가 다르다. 이 세 구간을 같은 빈도의 연속 시계열로 취급하지 않는다. 모든 사례·특징·결과 라벨에는 실제 `interval_seconds`, 원천 해상도, 원천 직접 수집인지 하위 해상도 집계인지, 해당 종목의 실제 최초·최종 시각을 남긴다. 날짜만 보고 해상도를 추정하지 않으며 신규 상장·공급자 공백은 별도 coverage로 둔다.

- **1분 학습 뷰**: 실제 1분봉이 완전한 최근 구간만 사용한다. 진입·이탈·재진입·장중 최대상승/하락처럼 정확한 타점 정책은 이 뷰에서만 비교한다.
- **5분 학습 뷰**: 약 5년의 거친 장중 구조·뉴스 반응·거래대금 확산을 본다. 최근 1분 구간을 장기 5분 비교에 포함할 때는 정규 5분 창으로 명시적으로 집계하고 `aggregated_from_60s`를 남긴다. 공급자 5분봉과 집계 5분봉의 OHLCV·거래대금 경계를 검증한다.
- **일봉 학습 뷰**: 약 7년의 시장 국면·중기 지속성·테마 반복·후보 장마감 특징에만 사용한다. 일봉으로 장중 체결 순서나 분봉 타점을 만들지 않는다.

`다음 N개 봉`처럼 해상도에 따라 실제 시간이 달라지는 라벨은 공통 학습 라벨로 쓰지 않는다. 5분·30분·장마감·다음 거래일처럼 실제 시간 지평을 고정하고, 필요한 지평을 해당 해상도로 관측할 수 없으면 `censored_by_resolution`로 남긴다. 학습·검증 지표는 1분/5분/일봉별로 분리해 보고하며 해상도가 섞인 단일 성과값으로 정책을 채택하지 않는다. 현재 `historical_reconstruction/v1`은 1분/5분 원천 구분과 5분→1분 확장 금지를 이미 보존하지만, 일봉 맥락과 공통 5분 집계 뷰는 후속 입력 계약으로 연결해야 한다.

### 거래비용 학습 계약

현재 사용자 합의 기본값은 체결금액별 매수 총비용 0.015%, 매도 총비용 0.215%다. 매도 총비용은 수수료 0.015%와 매도세 0.200%를 합친 값이므로, 세금을 0.215%로 다시 더하지 않는다. 같은 금액을 한 번 매수하고 한 번 매도하면 슬리피지를 제외한 왕복 비용은 약 0.230%다. 분할 체결은 각 실제 체결금액에 같은 비율을 적용하고, 재매수로 회전금액이 늘어난 부분만 비용을 추가한다.

현재 매매일지 예상비용은 이 매수·매도 총비용을 그대로 표현한다. 연구 실행에도 `fixed_bps/v2` 소수 bp 계약을 추가해 수수료 1.5bp를 매수·매도에 각각, 매도세 20bp를 매도에만 적용한다. 소수 bp는 JSON 문자열로 고정하고 기존 `fixed_bps/v1` 정수 요청은 같은 의미로 계속 읽는다. 기존 구조 검증용 역사 요청의 `commission_bps=1`, `sell_tax_bps=18`은 합의 비용값이 아니며 성과 비교에 재사용하지 않는다. 새 입력으로 요청을 다시 생성해야 한다.

2026-09-22 첫 최종 로컬 표본 `2026-09-18-initial-v2`는 2026-09-18 후보 50개 중 당시 현재 수집이 끝난 2개 종목의 1분봉 762행, 뉴스 관계 3,497행을 포함했다. 뉴스 관계는 발행시각 확인 2,056개, 원문 차단 1,295개, 시각 없음 74개와 그 밖의 실패 상태를 manifest에 분리했다. 나머지 48개 종목·일은 빈 봉을 만들어 채우지 않고 미확보로 집계했다. 수집이 계속되므로 이 표본은 불변으로 두고, 더 넓은 자료가 필요하면 새 dataset ID의 export를 만든다.

후속 어댑터는 후보일 장 마감 뒤의 날짜만 전략 결과 구간으로 인정한다. `2026-09-18-through-2026-09-21-v1`에서 파생한 `historical-strategy-input/2026-09-18-v1`은 후보 50개와 000150·005930의 2026-09-21 정규장 1분봉 각 381개를 포함한다. 원자료 `available_at`은 payload에 보존하고, 재생 순서에만 명시적인 `historical_bar_close` clock을 사용한다. 5분봉과 진행 중·실패 작업의 봉은 기존 1분 전략 입력에서 제외한다.

```powershell
.\.venv\Scripts\python.exe scripts\export_historical_reconstruction.py `
  --candidate-database <후보DB> --intelligence-database data\historical_intelligence.sqlite3 `
  --dates 2026-09-18 --outcome-end-date 2026-09-21 --output <복원출력>
.\.venv\Scripts\python.exe scripts\prepare_historical_research_input.py `
  --source <복원출력> --date 2026-09-18 --output <연구입력>
```

기존 runner는 `historical_candidate_population`을 명시적인 복원 입력에서만 허용한다. 이 모집단은 20개로 자르지 않으며 `top20_membership`으로 이름을 바꾸지 않는다. 당시 순위 연속성이 없으므로 rank persistence를 켠 설정은 실행 전에 거부한다. 가격 Factor·Paper 실행 계약은 재사용하지만, 실제 정책 비교는 수집 범위와 평가 분할·비용 가정을 고정한 뒤 수행한다.

### D03 다기간 입력

복수 후보일 export는 각 후보일 뒤 후보 DB에서 확인되는 바로 다음 거래일까지만 해당 사례의 결과 구간으로 쓴다. 다음 선택일이 수개월 뒤라는 이유로 그 사이 전체 봉을 한 사례 결과로 넣지 않는다. 어댑터는 사례별 후보군 revision을 다음 거래일 첫 봉 직전에 활성화하고, 완료된 대신 1분봉만 합친다. 결과 봉에는 `historical_case_date`를 남겨 사례 귀속을 검증한다. 완전한 1분 결과 봉이 없는 선택일은 합성하지 않고 파생 manifest의 `excluded_cases`에 남긴다.

첫 다기간 불변 표본은 `multi-period-2024-2026-v1`이다. 선택일과 결과 종료일은 2024-12-24→12-26, 2025-05-29→05-30, 2026-01-14→01-15이며, 후보군 revision 3개와 분봉 7,564개로 총 7,567개 연구 관측을 담았다. 이는 수집 도중 구조를 검증하기 위한 표본이다. 시간순 TRAIN/VALIDATION/OOS 정책과 비용 가정을 고정한 성과 비교는 수집 범위가 더 넓어진 뒤 별도 불변 요청으로 실행한다.

`scripts/plan_historical_research_split.py`는 완성된 사례만 통째로 과거순 TRAIN→VALIDATION→OOS에 배정한다. TRAIN·VALIDATION 사례 수를 명시하고 나머지를 최소 한 사례의 OOS로 남긴다. 산출물 `historical_chronological_split_plan/v1`은 source dataset ID와 revision hash, 사례별 봉 수·시간 경계, 기존 `chronological_holdout/v1` 평가 문서를 함께 고정한다. OOS는 항상 `SEALED`이고 결과를 포함하지 않는다. 첫 계획은 `data/research/historical-evaluation-plans/multi-period-2024-2026-v1.json`이며 사례를 각 1개씩 TRAIN/VALIDATION/OOS에 배정했다.

`scripts/prepare_historical_development_inputs.py`는 계획과 source ID/hash를 대조한 뒤 TRAIN·VALIDATION만 독립 `development_partition/v2` 입력으로 만든다. 역사 후보군 종류와 사후 모집단 제한을 투영 manifest에 유지하며 부분 입력의 ordinal은 새 파일 순서대로 다시 매긴다. 첫 패키지 `data/research/historical-development-inputs/multi-period-2024-2026-v1`은 두 개발 입력의 quality `PASS`와 revision 재현성 `VERIFIED`를 확인했고 `oos_included=false`다.

`scripts/prepare_historical_baseline_requests.py`는 이 OOS 없는 패키지에서 돌파·눌림 재가속의 TRAIN/VALIDATION 고정 요청 네 개만 만든다. 역사 입력에는 당시 TOP20이 없으므로 순위 Factor를 끄고, 잠정 파라미터·브로커 미검증 비용이라는 상태를 요청에 명시한다. `scripts/summarize_historical_baseline_results.py`는 실행 DB의 검열 사유까지 포함해 비교표를 만든다. 첫 구조 실행에서는 대부분의 주문이 다음 연속 1분봉 증거 부족으로 검열됐으므로 수집 확대 전 전략 선택 근거로 사용하지 않는다.

`scripts/assess_historical_development_readiness.py`는 각 분할의 모든 역사 사례에서 날짜·종목별 분봉 존재와 최소 한 연속 1분쌍을 검사한다. 이전 분할에서 복사한 재생용 후보군 seed는 분모에서 제외한다. 하나라도 빠지면 요청 생성 기본값은 `BLOCKED`이며, 최초 희소 표본처럼 실행 경로만 확인할 때에만 `--allow-partial`을 명시한다. 이 gate는 수익성이나 대표성을 보증하지 않고 현재 1분 전략이 후보 전체에서 다음 봉 체결을 검토할 최소 입력이 있는지만 판정한다.

백필 후 원본 후보에는 ETF·ETN도 남아 있으므로 복원 기록에 원본 DB의 `stocks.market_code`를 함께 동결한다. 이는 원본 DB의 현재 분류이며 후보일 당시의 시장 분류 이력이 아니다. `prepare_historical_research_input.py --individual-stocks-only`는 0/10 개별 주식만 파생 모집단과 결과 봉에 넣고 제외한 코드·시장 구분은 파생 manifest에 기록한다. 원본 후보와 원응답은 삭제하지 않는다. 2026-09-24 세 날짜 재구성에서는 원본 150개 중 149개에 선택일 1분봉이 있었고, 필터 후 TRAIN 42/42·VALIDATION 45/45에 결과일 1분봉이 있다. 각 한 종목(`000545`, `019175`)은 해당 날 14개의 봉이 30분 간격이므로 연속 1분쌍이 없고 readiness는 아직 `BLOCKED`다. 새 비용 v2 요청 네 개는 `--allow-partial` 구조 검증용으로만 생성했으며 성과 비교는 실행하지 않았다.

후보 코드에는 보통주 숫자 코드뿐 아니라 `00499K` 같은 영문 포함 6자리 단축코드도 있다. 대신 작업 원장과 두 StockChart bridge는 `[0-9A-Z]{6}`을 허용한다. 파생상품 여부나 연구 포함 여부는 후보 원장의 기존 선택을 유지하며, 코드 모양만으로 자동 제외하지 않는다.

2026-09-24 D03 품질 제외: 공급자 기록상 1분봉이더라도 해당 결과일에 연속 60초 봉이 한 쌍도 없는 종목은 사용자 결정에 따라 전략 연구 파생 입력에서 제외한다. `prepare_historical_research_input.py --individual-stocks-only --exclude-noncontinuous-minute-candidates`는 원본 복원본과 봉 DB를 바꾸지 않고 제외 종목·날짜·봉 수를 파생 manifest에 남긴다. 세 날짜의 새 파생 입력 `multi-period-2024-2026-stocks-pairs-v4`에서는 TRAIN `000545`(14봉), VALIDATION `019175`(14봉), OOS `012860`(0봉)이 제외됐으며 TRAIN 41/41·VALIDATION 44/44가 최소 연속쌍 gate를 통과한다. OOS 결과는 여전히 봉인한다. `scripts/audit_historical_minute_readiness.py`의 2024-09~2025-12 읽기 전용 감사에서는 후보일 321일 중 150일이 이 최소 조건을 충족했고 16개월 모두 후보일이 있으나, 종일 봉 완전성이나 표본 대표성은 별도로 검증해야 한다. 새 비용 계약의 첫 구조 실행은 512MB RSS 한도를 초과해 별도 1GB 한도에서 재실행했으며, 아래 체결시각 오류를 수정한 뒤 새 결과 DB를 사용한다.

2026-09-24 D03 확대 품질 감사: 위 150개 `READY` 후보일 전부의 결과일 6,327종목·일을 읽기 전용으로 조사해 [전체 결과](../data/research/historical-all-ready-bar-quality-2024-09-to-2025-12.json)에 보존했다. 1분봉 300개 미만은 263종목·일(124개 후보일에 분포), 100개 미만은 14, 10개 미만은 3이다. 이 263종목·일은 키움 일봉 거래량이 모두 양(+)이지만, 희소한 실제 거래와 공급자 미반환을 봉 개수만으로 구분할 수 없다. 일부 종목의 CREON 원주가 분봉 거래량 합과 키움 수정 일봉 거래량은 크게 다르며, 기업행동·공급자 기준의 영향을 아직 분리 검증하지 않았으므로 단순 일치율을 결측 판정으로 사용하지 않는다. 이 감사는 봉인 OOS와 가격 결과를 읽지 않았고, 150일의 최소 연속쌍 `READY`를 종일 완전성이나 전략 성과 승인으로 승격하지 않는다.

2026-09-24 D03 사례별 제외 투영: [읽기 전용 제외 원장](../data/research/historical-minute-exclusions-2024-09-to-2025-12.json)은 위 321일 원장의 SHA-256을 고정하고, 결과일에 봉이 없거나 연속 60초 봉 쌍이 없는 종목·일의 합집합만 제외한다. 전체 13,632종목·일에서 307종목·일을 제외하면 13,325종목·일이 남고 321일 모두 최소 한 후보가 남는다. 따라서 원래의 150/321은 **모든 후보가 통과해야 한다는 날짜 단위 gate**의 수치이지, 날짜 자체가 171일 결측이라는 뜻은 아니다. 이 투영은 원본·OOS·가격 결과를 읽거나 바꾸지 않았으며, 남은 사례의 종일 봉 완전성과 역사 후보의 대표성은 아직 승인하지 않는다. 실제 다기간 학습 입력은 투영 원장과 기존 복원·분할 계약을 결합해 별도로 동결해야 한다.

여기서 연속쌍은 **하루 어느 구간에든 정확히 60초 차이인 봉 두 개**라는 최소 조건이다. 장 시작시각의 봉을 요구하거나 2분 이상의 모든 공백을 종목 전체 탈락 사유로 삼지 않는다. 307건은 봉 없음 61건과 하루 전체에 연속쌍 없음 246건이며, 후자의 232건은 10개 이상 봉이 모두 29~30분 간격인 격자 사례다. 나머지 14건의 해당 종목·결과일에는 현재 KRX VI 원장에서 일치 발동을 찾지 못했다. 이는 VI가 초반 봉 공백을 만들 수 있다는 가능성을 부정하지 않으며, 개별 공백의 원인이나 체결 가능성은 주문시각별로 확인한다.

가격 제한 자체는 봉 제외 조건이 아니다. 2024-09-04 `049180`의 상한가 종가 2,560원 사례에는 CREON 원주가 1분봉 376개, 2024-12-27 `321820`의 하한가 종가 13,000원 사례에는 377개가 저장돼 있고 장 막판 가격 고정 봉에도 거래량이 있다. 반대로 `002630`의 2024-12-10 가격 제한일은 원응답 `data/historical_collection/daishin/002630/20260922T014552Z-attempt1/1m.ndjson`에 09:01~15:30의 약 30분 격자 14개만 있어 저장 단계의 누락이 아닌 공급자 원응답의 해상도 문제로 확인했다. 제한 가격 도달이나 VI를 봉 부재의 단일 원인으로 추정하지 않는다.

2026-09-24 다음 D03 다기간 구조 입력은 [월별 날짜 선택 원장](../data/research/historical-monthly-case-selection-2024-09-to-2026-01.json)으로 사전에 고정한다. 수익률·뉴스 내용·후보 성과를 보지 않고 2024-09~2025-12의 각 월 첫 후보일 16개를 선택했다. 앞 12개월은 TRAIN, 뒤 4개월은 VALIDATION이며, 기존 `multi-period-2024-2026-v1`의 2026-01-14 OOS 선택일은 봉인 계획의 날짜만 재사용한다. 이 16개 개발 날짜의 현재 분봉 DB 재감사에서 제외 종목이 기존 투영 원장과 모두 일치했고, 월별 연구 후보는 합계 657종목·일이다. 이 선택은 월별 표본을 늘리는 구조 검증이지 최종 전략 성과 승인이나 뉴스 백필 완료를 뜻하지 않는다.

선택 원장에서 `historical-reconstruction/monthly-2024-09-to-2026-01-v1`을 복원해 632,428개 원천 관측을 고정하고, 개별 주식·연속 1분쌍 제외 조건으로 `historical-strategy-input/monthly-2024-09-to-2026-01-v1`을 만들었다. 개발 16개 날짜의 파생 제외 15개와 잔존 657종목·일은 선택 원장과 전부 일치한다. 시간순 분할은 `historical-evaluation-plans/monthly-2024-09-to-2026-01-v1.json`, OOS 없는 독립 개발 입력은 `historical-development-inputs/monthly-2024-09-to-2026-01-v1`에 고정했다. 준비도 검사에서 기존 구현이 마지막 후보군만 보고, VALIDATION의 전 분할 seed를 현재 후보로 오인하는 문제를 발견해 모든 날짜·종목 쌍을 검사하도록 수정했다. [재검사 결과](../data/research/historical-development-readiness/monthly-2024-09-to-2026-01-v1.json) TRAIN 488/488, VALIDATION 169/169가 최소 연속쌍 조건에서 `READY`, `oos_included=false`다. 이는 각 종목의 장중 전체 봉 완전성이나 뉴스의 당시 가용성, 역사 후보의 대표성, 최종 전략 성과를 승인하지 않는다. 복원 당시 뉴스 백필도 진행 중이므로 이 입력은 구조 검증 스냅샷이다.

2026-09-25 선택 범위 감사는 전체 개발 후보일 321일과 고정 선택일 16일을 기존 준비도·제외 원장 SHA-256에 연결해 비교했다. 선택일의 파생 후보 657종목·일은 전체 13,325종목·일의 4.93%이며, 제외 후 일별 후보 수 중앙값은 선택일 41개·나머지 305일 42개다. 이 수량 대조는 월초 선택에 따른 시장 국면 편향이나 당시 뉴스·순위 가용성, 종일 봉 품질을 검증하지 않는다. [감사 원장](../data/research/historical-monthly-selection-coverage-20260925.json)은 가격 결과와 봉인 OOS를 읽지 않는다.

같은 날 첫 1GB TRAIN 실행의 `next_tradable_bar_gap` 262건은 모두 정각 주문시각과 다음 봉 시작시각이 같았다. 역사 봉 종료시각에 내린 결정을 다음 봉 시작시각에 체결할 수 있는데, 기존 체결 검증이 정각에도 1분을 더한 것이 직접 원인이다. 정각은 같은 분 시작, 초·마이크로초가 있으면 다음 분 시작으로 수정하고 관련 회귀 검사 45건을 통과했다. 당시 VI 겹침은 2건에 불과했고 262건 모두 이 계산 오류에 해당한다. 오류 이전 결과 DB는 비교에서 제외하고 새 DB의 TRAIN·VALIDATION 결과로만 구조 검증한다.

수정 후 `multi-period-2024-2026-stocks-pairs-v4-1g-clockfix`에서 TRAIN·VALIDATION 각각 돌파·눌림 네 실행을 완료했다. 각 TRAIN 14,975개·VALIDATION 16,539개 평가와 보고서 `ELIGIBLE`/자료 품질 `PASS`를 확인했고 OOS는 열지 않았다. [구조 비교표](../data/research/historical-development-results/multi-period-2024-2026-stocks-pairs-v4-1g-clockfix/SUMMARY.md)는 동일한 비용·체결 계약으로 계산했지만 각 분할이 하루이므로 정책 선택의 성과 근거가 아니다. 정각 체결과 실제 봉 공백·연구 구간 종료 검열을 구분하며, 비교표의 분할 창 안 검열과 전체 실행 사유 건수의 범위 차이를 명시했다. 더 넓은 기간 사례를 추가할 때는 후보일을 입력 품질만으로 사전 선택하고 가격 결과나 OOS 성과를 보고 선택하지 않는다.

2026-09-24 구현 갱신: 위 검토 뒤 `scripts/preprocess_historical_news_to_nas.py`와 인증된 `/api/v1/news/historical-jobs/claim`, `/complete`를 추가했다. 작업 선점은 `historical_backfill` 기사에 한정하고 BODY/RULE 결과·작업 완료를 같은 트랜잭션으로 저장한다. 동일 시도 재전송과 만료된 작업 시도를 구분한다. PC 작업기는 NAS 기사 원장에 이미 등록된 작업을 가져오므로 로컬 과거 DB 본문을 직접 NAS DB에 쓰지 않는다. 현재 NAS 실행 빌드에 대한 배포·표본 속도 검증은 별도이며, 검증 전 대량 실행하지 않는다.

### 네이버 증권 시황 원천 보완

종목명·날짜 검색은 전체 시장 뉴스의 대체 수단이 아니다. 네이버 증권 `FLASHNEWS`와 별도 `WORLDNEWS`를 날짜별 원천으로 분리한다. 과거 대화의 2026-09-10 표본에서 MAIN/FOCUS는 FLASH에 포함됐지만 이는 해당 날짜의 측정값이지 모든 날짜의 완전성 보증은 아니다. RANK는 날짜 조회가 현재 랭킹을 반환한 표본이 있어 과거 백필 원천으로 사용하지 않는다.

`scripts/collect_naver_stock_market_news.py`는 `data/historical_intelligence.sqlite3`의 `market_news_days/pages/articles`에 날짜·원응답·기사 ID·원문 URL·공급자가 준 발행시각을 저장한다. `--start-date`, `--end-date`, `--source flash|world|all`로 실행하고, 원장은 페이지 단위로 재개한다. 빈 공급 응답은 `empty`, 다음 날짜 이전으로 넘어간 페이지는 `complete_boundary`, 페이지 반복·미해석 행·공급 오류는 오류 또는 미완료로 남긴다. 2020-01-02 FLASH 실조회는 83페이지에서 전날로 넘어가는 경계를 확인해 당일 기사 1,232건을 저장했다. WORLD 같은 날짜는 빈 응답이었다. 파일 스냅샷은 기존 SQLite snapshot 경로로 NAS에 보존할 수 있으나, 시장 기사와 특정 종목의 과거 관련성을 자동 확정하거나 기존 `historical_reconstruction/v1` 입력에 자동 합치지 않는다. 해당 연결은 별도 검증이 필요하다.

NAS의 상시 시황 뉴스 수집기는 같은 두 원천을 날짜별 독립 cursor로 읽어 기존 `save_news_source_page()` 경로에 전달한다. 기사에 명확한 KRX 종목명이 없으면 종목 관계를 `unresolved`로 유지하며, 기존 BODY/RULE 원장과 처리 제외 공급자 정책을 따른다. NAS 이미지 재빌드·실행 증거가 확인되기 전에는 운영 수집 완료로 표시하지 않는다.

2026-09-23에는 `scripts/run_naver_stock_market_news.py`로 FLASH/WORLD 과거 전구간(2019-01-01~2026-09-22) 수집을 시작했다. 기존 종목별 뉴스 수집기의 약 86GB SQLite와 동시 쓰기를 피하기 위해 `data/naver_stock_market_news.sqlite3`에 별도 보존한다. `scripts/show_naver_stock_market_news_status.py` 또는 과거자료 수집기 모니터의 `네이버 증권 시황뉴스 (FLASH / WORLD)` 패널에서 현재 날짜·원천·마지막 저장 페이지·기사 건수·오류·전체 날짜 진행률을 확인한다. 같은 패널에서 중복 프로세스를 막은 시작/재시작과 현재 원천·날짜 완료 후 정지를 요청할 수 있다. 첫날과 이후 30일마다 일관성 검사한 불변 스냅샷을 `X:\kiwoom-monitor\deploy\synology\server-data\historical-intelligence\market-news-v1\runs`에 보관하며 `latest.json`을 원자적으로 갱신한다. `data/historical_collection/STOP_MARKET_NEWS` 파일을 만들면 현재 날짜/원천 작업이 끝난 뒤 중단하고, 동일 명령으로 재시작하면 페이지 원장에서 재개한다. 수집 중이라는 사실은 전체 완료나 연구 입력 연결을 뜻하지 않는다.

2026-09-24 과거 시황 운영 DB 연결: `scripts/import_historical_market_news_to_nas.py`는 PC의 `market_news_articles`에 제목·링크·발행시각만 확인하고 종목 관련성·불필요 기사 필터를 적용하지 않은 기사를 날짜·원천별 100건 이하로 기존 NAS 뉴스 소스 관측 API에 보낸다. 로컬 SQLite의 `nas_market_news_imports`는 재개 원장이며 기본 정보가 부족해 제외한 기사도 표시하고 NAS 운영 DB를 직접 쓰지 않는다. 기사가 종목을 정확히 언급하면 개별 주식 후보 카탈로그(`stocks.market_code` 0/10)에 한해 종목 관계를 확인하고, 종목 연결이 없어도 시황 기사는 FLASH/WORLD 탭에 남긴다. 2026-09-24 NAS에 먼저 반영한 FLASH 표본 103건은 기존 NAS BODY/RULE 작업기가 처리한다. 그 이후 PC가 올리는 기사는 `processing_owner=pc`로 `historical_market_pc_backfill` 범위에 저장한다. NAS 일반 작업기는 이 범위의 BODY/RULE을 선점하지 않고 PC 전처리 작업기(`--scope pc_market`)만 원자 선점한다. PC가 본문·원문 초 단위 시간·평가·핵심문장을 계산해 같은 NAS 뉴스 revision 경로에 인증 API로 저장한다. 주식 외 후보 코드는 PC 작업 선점에서도 제외한다. 현재 PC 원장에는 FLASH 547,575건(2019-01-01~2020-08-10), WORLD 4건만 확인되어 전체 과거 수집 완료로 보지 않는다. 대량 업로드 전 표본·중복·조회·본문 처리량과 PostgreSQL 실제 동작 검증이 필요하다.

후속 검증에서 v3 운영 NAS에 새 FLASH 표본 3건을 `processing_owner=pc`로 올리고 PC가 BODY 3건·RULE 3건을 완료했다. 네이버 검색뉴스도 신규분은 `historical_news_pc_backfill`로 분리한다. v4 서버의 일반 작업기는 이 범위의 BODY/RULE을 선점하지 않고 PC 전처리 작업기의 기본 `--scope pc`가 검색·시황 두 범위를 처리한다. 검색뉴스 업로드기는 NAS `/health.historical_news_pc_scopes`에 `search`가 없으면 기사와 로컬 업로드 원장을 쓰기 전에 거부한다. 따라서 v3가 운영 중인 동안 수집기는 원자료만 계속 모으며, v4 재빌드 검증 후 인증 API로 미반영 기사부터 업로드한다. 이미 NAS에 들어간 과거 기사는 기존 NAS 소유를 유지한다. 2020-07-08 FLASH 83페이지 반복 응답은 미완료 오류로 남겨 실제 응답을 확인한 뒤 별도 복구한다.

검색뉴스 작업 원장에서는 개별 주식 아닌 후보 308개가 과거에 검색 대상으로 남아 있었다. 현재 원장은 비주식 대기 일별 10,703건·범위 589건을 `excluded`로 표시하고 이전 상태·오류를 `last_error`에 보존한다. 이미 완료된 비주식 3,135일·225범위와 수집된 원응답·기사는 지우지 않는다. 새 작업 씨앗도 `stocks.market_code`가 개별 주식(0/10)인 후보만 만든다. 수집기 시작 시 기존 대기 작업 제외를 반복 실행해 신규 비주식 작업이 재유입돼도 검색 요청으로 이어지지 않게 한다. 진행률 분모에서 `excluded`는 뺀다.

2026-09-24 후속 결정(v5): 앞의 “기존 NAS 소유 유지”는 당시 배포 이력이며 운영 전환 목표가 아니다. NAS에 이미 들어간 `historical_backfill` 검색기사와 `historical_market_backfill` 시황기사의 미완료 BODY/RULE도 신규 PC 두 범위와 함께 PC `--scope pc`가 처리한다. NAS 일반 작업기는 네 과거 범위를 새로 claim하지 않는다. NAS에 이미 완료된 BODY/RULE·기사·원천 관측은 삭제하지 않고, 완료된 BODY에서 RULE만 남았으면 PC가 저장 본문으로 규칙만 계산한다. 검색뉴스 시각 확인에 사용한 HTML에서 본문을 함께 추출해 로컬 `news_article_body_snapshots`에 불변 저장하며 PC BODY가 이를 재사용한다. 기존 검색기사에 스냅샷이 없거나 시황 원장에 본문이 없다면 PC BODY 단계에서 한 번만 원문을 조회한다. 검색 수입은 원장의 `training_eligible=1` 및 `article_fetch_status='published_at_found'`만 허용하므로 기존 원문 불가·시각 미확인 판정 건은 재요청하지 않는다. 시황 원장에는 같은 원문 불가 판정이 아직 없으므로 첫 BODY 결과에 따라 `fulltext` 또는 `summary_only`로 기록한다. 이 소유권 전환은 v5 컨테이너 재빌드·운영 확인 후 적용된다.
