# Phase 9 거래강도 기반 기록

사용자 정의 거래강도는 키움 FID `228` 체결강도와 다른 값이다.

```text
유통시가총액(억원) = 시가총액(mac, 억원) × 유통비율(dstr_rt) ÷ 100
거래강도(%) = 기간 거래대금(억원) ÷ 유통시가총액(억원) × 100
```

계산 모듈과 단위 테스트를 추가했다. 다음 단계에서 Top 20의 `ka10001` 기본정보를 백그라운드 조회해 화면에 표시한다.
