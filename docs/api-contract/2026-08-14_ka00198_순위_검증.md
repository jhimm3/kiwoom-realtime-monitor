# ka00198 실시간종목조회순위 검증

검증일: 2026-08-14  
환경: 모의투자 (`mock`)  
요청: `POST /api/dostk/stkinfo`, `api-id: ka00198`, `qry_tp: 1` (1분)

## 결과

호출에 성공했고, 순위 목록 20개를 수신했다.

확인된 응답 필드:

| 필드 | 의미 | 화면 활용 |
| --- | --- | --- |
| `bigd_rank` | 빅데이터 순위 | 기본 순위 |
| `rank_chg`, `rank_chg_sign` | 순위 변동과 부호 | 선택 표시 |
| `stk_cd`, `stk_nm` | 종목코드, 종목명 | 종목 식별·표시 |
| `past_curr_prc` | 과거 현재가 | 초기 참고값; 실시간 현재가는 별도 시세 수신으로 갱신 필요 |
| `base_comp_chgr`, `base_comp_sign` | 기준가 대비 등락률·부호 | 등락률 표시 |
| `prev_base_chgr`, `prev_base_sign` | 직전 기준 대비 등락률·부호 | 선택 표시 |
| `dt`, `tm` | 기준 일자·시간 | 데이터 수신 시각 검증 |

## 제공되지 않은 필수 후보 데이터

이 API 응답에는 다음 필드가 없었다.

- 거래량
- 거래대금
- 시가총액
- 유통비율
- 테마

따라서 `ka00198`은 **Top 20 종목의 기준 목록과 순위/등락률 원천**으로만 사용한다. 현재가·거래량/거래대금은 실시간 시세 또는 별도 조회 API로, 테마는 SQLite/Excel로 분리한다.

## 다음 검증

1. 실시간 시세 WebSocket에서 현재가·누적 거래량·거래대금 수신 필드 확인
2. 주식기본정보에서 시가총액·유통비율 제공 여부 확인
3. 분봉/일봉에서 기간별 거래대금 산출 가능 여부 확인

